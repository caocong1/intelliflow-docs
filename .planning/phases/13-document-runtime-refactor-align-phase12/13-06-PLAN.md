---
phase: 13-document-runtime-refactor-align-phase12
plan: 06
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - packages/frontend/src/pages/projects/ProjectHome.tsx
  - packages/frontend/src/components/workspace/WorkflowPreview.tsx
  - packages/frontend/src/pages/projects/DocumentListItem.tsx
autonomous: true
requirements:
  - DOC-01
  - DOC-05

must_haves:
  truths:
    - "Workflow preview shows node list with type icons when selecting a workflow in create document modal"
    - "Document list shows progress bar and current node name for in_progress documents"
    - "All new UI text is in Chinese"
  artifacts:
    - path: "packages/frontend/src/components/workspace/WorkflowPreview.tsx"
      provides: "Lightweight workflow preview component"
    - path: "packages/frontend/src/pages/projects/ProjectHome.tsx"
      provides: "Enhanced create document modal with workflow preview"
  key_links:
    - from: "packages/frontend/src/components/workspace/WorkflowPreview.tsx"
      to: "packages/shared/src/types.ts"
      via: "WorkflowNodeDef[] rendered as preview list"
      pattern: "WorkflowNodeDef"
---

<objective>
Add workflow preview in create document modal and progress display in document list for in_progress documents.

Purpose: Users need to understand what a workflow does before selecting it, and see progress of ongoing documents.
Output: WorkflowPreview component, enhanced ProjectHome create modal, document list progress indicators.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/shared/src/types.ts
@packages/frontend/src/pages/projects/ProjectHome.tsx
@.planning/phases/13-document-runtime-refactor-align-phase12/13-02-SUMMARY.md

<interfaces>
From packages/shared/src/types.ts:
```typescript
export interface WorkflowNodeDef {
  id: string;
  type: WorkflowNodeType;
  label: string;
  position: { x: number; y: number };
  config: NodeConfig;
  outputs?: OutputDef[];
}

export type WorkflowNodeType = "input_transform" | "desensitize" | "model_call" | "restore" | "export";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorkflowPreview component + integrate into create document modal</name>
  <files>packages/frontend/src/components/workspace/WorkflowPreview.tsx, packages/frontend/src/pages/projects/ProjectHome.tsx</files>
  <action>
1. **Generate Stitch design mockup** for workflow preview using Stitch MCP (`GEMINI_3_1_PRO`). Prompt:
   - "Workflow preview panel inside a modal for Chinese enterprise document platform"
   - "SolidJS + Tailwind, vertical node list with connecting lines and type icons"
   - "Each node: type icon, label, type badge. Bottom: node count summary"
   - "Chinese: 流程预览, 输入转换, 信息脱敏, 模型调用, 信息恢复, 文件导出, 共 N 个节点"

2. **Create WorkflowPreview.tsx** — a lightweight read-only preview:
   - Props: `nodes: WorkflowNodeDef[], description?: string`
   - Render a vertical list of nodes with:
     - Type icon (use simple SVG icons or unicode characters per type)
     - Node label
     - Type badge with Chinese name: "输入转换", "信息脱敏", "模型调用", "信息恢复", "文件导出"
     - Vertical connecting lines between nodes (simple CSS borders, not full canvas)
   - Bottom summary: "共 N 个节点，预计 N 个步骤"
   - If description provided, show workflow description text

3. **Integrate into ProjectHome.tsx create document modal:**
   - After user selects a workflow from the dropdown, fetch the workflow detail (if not already loaded) to get its `nodes` array
   - Show `<WorkflowPreview>` below the workflow selector
   - If no workflow selected, show placeholder: "请选择工作流以查看流程预览"
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && test -f packages/frontend/src/components/workspace/WorkflowPreview.tsx && grep -c "流程预览\|共.*个节点" packages/frontend/src/components/workspace/WorkflowPreview.tsx && grep -c "WorkflowPreview" packages/frontend/src/pages/projects/ProjectHome.tsx</automated>
  </verify>
  <done>WorkflowPreview component renders vertical node list with Chinese labels. Integrated into create document modal in ProjectHome.</done>
</task>

<task type="auto">
  <name>Task 2: Add progress display for in_progress documents in document list</name>
  <files>packages/frontend/src/pages/projects/ProjectHome.tsx</files>
  <action>
1. For documents with status "in_progress" in the document list, show a progress indicator:
   - Progress bar showing `completedNodes / totalNodes` ratio
   - Current node label: "3/5 模型调用" format (stepOrder/totalSteps + current node's Chinese type name)
   - Node type name mapping: same Chinese names as WorkflowPreview

2. The document list data should already include enough info (document status). For progress detail, either:
   - Option A: Include progress info in the document list API response (add `currentStep` and `totalSteps` fields)
   - Option B: Derive from existing data if available

3. Use Option A: Add `progressStep` and `totalSteps` and `currentNodeLabel` fields to the document list response. In the backend document listing service, for in_progress documents, query `nodeExecutions` to get progress:
```typescript
// In document list query, for each in_progress doc, get:
// - total node count: COUNT(nodeExecutions WHERE isCurrent=true)
// - completed count: COUNT(nodeExecutions WHERE isCurrent=true AND status IN (completed, skipped))
// - current node: first nodeExecution WHERE isCurrent=true AND status=in_progress
```

4. Display in the document list row:
   - Small progress bar (Tailwind: `h-1.5 bg-indigo-500 rounded-full`)
   - Text: "进度: 3/5 · 模型调用" in a muted smaller font below the document title
   - For completed docs: show "已完成" badge
   - For draft docs: show "草稿" badge

5. All text in Chinese: "进度", "已完成", "草稿", "进行中"
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "进度\|已完成\|草稿" packages/frontend/src/pages/projects/ProjectHome.tsx</automated>
  </verify>
  <done>Document list shows progress bar + current node name for in_progress documents. Status badges for all states in Chinese.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- Create document modal shows workflow preview when workflow is selected
- Document list shows progress for in_progress documents
- All new UI text is in Chinese
</verification>

<success_criteria>
Workflow preview is visible in create document modal. Document list displays meaningful progress for ongoing documents. All Chinese localized.
</success_criteria>

<output>
After completion, create `.planning/phases/13-document-runtime-refactor-align-phase12/13-06-SUMMARY.md`
</output>
