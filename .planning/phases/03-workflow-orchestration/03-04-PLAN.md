---
phase: 03-workflow-orchestration
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - packages/frontend/src/components/workflow/config/ConfigPanel.tsx
  - packages/frontend/src/components/workflow/config/InputTransformConfig.tsx
  - packages/frontend/src/components/workflow/config/DesensitizeConfig.tsx
  - packages/frontend/src/components/workflow/config/ModelCallConfig.tsx
  - packages/frontend/src/components/workflow/config/RestoreConfig.tsx
  - packages/frontend/src/components/workflow/config/ExportConfig.tsx
  - packages/frontend/src/components/workflow/prompt/PromptEditor.tsx
  - packages/frontend/src/components/workflow/prompt/VariablePicker.tsx
  - packages/frontend/src/components/workflow/config/OutputsEditor.tsx
  - packages/frontend/src/pages/admin/WorkflowEditor.tsx
autonomous: true
requirements: [FLOW-04, FLOW-05, FLOW-06, FLOW-07, FLOW-08, FLOW-09]

must_haves:
  truths:
    - "Clicking a node opens a right-side config panel without obstructing the canvas"
    - "Admin can configure input transform node with form fields, file upload toggle, and output definitions"
    - "Admin can configure desensitize node with rule types, placeholder format, and local model selection"
    - "Admin can configure model call node with prompt template containing {{variable}} tags"
    - "Prompt editor shows upstream output references as colored inline tags"
    - "Typing {{ triggers a variable picker dropdown grouped by source node"
    - "Admin can add/name output content blocks on any node"
    - "Admin can configure restore node (shows pairing status with desensitize node)"
    - "Admin can configure export node with format selection and content mapping"
  artifacts:
    - path: "packages/frontend/src/components/workflow/config/ConfigPanel.tsx"
      provides: "Right-side slide-out container that renders type-specific config"
      min_lines: 50
    - path: "packages/frontend/src/components/workflow/prompt/PromptEditor.tsx"
      provides: "Textarea with {{ trigger for variable insertion and tag rendering"
      min_lines: 80
    - path: "packages/frontend/src/components/workflow/prompt/VariablePicker.tsx"
      provides: "Dropdown showing available variables grouped by source node"
      min_lines: 60
  key_links:
    - from: "ConfigPanel.tsx"
      to: "WorkflowEditor.tsx"
      via: "selectedNodeId signal and onConfigChange callback"
      pattern: "selectedNode"
    - from: "PromptEditor.tsx"
      to: "VariablePicker.tsx"
      via: "{{ trigger opens picker, selection inserts variable"
      pattern: "showPicker|VariablePicker"
---

<objective>
Build the right-side configuration panel with type-specific forms for all 5 node types, plus the prompt editor with {{variable}} tag system for model call nodes.

Purpose: Each node type needs a specific configuration form. The prompt editor with variable references is the key UX for connecting data flow between nodes — upstream outputs become variables in downstream prompts.
Output: ConfigPanel container, 5 type-specific config forms, PromptEditor with VariablePicker, and OutputsEditor for managing content block outputs.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-orchestration/03-CONTEXT.md
@.planning/phases/03-workflow-orchestration/03-03-SUMMARY.md

<interfaces>
<!-- From shared types (03-01) -->
```typescript
interface InputTransformConfig { type: "input_transform"; formFields: FormFieldDef[]; allowFileUpload: boolean; acceptedFileTypes?: string[]; }
interface DesensitizeConfig { type: "desensitize"; ruleTypes: string[]; placeholderFormat: string; localModelId: string | null; }
interface ModelCallConfig { type: "model_call"; displayName: string; modelId: string | null; promptTemplate: string; inputRefs: VariableRef[]; }
interface RestoreConfig { type: "restore"; pairedDesensitizeNodeId: string | null; }
interface ExportConfig { type: "export"; format: "word" | "pdf" | "markdown"; templateId: string | null; contentMapping: VariableRef[]; }
interface OutputDef { id: string; name: string; description?: string; }
interface VariableRef { nodeId: string; outputId: string; variableName: string; }
interface FormFieldDef { id: string; name: string; label: string; type: "text" | "textarea" | "file"; required: boolean; }
```

<!-- From 03-03: WorkflowEditor state -->
WorkflowEditor manages: nodes signal, edges signal, selectedNodeId signal
Node selection: clicking a node sets selectedNodeId
Config changes: callback updates the node's config in the nodes array

<!-- Available from backend (for model selection dropdown) -->
GET /api/models?providerId= → Model[] (filter for local models for desensitize node)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config panel container, outputs editor, and node config forms</name>
  <files>packages/frontend/src/components/workflow/config/ConfigPanel.tsx, packages/frontend/src/components/workflow/config/OutputsEditor.tsx, packages/frontend/src/components/workflow/config/InputTransformConfig.tsx, packages/frontend/src/components/workflow/config/DesensitizeConfig.tsx, packages/frontend/src/components/workflow/config/RestoreConfig.tsx, packages/frontend/src/components/workflow/config/ExportConfig.tsx, packages/frontend/src/pages/admin/WorkflowEditor.tsx</files>
  <action>
1. Create `ConfigPanel.tsx`:
   - Right-side slide-out panel (w-80 or w-96), positioned alongside canvas (not overlapping)
   - Shows when a node is selected (selectedNodeId is set)
   - Header: node type icon + node label (editable inline) + close button (deselects node)
   - Body: renders type-specific config component based on node.config.type
   - Footer: common OutputsEditor for managing the node's outputs
   - Receives: selectedNode, allNodes (for upstream output references), onConfigChange, onOutputsChange, onLabelChange callbacks
   - Smooth slide-in/out animation (Tailwind transition: translate-x)

2. Create `OutputsEditor.tsx`:
   - Renders the node's `outputs: OutputDef[]` as an editable list
   - Each output: name input + description input + delete button
   - "添加输出" button creates a new output with unique ID and empty name
   - This is the "内容块" concept — user just names their outputs (not files)

3. Create `InputTransformConfig.tsx`:
   - Form fields editor: list of FormFieldDef entries, each with name, label, type selector (text/textarea/file), required toggle
   - "添加字段" button to add new form field
   - Drag to reorder (or simple up/down buttons)
   - File upload toggle (allowFileUpload checkbox)
   - Accepted file types multi-select when file upload enabled

4. Create `DesensitizeConfig.tsx`:
   - Rule types: checkboxes or multi-select for common sensitive info types (姓名, 身份证号, 手机号, 地址, 银行卡号, 自定义)
   - Placeholder format input (default: `[MASKED_{type}_{index}]`)
   - Local model selector: dropdown fetching models from API, filtered to show only models from local-deployment providers
   - Show "(未选择)" placeholder when no model selected

5. Create `RestoreConfig.tsx`:
   - Paired desensitize node selector: dropdown showing all desensitize nodes in the current workflow
   - Shows pairing status: "已配对" with green indicator when paired, "未配对" with warning when not
   - Minimal config — restoration is automatic

6. Create `ExportConfig.tsx`:
   - Format selector: radio group or select for Word/PDF/Markdown
   - Template selector: placeholder for future template system (show "默认模板" for now)
   - Content mapping: list of upstream output references to include in export, using the same VariableRef picker pattern

7. Wire ConfigPanel into WorkflowEditor.tsx:
   - Add ConfigPanel to the right column of the three-column layout
   - Connect selectedNodeId, pass node data and change callbacks
   - When config changes, update the corresponding node in the nodes array signal
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter frontend tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ConfigPanel slides in when node selected. All 5 node types have working config forms. OutputsEditor allows adding/editing/removing content block outputs. Config changes propagate back to nodes array.</done>
</task>

<task type="auto">
  <name>Task 2: Prompt editor with variable tag system</name>
  <files>packages/frontend/src/components/workflow/config/ModelCallConfig.tsx, packages/frontend/src/components/workflow/prompt/PromptEditor.tsx, packages/frontend/src/components/workflow/prompt/VariablePicker.tsx</files>
  <action>
1. Create `PromptEditor.tsx`:
   - Textarea-based editor for prompt templates
   - Detects `{{` typing: when user types `{{`, opens the VariablePicker dropdown below the cursor position
   - Renders existing `{{variableName}}` references as visible text in the textarea (not replacing them with tags — textarea cannot render rich content). Instead, use a preview overlay or a split view:
     - **Approach**: Textarea for editing (plain text with {{variableName}} syntax) + a preview div below/beside that renders tags as colored inline spans
     - The preview div parses `{{...}}` patterns and renders them as colored tag chips (different colors per source node, as per user decision)
   - "插入变量" button next to textarea that also opens VariablePicker
   - System variables section: show available system variables ({{工作目录}}, {{输入目录}}, {{输出目录}}, {{脱敏规则}}) with different color from node output tags (gray/neutral color vs colored per-node)
   - On variable selection from picker: inserts `{{variableName}}` at cursor position in textarea

2. Create `VariablePicker.tsx`:
   - Dropdown/popover component
   - Groups available variables by source node: each group header shows source node icon + label
   - Under each group: list of that node's outputs (OutputDef name)
   - System variables group at bottom with distinct styling
   - Selecting an item calls onSelect with the VariableRef and closes the picker
   - Filter/search input at top for quick finding
   - Variable name is derived from: `{nodeLabel}.{outputName}` (e.g., "输入转换.原始文本")

3. Create `ModelCallConfig.tsx`:
   - Display name input
   - Model selector: dropdown fetching all active models from API (GET /api/models), showing model displayName grouped by provider
   - Input files/refs: VariableRef list showing which upstream outputs this node reads (auto-populated from prompt template references, but also editable)
   - PromptEditor component for the prompt template
   - The availableVariables prop for PromptEditor: computed from all upstream nodes (nodes connected via edges leading TO this node) and their outputs

4. Computing available variables for a node:
   - Traverse edges backward from the current node to find all upstream nodes
   - Collect all OutputDef entries from upstream nodes
   - Map to VariableRef format with nodeId, outputId, variableName
   - Pass as props to PromptEditor and VariablePicker
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter frontend tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PromptEditor renders textarea with {{ trigger opening VariablePicker. Preview shows colored tags for variables. VariablePicker groups variables by source node with system variables section. ModelCallConfig integrates model selector and prompt editor. Upstream variable computation works via edge traversal.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Clicking any node opens the config panel with type-appropriate form
- Model call config has working prompt editor with {{ trigger
- Variable picker shows upstream node outputs grouped by source
- System variables appear with distinct color
- Output content blocks can be added/edited/removed on any node
</verification>

<success_criteria>
- All 5 node types fully configurable via right-side panel
- Prompt editor supports {{variable}} insertion with visual preview
- Variable picker groups by source node and includes system variables
- Config changes saved to node state for persistence
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-orchestration/03-04-SUMMARY.md`
</output>
