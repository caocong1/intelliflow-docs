---
phase: 16-fix-version-history-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/pages/documents/VersionHistory.tsx
  - packages/frontend/src/pages/documents/DocumentDetail.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VER-02, VER-03]

must_haves:
  truths:
    - "VersionHistory.tsx uses useParams<{ documentId: string }>() matching the route :documentId parameter"
    - "All params.id references in VersionHistory.tsx are replaced with params.documentId (3 occurrences)"
    - "Version history page can load version timeline for a document (API calls receive real documentId, not undefined)"
    - "Version diff comparison route receives correct documentId for API calls"
    - "DocumentDetail.tsx is deleted from the codebase"
    - "REQUIREMENTS.md coverage count says 84 (not 85)"
  artifacts:
    - packages/frontend/src/pages/documents/VersionHistory.tsx
  key_links:
    - "VersionHistory.tsx useParams type matches App.tsx route parameter name :documentId"
    - "Eden Treaty API calls pass params.documentId as the value (keep { id: ... } as the API param key)"
---

<objective>
Fix the VersionHistory.tsx route param mismatch that makes version history completely non-functional, delete orphaned DocumentDetail.tsx, and correct the REQUIREMENTS.md coverage count.

Purpose: VER-02 (version timeline viewing) and VER-03 (version diff comparison) are implemented in backend and frontend but broken by a single route param name mismatch — `params.id` is always `undefined` because the route uses `:documentId`. This is the last known bug blocking full v1.0 requirement coverage.

Output: Working version history page, cleaned dead code, accurate requirements tracking.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-fix-version-history-dead-code-cleanup/16-RESEARCH.md
@packages/frontend/src/pages/documents/VersionHistory.tsx
@packages/frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix VersionHistory.tsx route param mismatch and delete DocumentDetail.tsx</name>
  <files>packages/frontend/src/pages/documents/VersionHistory.tsx, packages/frontend/src/pages/documents/DocumentDetail.tsx</files>
  <action>
In `packages/frontend/src/pages/documents/VersionHistory.tsx`:

1. Change the useParams type from `useParams<{ id: string }>()` to `useParams<{ documentId: string }>()`.

2. Update all 3 occurrences of `params.id` to `params.documentId`:
   - **Document fetch** (~line 44): `api.api.documents({ id: params.documentId }).get()` — NOTE: the `{ id: ... }` key is the Eden Treaty path param and stays as `id`; only the VALUE changes from `params.id` to `params.documentId`.
   - **Versions list fetch** (~line 58): `api.api.versions.get({ query: { documentId: params.documentId } })`
   - **Breadcrumb href** (~line 148): `` href={`/documents/${params.documentId}`} ``

3. Delete `packages/frontend/src/pages/documents/DocumentDetail.tsx` entirely — it is orphaned dead code (not imported in App.tsx or anywhere else). No import cleanup needed.

IMPORTANT: Do NOT change the Eden Treaty API call structure (`{ id: ... }` key). Only change the value passed from `params.id` to `params.documentId`.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "params\.id" packages/frontend/src/pages/documents/VersionHistory.tsx | grep -q "^0$" && echo "PASS: no params.id references remain" || echo "FAIL: params.id still exists"; grep -c "params\.documentId" packages/frontend/src/pages/documents/VersionHistory.tsx | grep -q "^3$" && echo "PASS: 3 params.documentId references" || echo "FAIL: wrong count of params.documentId"; test ! -f packages/frontend/src/pages/documents/DocumentDetail.tsx && echo "PASS: DocumentDetail.tsx deleted" || echo "FAIL: DocumentDetail.tsx still exists"</automated>
  </verify>
  <done>VersionHistory.tsx uses params.documentId in all 3 locations. DocumentDetail.tsx is deleted. No other files affected.</done>
</task>

<task type="auto">
  <name>Task 2: Fix REQUIREMENTS.md coverage count</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In `.planning/REQUIREMENTS.md`, update the Coverage section (around line 293-298):

Change:
```
- v1 requirements: 85 total (AIMC-08 moved to Out of Scope, RECV-03 deferred to v2)
- Mapped to phases: 85
- Satisfied: 82
```

To:
```
- v1 requirements: 84 total (AIMC-08 moved to Out of Scope, RECV-03 deferred to v2)
- Mapped to phases: 84
- Satisfied: 82
```

Rationale: RECV-03 is deferred to v2 and should not count toward v1 total. 82 satisfied + 2 pending = 84 v1 requirements.

Also update the Pending line to reflect that VER-02 and VER-03 will be satisfied after this phase executes (but leave them as Pending since this is a plan, not a completion marker).
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep "v1 requirements: 84" .planning/REQUIREMENTS.md && echo "PASS: count corrected to 84" || echo "FAIL: count not corrected"</automated>
  </verify>
  <done>REQUIREMENTS.md coverage section shows 84 total v1 requirements (not 85), with 82 satisfied, 2 pending, 1 deferred.</done>
</task>

</tasks>

<verification>
1. `grep "documentId" packages/frontend/src/pages/documents/VersionHistory.tsx` — confirms useParams type and all 3 usage sites use documentId
2. `grep "params\.id" packages/frontend/src/pages/documents/VersionHistory.tsx` — returns zero matches (no stale references)
3. `test ! -f packages/frontend/src/pages/documents/DocumentDetail.tsx` — confirms deletion
4. `grep "84 total" .planning/REQUIREMENTS.md` — confirms corrected count
5. `cd packages/frontend && bunx tsc --noEmit 2>&1 | head -20` — no new TypeScript errors from the changes
</verification>

<success_criteria>
- VersionHistory.tsx reads `params.documentId` (matching `:documentId` route) in all 3 API call sites
- DocumentDetail.tsx no longer exists in the codebase
- REQUIREMENTS.md shows 84 v1 requirements total
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/16-fix-version-history-dead-code-cleanup/16-01-SUMMARY.md`
</output>
