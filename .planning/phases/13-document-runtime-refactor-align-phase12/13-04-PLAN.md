---
phase: 13-document-runtime-refactor-align-phase12
plan: 04
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx
  - packages/frontend/src/components/workspace/nodes/ModelCompareView.tsx
autonomous: true
requirements:
  - NODE-09
  - NODE-10
  - NODE-11
  - NODE-12
  - NODE-13
  - NODE-14
  - NODE-15
  - NODE-16

must_haves:
  truths:
    - "ModelCallExecutor displays all UI text in Chinese"
    - "ModelCallExecutor uses modelIds[] from real config to determine models"
    - "SSE streaming, multi-model parallel, tab switching, retry, output selection all work"
    - "ModelCompareView shows Chinese labels for side-by-side comparison"
    - "Reconnect after SSE disconnect polls /status instead of re-triggering model call"
  artifacts:
    - path: "packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx"
      provides: "Chinese-localized model call executor with Stitch design"
    - path: "packages/frontend/src/components/workspace/nodes/ModelCompareView.tsx"
      provides: "Chinese-localized side-by-side model comparison"
  key_links:
    - from: "packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx"
      to: "packages/shared/src/types.ts"
      via: "ModelCallConfig prop with modelIds, promptTemplate, VariableRef"
      pattern: "ModelCallConfig"
---

<objective>
Redesign ModelCallExecutor and ModelCompareView with Chinese localization, Stitch-designed UI, and proper SSE reconnect handling.

Purpose: Model call is the core AI generation step — must display Chinese, use real config, and handle reconnection without re-triggering calls.
Output: Redesigned model call executor and comparison view with professional Chinese UI.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/shared/src/types.ts
@packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx
@packages/frontend/src/components/workspace/nodes/ModelCompareView.tsx
@.planning/phases/13-document-runtime-refactor-align-phase12/13-01-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-02-SUMMARY.md

<interfaces>
From packages/shared/src/types.ts:
```typescript
export interface ModelCallConfig {
  displayName?: string;
  modelIds: string[];
  promptTemplate: string;
  variables?: VariableRef[];
  systemPrompt?: string;
  outputFormat?: string;
}

export interface VariableRef {
  nodeId: string;
  outputId: string;
  label?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign ModelCallExecutor — Stitch design + Chinese UI + reconnect safety</name>
  <files>packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx</files>
  <action>
1. **Generate Stitch design mockup** for the model call executor using Stitch MCP (`GEMINI_3_1_PRO`). Prompt:
   - "AI model generation interface for Chinese enterprise document platform"
   - "SolidJS + Tailwind CSS, showing streaming output with status indicators"
   - "Tab-based multi-model view, Markdown rendered output, retry button per model, select best output"
   - "Status states: 等待中, 思考中, 生成中, 已完成, 失败"
   - "All Chinese: 模型调用, 单模型模式, 多模型对比, 重试, 选择此输出, 生成中..."

2. **Implement the redesigned UI** based on Stitch mockup with SolidJS primitives:
   - Status labels: "等待中" (waiting), "思考中" (thinking), "生成中" (generating), "已完成" (done), "失败" (failed)
   - Mode labels: "单模型模式", "多模型对比模式"
   - Buttons: "开始生成", "重试", "选择此输出", "切换视图" (Markdown/源码)
   - Tab labels: model display names from config
   - Empty state: "请选择模型并开始生成"
   - Error per model: clear Chinese error message (e.g., "网络超时，请重试", "API 调用失败", "模型不可用")

3. **SSE reconnect safety**: When component mounts with existing `outputData.models`:
   - If any model status is "completed" → display result directly, no API call
   - If any model status is "streaming" → poll `GET /api/runtime/:docId/model-call/:execId/status` every 3 seconds, show "模型生成中..." loading state, stop polling when status becomes "completed" or "failed"
   - If model status is "failed" → show error + retry button
   - If no models in outputData → allow starting generation
   - **NEVER** call the execute endpoint if models already exist in outputData

4. **Use modelIds from config**: Read `props.config.modelIds` to determine which models to call. If `modelIds` is empty or undefined, show "未配置模型" message.

5. Keep all existing functional logic: SSE streaming via fetch+ReadableStream, `Promise.allSettled` multi-model parallel, tab switching between outputs, retry individual models, select best output.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "模型调用\|生成中\|重试\|选择此输出" packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx && grep -c "modelIds" packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx</automated>
  </verify>
  <done>ModelCallExecutor displays Chinese UI with Stitch design. Uses modelIds from config. SSE reconnect polls status instead of re-triggering. All text localized.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ModelCompareView — Chinese labels + Stitch polish</name>
  <files>packages/frontend/src/components/workspace/nodes/ModelCompareView.tsx</files>
  <action>
1. **Generate Stitch design mockup** for side-by-side model comparison using Stitch MCP (`GEMINI_3_1_PRO`). Prompt:
   - "Side-by-side AI model output comparison view for Chinese enterprise platform"
   - "SolidJS + Tailwind, two-column layout with model names as headers"
   - "Each column: model name, status badge, Markdown rendered content, select button"
   - "Chinese labels: 模型对比, 选择此输出, Markdown 视图, 源码视图"

2. **Implement redesigned comparison view:**
   - Header: "模型输出对比"
   - Column headers: model display names
   - Status badges: "已完成", "生成中", "失败" with color coding (green/yellow/red)
   - View toggle: "Markdown 视图" / "源码视图"
   - Select button: "选择此输出" under each column
   - Empty column: "等待生成结果..."

3. Ensure the compare view handles 2+ models gracefully (horizontal scroll if >2).
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "模型对比\|选择此输出\|源码视图" packages/frontend/src/components/workspace/nodes/ModelCompareView.tsx</automated>
  </verify>
  <done>ModelCompareView displays Chinese labels with Stitch-designed layout. Side-by-side comparison works for 2+ models. Status badges and view toggles in Chinese.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- ModelCallExecutor uses modelIds from config, all text Chinese
- SSE streaming displays status in Chinese (等待中→思考中→生成中→已完成)
- Reconnect after page refresh polls /status, does NOT re-trigger model call
- ModelCompareView shows Chinese labels for all elements
</verification>

<success_criteria>
Model call executor is redesigned with professional Chinese UI, proper SSE reconnect behavior, and real config usage. Side-by-side comparison view is fully localized.
</success_criteria>

<output>
After completion, create `.planning/phases/13-document-runtime-refactor-align-phase12/13-04-SUMMARY.md`
</output>
