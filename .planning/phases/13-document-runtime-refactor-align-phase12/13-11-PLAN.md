---
phase: 13-document-runtime-refactor-align-phase12
plan: 11
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "进行中的文档在文档列表显示进度条和当前节点信息"
    - "脱敏节点接收上游InputTransform输出并自动触发检测"
  artifacts:
    - path: "packages/backend/src/modules/documents/documents.service.ts"
      provides: "Progress subqueries for listDocuments API"
    - path: "packages/backend/src/modules/runtime/input-transform.service.ts"
      provides: "outputData.text field for downstream data flow"
    - path: "packages/frontend/src/pages/workspace/DocumentWorkspace.tsx"
      provides: "Draft save with { data } body envelope"
---

<objective>
Re-run UAT to verify the 2 gap fixes from Plan 13-10 (progress display + desensitize upstream data) work correctly, then update UAT.md status from failed to pass.
</objective>

<context>
@.planning/phases/13-document-runtime-refactor-align-phase12/13-UAT.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-10-SUMMARY.md

## Gap Background

The UAT.md (diagnosed 2026-03-25T03:20:00Z) reported 2 failed tests:

**Test 3 (minor):** Document list progress display -- "进行中的文档只显示'进行中'状态标签，没有进度条"
- Root cause: backend missing progress subqueries
- Fix: Plan 10 added `progressStep`, `totalSteps`, `currentNodeLabel` subqueries in documents.service.ts
- Current code verified: lines 132-148 contain the subqueries

**Test 5 (blocker):** Desensitize node receiving upstream data -- "暂无输入文本，请等待上游节点完成"
- Root cause: (1) outputData missing text field, (2) draft save body shape mismatch
- Fix: Plan 10 added `text: combinedText` in input-transform.service.ts (line 266) and wrapped draft save bodies in `{ data }` envelope (lines 98, 404)
- Current code verified: all 3 fixes confirmed present in files

## What This Plan Does

This plan is a **verification-only plan**. The code fixes from Plan 13-10 are already in place. This plan:
1. Runs the 2 failed UAT tests against the fixed codebase
2. Re-tests any skipped tests that depended on the blocker (Tests 6, 7, 8, 9, 10, 11, 16)
3. Updates UAT.md status from "failed/diagnosed" to "pass" for all fixed items

Since the fixes are confirmed in code, this plan primarily involves running Playwright tests and updating documentation.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-run UAT Tests 3 and 5 (the 2 previously failed tests)</name>
  <action>
    Run the full UAT test suite for Phase 13. The key tests to verify are:

**Test 3:** Document list progress display
- Create a document and execute only the first node (leaving it in_progress)
- Return to the project document list
- Expected: Document row shows progress bar and "进度: N/M · 节点名" text

**Test 5:** Desensitize node receiving upstream InputTransform output
- Execute InputTransform node first (fill form, confirm)
- Advance to Desensitize node
- Expected: No "暂无输入文本" message; upstream text is displayed; auto-detection triggers

Also re-test dependent tests that were skipped due to Test 5 blocker:
- Test 6: Multi-source desensitize input
- Test 7: Model call node execution
- Test 8: Model comparison view
- Test 10: Restore node execution
- Test 11: Export node execution
- Test 16: Auto-save indicator

Also verify:
- Test 14: Execution round selector (if multi-round test data available)
- Test 19: Workflow editor inputSources auto-population
  </action>
  <verify>Manual Playwright browser testing of the full workflow</verify>
  <done>All previously-failed and dependent tests pass; workflow completes from input transform through export without data flow errors</done>
</task>

<task type="auto">
  <name>Task 2: Update UAT.md status to reflect verification results</name>
  <files>
    .planning/phases/13-document-runtime-refactor-align-phase12/13-UAT.md
  </files>
  <action>
    Read the current 13-UAT.md. Update the status of all previously-failed and skipped tests based on the verification results from Task 1:

For Test 3: Change status from "issue" to "pass" with note "Fixed by Plan 13-10 (progress subqueries added)"

For Test 5: Change status from "issue" to "pass" with note "Fixed by Plan 13-10 (outputData.text + draft save body shape)"

For each dependent test (6, 7, 8, 9, 10, 11, 16): Update from "skipped" to "pass" or "issue" based on actual results.

Update the summary section at the top:
- Update passed count to reflect all fixed tests
- Update issues count to reflect remaining failures (if any)
- Update gap status for both gaps from "failed" to "pass"

Add a verification note at the top: "Verified by Plan 13-11 re-run: 2026-03-27"
  </action>
  <verify>13-UAT.md file updated with correct test statuses reflecting verification results</verify>
  <done>UAT.md reflects accurate pass/fail status for all 20 tests after gap fix verification</done>
</task>

</tasks>

<verification>
- [ ] Test 3 (progress display): Pass
- [ ] Test 5 (desensitize upstream data): Pass
- [ ] Dependent tests (6, 7, 8, 9, 10, 11, 16): Status updated
- [ ] UAT.md summary counts accurate
- [ ] UAT.md gap statuses updated to pass
</verification>

<success_criteria>
- All previously-failed tests (Test 3, Test 5) pass after re-verification
- All dependent skipped tests are retried and have updated status
- UAT.md is updated with accurate current state
</success_criteria>

<output>
After completion, create `.planning/phases/13-document-runtime-refactor-align-phase12/13-11-SUMMARY.md` referencing this plan.
</output>
