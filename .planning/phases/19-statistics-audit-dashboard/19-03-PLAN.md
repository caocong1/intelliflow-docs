---
phase: 19-statistics-audit-dashboard
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - packages/frontend/src/pages/admin/stats/OverviewCharts.tsx
  - packages/frontend/src/pages/admin/StatsDashboard.tsx
requirements: [STAT-01, STAT-02, STAT-03]
autonomous: true

must_haves:
  truths:
    - "Overview tab shows 4 chart modules: call trend line chart, model distribution pie chart, user TOP bar chart, recent audit records table"
    - "Charts respond to shared filter changes and re-fetch data"
    - "Charts have tooltip on hover and clickable legend to toggle series"
  artifacts:
    - path: "packages/frontend/src/pages/admin/stats/OverviewCharts.tsx"
      provides: "4 chart modules for overview tab"
  key_links:
    - from: "OverviewCharts.tsx"
      to: "ChartContainer.tsx"
      via: "component usage"
    - from: "OverviewCharts.tsx"
      to: "statistics.ts API"
      via: "fetchTrends, fetchByModel, fetchByUser, fetchAuditByDocument (audit routes registered in Plan 01)"
---

<objective>
Build the 4 overview chart modules: call trend, model distribution, user TOP ranking, and recent audit records.

Purpose: Give admins visual insights into platform usage patterns at a glance.
Output: Fully interactive overview tab with 4 chart panels.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-statistics-audit-dashboard/19-RESEARCH.md
@.planning/phases/19-statistics-audit-dashboard/19-01-SUMMARY.md
@.planning/phases/19-statistics-audit-dashboard/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overview charts component</name>
  <files>packages/frontend/src/pages/admin/stats/OverviewCharts.tsx</files>
  <action>
Create `OverviewCharts.tsx` with props receiving the shared filters accessor.

4 chart modules arranged in a 2x2 grid (grid-cols-2 on large, grid-cols-1 on mobile):

1. **调用趋势图** (Call Trend) — Area/line chart:
   - Fetch from `fetchTrends(filters)` via createResource keyed on filters
   - X-axis: period dates, Y-axis: callCount
   - Use area chart style (areaStyle with low opacity fill)
   - Show tooltip with date + call count + total tokens

2. **模型分布** (Model Distribution) — Pie/doughnut chart:
   - Fetch from `fetchByModel(filters)` — use the summary data (not trends)
   - Pie chart showing call count proportion per model
   - Use doughnut style (radius: ['40%', '70%'])
   - Show model name + percentage in tooltip

3. **用户 TOP 排名** (User TOP Ranking) — Horizontal bar chart:
   - Fetch from `fetchByUser(filters)` — take top 10
   - Horizontal bar chart: Y-axis = user names, X-axis = call count
   - Sort descending by call count

4. **最近审计记录** (Recent Audit) — Simple table (not a chart):
   - Fetch from `fetchAuditByDocument(filters)` with limit=10
   - Show columns: 时间, 用户, 流程, 模型, Token数, 耗时, 成本
   - Use existing Table component styling pattern
   - Each row is a summary, not expandable here (detailed audit is in the Audit tab)

Each chart panel: white card with rounded corners, shadow, padding, title at top.
Use ChartContainer for the 3 ECharts panels. Configure tooltip and legend per research doc guidelines.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs/packages/frontend && test -f src/pages/admin/stats/OverviewCharts.tsx && grep -q "ChartContainer" src/pages/admin/stats/OverviewCharts.tsx</automated>
  </verify>
  <done>Overview tab displays 4 chart/table modules in 2x2 grid; all respond to filter changes</done>
</task>

<task type="auto">
  <name>Task 2: Wire overview charts into dashboard tabs</name>
  <files>packages/frontend/src/pages/admin/StatsDashboard.tsx</files>
  <action>
Update StatsDashboard.tsx:

1. Import OverviewCharts component.
2. In the "总览" tab content area (first tab, the default), render `<OverviewCharts filters={filters} />`.
3. Ensure the overview tab is shown by default when the page loads.
4. The other 4 tabs remain as placeholder content for now (subsequent plans fill them).
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs/packages/frontend && grep -q "OverviewCharts" src/pages/admin/StatsDashboard.tsx</automated>
  </verify>
  <done>Overview tab renders all 4 chart modules; switching tabs hides/shows content</done>
</task>

</tasks>

<verification>
- `bun run check` passes
- Overview tab shows trend chart, pie chart, bar chart, and audit table
- Charts update when date range or filters change
</verification>

<success_criteria>
Admin sees a complete overview tab with call trend, model distribution, user ranking, and recent audit records — all driven by live data and responsive to filter changes.
</success_criteria>

<output>
After completion, create `.planning/phases/19-statistics-audit-dashboard/19-03-SUMMARY.md`
</output>
