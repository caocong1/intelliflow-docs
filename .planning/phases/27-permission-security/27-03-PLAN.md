---
phase: 27-permission-security
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - packages/backend/src/modules/runtime/desensitize.routes.ts
  - packages/backend/src/modules/runtime/model-call.routes.ts
  - packages/backend/src/modules/runtime/restore.routes.ts
  - packages/backend/src/modules/runtime/inline-edit.routes.ts
  - packages/backend/src/modules/runtime/input-transform.routes.ts
autonomous: true
requirements:
  - PERM-03
  - PERM-05

must_haves:
  truths:
    - "desensitize.routes.ts uses canEditDocument for detect and confirm, but keeps GET rules on isDocumentProjectMember"
    - "model-call.routes.ts treats execute and retry (GET but mutating), select, revalidate, and ai-fix as creator-or-owner actions, while status stays membership-based"
    - "restore.routes.ts, inline-edit.routes.ts, and input-transform.routes.ts all require canEditDocument before any write or file-system side effect"
  artifacts:
    - path: "packages/backend/src/modules/runtime/desensitize.routes.ts"
      provides: "creator-or-owner guards on desensitize mutations and membership guard on rules"
      contains: "canEditDocument"
    - path: "packages/backend/src/modules/runtime/model-call.routes.ts"
      provides: "creator-or-owner guards on model-call mutations and membership guard on status polling"
      contains: "canEditDocument"
    - path: "packages/backend/src/modules/runtime/restore.routes.ts"
      provides: "creator-or-owner guards on restore execution and text update"
      contains: "canEditDocument"
    - path: "packages/backend/src/modules/runtime/inline-edit.routes.ts"
      provides: "creator-or-owner guard on inline-edit streaming"
      contains: "canEditDocument"
    - path: "packages/backend/src/modules/runtime/input-transform.routes.ts"
      provides: "creator-or-owner guards on upload and confirm"
      contains: "canEditDocument"
  key_links:
    - from: "model-call.routes.ts::execute/retry/select/revalidate/ai-fix"
      to: "versions.service.ts::canEditDocument"
      via: "route guard before mutating nodeExecutions or starting model work"
      pattern: "await canEditDocument("
    - from: "desensitize.routes.ts::GET rules"
      to: "versions.service.ts::isDocumentProjectMember"
      via: "read-only access gate"
      pattern: "await isDocumentProjectMember("
    - from: "model-call.routes.ts::GET status"
      to: "versions.service.ts::isDocumentProjectMember"
      via: "read-only polling gate"
      pattern: "await isDocumentProjectMember("
---

<objective>
Apply the Phase 27 creator-or-owner guard across all runtime subroutes that mutate document workflow state, files, or model outputs.

Purpose: Close the remaining permission gap outside `runtime.routes.ts` while leaving genuinely read-only subroute endpoints accessible to ordinary project members.
Output: Desensitize, model-call, restore, inline-edit, and input-transform routes updated to use `canEditDocument()` only where the handler is mutating or action-driving.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/27-permission-security/27-01-SUMMARY.md

@packages/backend/src/modules/runtime/desensitize.routes.ts
@packages/backend/src/modules/runtime/model-call.routes.ts
@packages/backend/src/modules/runtime/restore.routes.ts
@packages/backend/src/modules/runtime/inline-edit.routes.ts
@packages/backend/src/modules/runtime/input-transform.routes.ts
@packages/backend/src/modules/runtime/desensitize.service.ts
@packages/backend/src/modules/runtime/model-call.service.ts
@packages/backend/src/modules/runtime/restore.service.ts
@packages/backend/src/modules/runtime/inline-edit.service.ts
@packages/backend/src/modules/runtime/input-transform.service.ts
@packages/backend/src/modules/versions/versions.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move desensitize, restore, inline-edit, and input-transform mutations to canEditDocument()</name>
  <files>packages/backend/src/modules/runtime/desensitize.routes.ts, packages/backend/src/modules/runtime/restore.routes.ts, packages/backend/src/modules/runtime/inline-edit.routes.ts, packages/backend/src/modules/runtime/input-transform.routes.ts</files>
  <read_first>packages/backend/src/modules/runtime/desensitize.routes.ts, packages/backend/src/modules/runtime/restore.routes.ts, packages/backend/src/modules/runtime/inline-edit.routes.ts, packages/backend/src/modules/runtime/input-transform.routes.ts, packages/backend/src/modules/runtime/desensitize.service.ts, packages/backend/src/modules/runtime/restore.service.ts, packages/backend/src/modules/runtime/inline-edit.service.ts, packages/backend/src/modules/runtime/input-transform.service.ts, packages/backend/src/modules/versions/versions.service.ts</read_first>
  <action>Import `canEditDocument` into all four route modules. In `desensitize.routes.ts`, switch both `POST /:documentId/desensitize/:nodeExecutionId/detect` and `POST /:documentId/desensitize/:nodeExecutionId/confirm` to `await canEditDocument(params.documentId, user!.id)` because both endpoints are part of the editable desensitization flow and `confirm` persists DB changes. Keep `GET /:documentId/desensitize/:nodeExecutionId/rules` on `isDocumentProjectMember` because it is read-only. In `restore.routes.ts`, switch both `POST /:documentId/restore/:nodeExecutionId/execute` and `PUT /:documentId/restore/:nodeExecutionId/text` to `canEditDocument`. In `inline-edit.routes.ts`, switch `POST /:documentId/inline-edit/:nodeExecutionId/stream` to `canEditDocument` because it triggers model execution and logs a model call. In `input-transform.routes.ts`, switch both `POST /:documentId/input-transform/:nodeExecutionId/upload` and `POST /:documentId/input-transform/:nodeExecutionId/confirm` to `canEditDocument` because they write files or mutate node execution data. Update each 403 message to creator-or-owner wording that matches the action being blocked.</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "canEditDocument\\(|isDocumentProjectMember\\(" packages/backend/src/modules/runtime/desensitize.routes.ts packages/backend/src/modules/runtime/restore.routes.ts packages/backend/src/modules/runtime/inline-edit.routes.ts packages/backend/src/modules/runtime/input-transform.routes.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `desensitize.routes.ts` uses `canEditDocument` in both `detect` and `confirm`, but `rules` still uses `isDocumentProjectMember`
    - `restore.routes.ts`, `inline-edit.routes.ts`, and `input-transform.routes.ts` each import and use `canEditDocument`
    - The modified 403 responses in these four files mention `仅文档创建者或项目负责人`
  </acceptance_criteria>
  <done>All non-export runtime subroutes that perform editing, uploads, model execution, or DB mutation now require creator-or-owner access.</done>
</task>

<task type="auto">
  <name>Task 2: Reclassify model-call routes by actual side effects, not HTTP verb</name>
  <files>packages/backend/src/modules/runtime/model-call.routes.ts</files>
  <read_first>packages/backend/src/modules/runtime/model-call.routes.ts, packages/backend/src/modules/runtime/model-call.service.ts, packages/backend/src/modules/versions/versions.service.ts</read_first>
  <action>In `packages/backend/src/modules/runtime/model-call.routes.ts`, import `canEditDocument` alongside `isDocumentProjectMember`. Change these exact handlers to guard with `await canEditDocument(params.documentId, user!.id)`: `GET /:documentId/model-call/:nodeExecutionId/execute`, `GET /:documentId/model-call/:nodeExecutionId/retry/:modelId`, `POST /:documentId/model-call/:nodeExecutionId/select`, `POST /:documentId/model-call/:nodeExecutionId/models/:modelId/revalidate`, and `POST /:documentId/model-call/:nodeExecutionId/models/:modelId/ai-fix`. Treat the two GET routes as writes anyway, because `executeModelCall()` initializes `nodeExecutions.outputData` and `retryModelCall()` rewrites stored model output. Leave only `GET /:documentId/model-call/:nodeExecutionId/status` on `isDocumentProjectMember`, because it only reads the existing `outputData.models` map. Keep the handler bodies, SSE streaming code, and validation logic unchanged after the permission swap.</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "execute|retry|select|status|revalidate|ai-fix|canEditDocument\\(|isDocumentProjectMember\\(" packages/backend/src/modules/runtime/model-call.routes.ts</verify>
  <acceptance_criteria>
    - `model-call.routes.ts` uses `canEditDocument` in `execute`, `retry`, `select`, `revalidate`, and `ai-fix`
    - `model-call.routes.ts` keeps `status` on `await isDocumentProjectMember(params.documentId, user!.id)`
    - No SSE header, validation, or DB-update code is removed from the modified handlers
  </acceptance_criteria>
  <done>Model-call permissions now reflect actual document-mutating behavior instead of the route's HTTP verb.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/backend/tsconfig.json` passes
- `desensitize.routes.ts`, `model-call.routes.ts`, `restore.routes.ts`, `inline-edit.routes.ts`, and `input-transform.routes.ts` import `canEditDocument`
- Read-only routes (`desensitize rules`, `model-call status`) still use `isDocumentProjectMember`
- All mutation-driving subroute handlers guard before any service call that writes files, DB rows, or model outputs
</verification>

<success_criteria>
- Regular members can no longer use subroute actions that mutate runtime artifacts or trigger write-path model work
- Read-only subroute endpoints still work for project members
- The permission split is explicit in code and verifiable route by route
</success_criteria>

<output>
After completion, create `.planning/phases/27-permission-security/27-03-SUMMARY.md`
</output>
