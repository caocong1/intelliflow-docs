---
phase: 05-document-creation-runtime
plan: 08
type: execute
wave: 5
depends_on: ["05-03", "05-04", "05-05", "05-06", "05-07"]
files_modified:
  - packages/frontend/src/components/workspace/InlineEditor.tsx
  - packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx
  - packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx
  - packages/frontend/src/components/workspace/nodes/RestoreExecutor.tsx
  - packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
  - packages/frontend/src/pages/documents/DocumentDetail.tsx
autonomous: true
requirements: [NOPS-02, NOPS-03, RECV-03]

must_haves:
  truths:
    - "User can expand an inline Markdown WYSIWYG editor to modify current node output at any node"
    - "Edits auto-save immediately via draft save API (no timer)"
    - "Skip button appears only for nodes with skippable=true and jumps to next required node"
  artifacts:
    - path: "packages/frontend/src/components/workspace/InlineEditor.tsx"
      provides: "Markdown WYSIWYG editor component with auto-save"
    - path: "packages/frontend/src/pages/workspace/DocumentWorkspace.tsx"
      provides: "Updated workspace with inline editor toggle and skip logic"
  key_links:
    - from: "packages/frontend/src/components/workspace/InlineEditor.tsx"
      to: "/api/runtime/:documentId/nodes/:nodeExecutionId/draft"
      via: "PUT on every change for auto-save"
      pattern: "draft"
---

<objective>
Add inline Markdown WYSIWYG editor for node output editing, wire up skip logic for optional nodes, and handle RECV-03 (deferred — cancel AI generation not implemented in v1 per user decision, mark as acknowledged).

Purpose: Polish the workspace with editing capabilities and complete common operations.
Output: Inline editor component, skip wiring, auto-save on edit.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-document-creation-runtime/05-CONTEXT.md
@.planning/phases/05-document-creation-runtime/05-02-SUMMARY.md
@.planning/phases/05-document-creation-runtime/05-05-SUMMARY.md

<interfaces>
NodeExecution { ..., outputData, ... }
saveNodeDraft(documentId, nodeExecutionId, data) — PUT /runtime/:documentId/nodes/:nodeExecutionId/draft
skipNode(documentId, nodeExecutionId, userId) — POST /runtime/:documentId/skip/:nodeExecutionId
Node config fields: autoAdvance, allowEdit, skippable (all boolean, optional, from workflow definition)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline Markdown WYSIWYG editor component</name>
  <files>packages/frontend/src/components/workspace/InlineEditor.tsx</files>
  <action>
**InlineEditor.tsx** — Reusable Markdown WYSIWYG editor:
- Props: `content: string, onChange: (content: string) => void, readOnly: boolean, placeholder?: string`
- Implementation approach: use a contenteditable div with Markdown rendering (lightweight WYSIWYG):
  - Render markdown to HTML for display (bold, italic, headings, lists, code blocks)
  - On input events, extract text content and re-parse
  - Toolbar at top: Bold (Ctrl+B), Italic (Ctrl+I), Heading (H1/H2/H3 dropdown), List (bullet/numbered), Code block
  - Each toolbar button wraps selection with corresponding Markdown syntax
  - Support Ctrl+Z / Cmd+Z undo (native browser undo works with contenteditable)
- Styling: border rounded-lg, padding, min-height 200px, max-height 60vh with overflow scroll
- Focus ring with indigo border on focus

Alternative simpler approach (Claude's discretion if contenteditable is too complex):
- Use a split view: left textarea for Markdown source, right preview panel
- Toolbar buttons insert Markdown syntax at cursor position in textarea
- Preview updates on every keystroke

Either way, the component must:
- Call onChange on every edit (for auto-save)
- Support readOnly mode (no editing, just rendered view)
- Handle empty content gracefully with placeholder
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs/packages/frontend && bun run tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>InlineEditor component renders Markdown with editing toolbar, calls onChange on edit, supports readOnly mode</done>
</task>

<task type="auto">
  <name>Task 2: Wire inline editor into workspace + skip logic + RECV-03 acknowledgement</name>
  <files>packages/frontend/src/pages/workspace/DocumentWorkspace.tsx, packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx, packages/frontend/src/components/workspace/nodes/ModelCallExecutor.tsx, packages/frontend/src/components/workspace/nodes/RestoreExecutor.tsx</files>
  <action>
**DocumentWorkspace.tsx updates:**

1. **Inline editor toggle:**
   - Add an "编辑输出" (Edit Output) button below the node executor area
   - Only shown when current node's config has `allowEdit: true` (or allowEdit is undefined, default to true)
   - Click toggles the InlineEditor open/closed
   - InlineEditor content: the current node's outputData main text content (extract the relevant text field from outputData based on node type)
   - On every InlineEditor onChange: immediately call PUT /draft with updated outputData (auto-save per user decision — on modification, not timed)
   - Show a subtle "已保存" (Saved) indicator that briefly appears after each save

2. **Skip button enhancement:**
   - The skip button in the bottom action bar was added in Plan 02 as conditional
   - Verify: skip button only shows when current node config has `skippable: true`
   - When clicked, call POST /skip endpoint
   - The runtime.service.skipNode should find the next non-skippable node or next pending node
   - If NOPS-03 says "skip to next required node or export", the skip should advance past all consecutive skippable nodes until hitting a non-skippable one

3. **Node executor integration:**
   - Update InputTransformExecutor: after confirm, if allowEdit is true, show InlineEditor toggle for editing the confirmed text output
   - Update ModelCallExecutor: after output selection, if allowEdit is true, show InlineEditor for editing the selected output before confirming
   - Update RestoreExecutor: after restore, the text is already editable inline (from Plan 06), but also support the InlineEditor toggle for full WYSIWYG editing

4. **RECV-03 acknowledgement:**
   - Per user decision in CONTEXT.md, RECV-03 (cancel AI generation) is deferred to a later version
   - No cancel button needed in the model call executor
   - Add a comment in ModelCallExecutor.tsx: `// RECV-03: Cancel AI generation deferred to v2 per user decision`

5. **DocumentDetail.tsx refinement:**
   - Ensure "进入工作台" button is prominently shown for draft/in_progress documents
   - For completed documents, show "查看工作台" (View Workspace) that opens workspace in read-only mode
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs/packages/frontend && bun run tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Inline editor works at every node with allowEdit, auto-saves on each change, skip jumps to next required node, RECV-03 acknowledged as deferred</done>
</task>

</tasks>

<verification>
- At any node with allowEdit: click "编辑输出" -> WYSIWYG editor opens with node output content
- Edit text -> "已保存" indicator appears, draft saved to server
- Refresh page -> edited content persists (auto-save worked)
- At a skippable node: click "跳过" -> advances past all consecutive skippable nodes
- Ctrl+Z undoes last edit
- `bun run tsc --noEmit` passes
</verification>

<success_criteria>
Inline Markdown editor available at all editable nodes with immediate auto-save. Skip logic works for optional nodes. All NOPS and RECV requirements addressed (RECV-03 explicitly deferred per user decision).
</success_criteria>

<output>
After completion, create `.planning/phases/05-document-creation-runtime/05-08-SUMMARY.md`
</output>
