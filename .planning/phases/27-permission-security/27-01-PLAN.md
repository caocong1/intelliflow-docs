---
phase: 27-permission-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/modules/versions/versions.service.ts
autonomous: true
requirements:
  - PERM-01

must_haves:
  truths:
    - "versions.service.ts exports canEditDocument(documentId, userId) and returns true when the user is the document creator or has projectMembers.role === 'owner' on the document's project"
    - "isDocumentProjectMember(documentId, userId) still exists as the read-only membership helper"
    - "Route modules can import canEditDocument from the same helper module they already use for document membership checks"
  artifacts:
    - path: "packages/backend/src/modules/versions/versions.service.ts"
      provides: "canEditDocument write-access helper plus the existing isDocumentProjectMember read-access helper"
      contains: "export async function canEditDocument"
  key_links:
    - from: "versions.service.ts::canEditDocument"
      to: "documents.createdBy + projectMembers.role"
      via: "creator-or-owner boolean check"
      pattern: "createdBy"
---

<objective>
Add the reusable document-edit authorization helper required by Phase 27.

Purpose: Centralize the Phase 27 rule once so every runtime write route can enforce the same creator-or-owner policy.
Output: `canEditDocument(documentId, userId)` exported from `versions.service.ts`, with `isDocumentProjectMember()` kept intact for read-only routes.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@packages/backend/src/modules/versions/versions.service.ts
@packages/backend/src/modules/documents/documents.routes.ts
@packages/backend/src/modules/projects/projects.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canEditDocument() beside the existing membership helper</name>
  <files>packages/backend/src/modules/versions/versions.service.ts</files>
  <read_first>packages/backend/src/modules/versions/versions.service.ts, packages/backend/src/modules/documents/documents.routes.ts, packages/backend/src/modules/projects/projects.service.ts</read_first>
  <action>In `packages/backend/src/modules/versions/versions.service.ts`, keep `isDocumentProjectMember(documentId, userId)` as the broad read-access helper, then add a new exported helper directly below it with the exact signature `export async function canEditDocument(documentId: string, userId: string): Promise<boolean>`. Implement the write rule for Phase 27 as "document creator OR project owner". Use the existing `documents` and `projectMembers` tables already imported in this file, and read both `documents.createdBy` and the current user's membership row for the document's project. A concrete implementation target is: select the document row, left-join `projectMembers` on `documents.projectId = projectMembers.projectId`, `projectMembers.userId = userId`, and `projectMembers.role = "owner"`, then return `true` only when a document row exists and either `row.createdBy === userId` or the joined owner membership row is non-null. Do not weaken `isDocumentProjectMember()` or change its semantics.</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "export async function canEditDocument|createdBy|role, \"owner\"|isDocumentProjectMember" packages/backend/src/modules/versions/versions.service.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `packages/backend/src/modules/versions/versions.service.ts` contains `export async function canEditDocument(documentId: string, userId: string): Promise<boolean>`
    - `canEditDocument` checks both `documents.createdBy` and owner membership via `projectMembers.role === "owner"`
    - `packages/backend/src/modules/versions/versions.service.ts` still exports `isDocumentProjectMember(documentId: string, userId: string): Promise<boolean>`
  </acceptance_criteria>
  <done>`versions.service.ts` exposes both access helpers: membership for reads and creator-or-owner for writes.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/backend/tsconfig.json` passes
- `versions.service.ts` exports `canEditDocument`
- `canEditDocument` encodes the exact Phase 27 rule: creator OR project owner
- `isDocumentProjectMember` remains available for read-only routes
</verification>

<success_criteria>
- A single shared helper now represents the Phase 27 write policy
- The helper is implementable without changing route semantics yet
- Read-only membership checks are preserved for downstream route plans
</success_criteria>

<output>
After completion, create `.planning/phases/27-permission-security/27-01-SUMMARY.md`
</output>
