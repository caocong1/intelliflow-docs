项目
    成员
        邀请、退出，管理员退出时必须移交权限
    主要功能，生成文档。 

    项目资料库中，分类存放，有版本号

文档
    分类（关键！！！！）


不同类型文档
    编排流程

全局
    文档类型
    流程节点


一个类型文档，对应多个流程  1:N


A类文档， 编排流程，a1流程，a2流程，a3流程，以此类推，多个流程

流程，添加节点，差异点：


1.流程节点不同，调用次数，顺序，迭代次数，不同。
2.同样的节点，如果有附带模板，模板不同，提示词不同，变量不同。



 人工审核 这版先不做

 信息录入，名称改输入转换

 选择资料、大纲确认，先不做

 信息脱敏、信息恢复，本地AI处理节点

 AI生成、AI质检、合规检查、一致性检查、查重检测，都是AI处理节点（模型调用），合并

 
输入转换、信息脱敏、信息恢复，模型调用
先定好就这4个节点，在编排流程节点时，比如要加合规检查节点，就选择模型调用添加，然后定义其中的输入输出，调用什么哪些模型等等设置

流程节点一起编排


