---
phase: 23-output-path-grammar-file-slots-export-contentmapping
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - packages/frontend/src/components/workflow/config/InputTransformConfig.tsx
  - packages/frontend/src/components/workflow/config/ExportConfig.tsx
  - packages/frontend/src/components/workflow/prompt/VariablePicker.tsx
  - packages/frontend/src/components/workflow/prompt/PromptEditor.tsx
  - packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx
autonomous: false
requirements: [OUTPUT-PATH-03, FILE-SLOT-03, EXPORT-CM-03]

must_haves:
  truths:
    - "machineKey input appears in advanced settings collapsible per form field with real-time format validation"
    - "fileSlotId and fileSlotLabel appear in advanced settings collapsible for file-type fields"
    - "segmentKey cross-type collision shows field-level real-time warning"
    - "Each file slot renders as independent card with fileSlotLabel title and drag-upload zone in executor"
    - "File slot cards appear in config order first; non-slot file fields appear as 'other files' area after"
    - "VariablePicker shows type icons (T for text, file icon for file slot, robot for model) grouped by node"
    - "Selected variable shown as readable label chip in PromptEditor"
    - "ExportConfig contentMapping uses VariablePicker for selection, supports drag reorder"
    - "Variable path hints shown only on admin config page, not on user execution page"
  artifacts:
    - path: "packages/frontend/src/components/workflow/config/InputTransformConfig.tsx"
      provides: "Advanced settings collapsible with machineKey + fileSlotId/fileSlotLabel inputs"
      contains: "machineKey"
    - path: "packages/frontend/src/components/workflow/config/ExportConfig.tsx"
      provides: "ContentMapping config with VariablePicker + drag reorder list"
      contains: "contentMapping"
    - path: "packages/frontend/src/components/workflow/prompt/VariablePicker.tsx"
      provides: "Type-icon prefixed output items, segmentKey-based selection"
      contains: "segmentKey"
    - path: "packages/frontend/src/components/workflow/prompt/PromptEditor.tsx"
      provides: "Readable label chips for selected variables"
      contains: "segmentKey"
    - path: "packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx"
      provides: "Independent file slot card rendering with mixed layout"
      contains: "fileSlotId"
  key_links:
    - from: "InputTransformConfig.tsx"
      to: "types.ts"
      via: "FormFieldDef.machineKey, fileSlotId, fileSlotLabel"
      pattern: "machineKey|fileSlotId"
    - from: "VariablePicker.tsx"
      to: "derive-outputs.ts"
      via: "OutputDef.segmentKey for variable selection"
      pattern: "segmentKey"
    - from: "ExportConfig.tsx"
      to: "VariablePicker.tsx"
      via: "Reuses VariablePicker for contentMapping item selection"
      pattern: "VariablePicker"
    - from: "InputTransformExecutor.tsx"
      to: "input-transform confirm API"
      via: "Passes slotId per file in confirm payload"
      pattern: "slotId"
---

<objective>
Implement all frontend changes: config panels with advanced settings (machineKey, fileSlot), executor with file slot cards, VariablePicker with type icons and segmentKey, PromptEditor label chips, and ExportConfig contentMapping with drag reorder.

Purpose: Makes the output path grammar, file slots, and export contentMapping visible and configurable in the UI.
Output: Updated config panels, executor, variable picker, prompt editor, and export config.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-output-path-grammar-file-slots-export-contentmapping/23-CONTEXT.md
@.planning/phases/23-output-path-grammar-file-slots-export-contentmapping/23-RESEARCH.md
@.planning/phases/23-output-path-grammar-file-slots-export-contentmapping/23-01-SUMMARY.md
@.planning/phases/23-output-path-grammar-file-slots-export-contentmapping/23-02-SUMMARY.md

<interfaces>
<!-- Types and contracts from Plans 01 and 02 -->

From packages/shared/src/types.ts (after Plan 01):
```typescript
export interface OutputDef {
  id: string;
  name: string;
  description?: string;
  segmentKey?: string;
}

export interface VariableRef {
  nodeId: string;
  outputId: string;       // stores segmentKey
  variableName: string;
  fieldPath?: string;
}

export interface FormFieldDef {
  id: string;
  label: string;
  type: FormFieldType;
  required: boolean;
  machineKey?: string;
  fileSlotId?: string;
  fileSlotLabel?: string;
  fileCountMode?: "single" | "unlimited";
  acceptedFileTypes?: string[];
  options?: string[];
  defaultValue?: string;
  defaultValues?: string[];
}
```

From derive-outputs.ts (after Plan 01):
```typescript
// OutputDef.id format: ${nodeId}-field-${segmentKey}, ${nodeId}-fileslot-${segmentKey}, ${nodeId}-model-${modelId}
// OutputDef.segmentKey: machineKey || field.id for fields, fileSlotId for slots, modelId for models
// Type can be determined from id prefix after nodeId: "field", "fileslot", "model"
```

From model-call.service.ts (after Plan 02):
```typescript
export function resolveRef(
  ref: { nodeId: string; outputId: string; fieldPath?: string },
  nodeExecs: Array<{ nodeId: string; outputData: Record<string, unknown> | null }>,
): string | undefined;
// Priority: fieldsByKey -> fields -> fileSlots -> namedOutputs -> models -> direct
```

From input-transform.service.ts (after Plan 02):
```typescript
// confirmInputTransform now expects fileOutputs items with optional slotId:
// fileOutputs: Array<{ fileId: string; name: string; parsedText: string; slotId?: string }>
// outputData now includes fieldsByKey and fileSlots
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InputTransformConfig advanced settings + VariablePicker segmentKey + PromptEditor chips</name>
  <files>packages/frontend/src/components/workflow/config/InputTransformConfig.tsx, packages/frontend/src/components/workflow/prompt/VariablePicker.tsx, packages/frontend/src/components/workflow/prompt/PromptEditor.tsx</files>
  <action>
**InputTransformConfig.tsx — Advanced settings collapsible per field:**

Add a collapsible "advanced settings" section below each form field's basic config (label, type, required, options). Per user decisions:

1. **machineKey input** (all field types): Text input with placeholder like `project_name`. Real-time inline validation with regex `/^[a-zA-Z_][a-zA-Z0-9_]*$/` — show red text below input on violation: "标识符格式错误，仅允许字母、数字和下划线，不能以数字开头". On valid input, also check cross-type segmentKey uniqueness: collect all segmentKeys within the same node (machineKey values + fileSlotId values), if collision detected show: "与 [xx] 字段的标识符冲突".

2. **fileSlotId + fileSlotLabel inputs** (only for `type === "file"` fields): Two additional text inputs in the same advanced settings area. fileSlotId has same format validation as machineKey and same cross-type collision check. fileSlotLabel is free text (display name for the slot card).

3. **Default collapsed state.** Show a subtle dot indicator when any advanced setting has a value configured (machineKey, fileSlotId, or fileSlotLabel is non-empty).

Use a simple `<details>` or a SolidJS Show + signal toggle for the collapsible. Style consistently with existing config panel patterns.

**VariablePicker.tsx — Type icons + segmentKey:**

1. **Type icon prefix:** Determine output type from `OutputDef.id` — split after the nodeId prefix to extract `field`, `fileslot`, `model`, `desensitized`, `restored`. Show icon before each output name:
   - `field` -> "T" text badge (or a simple `<span class="...">T</span>`)
   - `fileslot` -> file icon (use an inline SVG or unicode character)
   - `model` -> robot/AI icon (use an inline SVG or unicode character)
   - `desensitized`/`restored` -> shield or lock icon
   Per user decision: grouped by node, each group title = node name.

2. **Selection uses segmentKey:** When user selects a variable, construct `VariableRef` with `outputId: output.segmentKey || output.id` (not the full `OutputDef.id`). This is critical — the `outputId` field now stores segmentKey for resolveRef() compatibility.

3. **variableName format:** Set to `${nodeId}.${output.name}` for display (e.g., "n1.项目名称").

4. No search/filter needed per user decision.

**PromptEditor.tsx — Label chips:**

Update variable chip rendering: when displaying an inserted variable reference, show it as a readable label chip like `[n1.项目名称]` using the `variableName` field. The underlying stored format remains `{{nodeId.segmentKey}}`. Ensure existing chip rendering logic works with the new segmentKey-based outputId.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter @intelliflow/frontend typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>InputTransformConfig has advanced settings collapsible with machineKey (all fields) and fileSlotId/fileSlotLabel (file fields), with real-time format validation and cross-type collision detection. VariablePicker shows type icons and uses segmentKey for selection. PromptEditor shows readable label chips. Frontend typechecks pass.</done>
</task>

<task type="auto">
  <name>Task 2: InputTransformExecutor file slot cards + ExportConfig contentMapping drag reorder</name>
  <files>packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx, packages/frontend/src/components/workflow/config/ExportConfig.tsx</files>
  <action>
**InputTransformExecutor.tsx — File slot card rendering:**

1. **Load formFieldDefs from workflow config** to determine which file fields have fileSlotId. The executor already has access to the node config (passed as props or loaded from workflow store).

2. **Mixed layout per user decision:**
   - File fields WITH fileSlotId: render as independent card areas, ordered by their position in formFields. Each card has:
     - `fileSlotLabel` as card title (or field.label as fallback)
     - Drag-and-drop upload zone (reuse existing upload logic, but scoped to this slot)
     - List of uploaded files for this slot
     - Variable path hint (`{{nodeId.fileSlotId}}`) shown ONLY in admin/config context, NOT in user execution page (per user decision)
   - File fields WITHOUT fileSlotId: render in a single "其他文件" area at the bottom

3. **Pass slotId in confirm payload:** When calling the confirm endpoint, include `slotId` for each file in the `fileOutputs` array. Files uploaded in a slot card get `slotId: field.fileSlotId`. Files in the "other files" area get no slotId.

4. **Preserve existing behavior:** The single merged upload area should continue to work for workflows that have no file slots configured (backward compatible).

**ExportConfig.tsx — ContentMapping with VariablePicker + drag reorder:**

1. **Reuse VariablePicker** per user decision: Add a VariablePicker instance that lets admins select upstream output variables to include in the export content. On selection, add the VariableRef to the `contentMapping` array.

2. **Drag reorder list:** Display selected contentMapping items as an ordered list. Each item shows the variable's readable name (variableName). Support drag reorder using HTML5 native drag events (`onDragStart`, `onDragOver`, `onDrop`) — no library needed given the small list size (typically 3-5 items per user decision). Each item has a remove button (x icon).

3. **Drag reorder determines export segment order:** The order of items in contentMapping is the order segments appear in the exported document.

4. **Store as VariableRef[]:** Each contentMapping entry is a full VariableRef with nodeId, outputId (segmentKey), and variableName.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter @intelliflow/frontend typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>InputTransformExecutor renders file slot cards with mixed layout (slot cards first, "other files" after), passes slotId in confirm payload. ExportConfig contentMapping uses VariablePicker for selection with drag-reorderable list. Frontend typechecks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of output path grammar, file slots, and export contentMapping UI</name>
  <action>
  Built complete output path grammar, file slot, and export contentMapping UI across config panels and executor. Human verification needed for visual/interactive elements.
  </action>
  <what-built>Complete output path grammar, file slot, and export contentMapping UI across config panels and executor</what-built>
  <how-to-verify>
    1. Start dev server: `cd /Users/dongli/Workspace/intelliflow-docs && bun run dev`
    2. Open workflow editor, add an input_transform node with text and file fields
    3. Verify: Each field has a collapsible "advanced settings" section
    4. Verify: machineKey input validates format in real-time (try invalid chars -> red error)
    5. Verify: For file fields, fileSlotId and fileSlotLabel inputs appear in advanced settings
    6. Verify: Duplicate machineKey/fileSlotId across fields shows collision warning
    7. Add a model_call node with prompt editor
    8. Verify: VariablePicker shows type icons (T for text, file icon for slots, robot for models) grouped by node
    9. Verify: Selecting a variable inserts a readable label chip in PromptEditor
    10. Add an export node
    11. Verify: ContentMapping section has VariablePicker for selecting upstream outputs
    12. Verify: Selected items appear in reorderable list, drag to change order works
    13. Create a test document with file slots configured
    14. Verify: Executor shows independent file slot cards with labels, "other files" area for non-slot files
    15. Verify: Variable path hints NOT shown on user execution page
  </how-to-verify>
  <verify>Human visual verification of all UI elements</verify>
  <done>User confirms all config panels, executor, variable picker, and export config work correctly</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `bun run --filter @intelliflow/frontend typecheck` succeeds
- InputTransformConfig shows advanced settings with machineKey + fileSlot config
- VariablePicker displays type-specific icons and uses segmentKey
- PromptEditor shows readable label chips
- ExportConfig contentMapping supports variable selection and drag reorder
- InputTransformExecutor renders independent file slot cards
- All existing form field types continue to work (backward compatible)
</verification>

<success_criteria>
All frontend components updated for output path grammar, file slots, and export contentMapping. Admin config panels expose machineKey and fileSlot settings in collapsible advanced areas. VariablePicker uses segmentKey with type icons. Export contentMapping is configurable with drag reorder. File slot executor renders independent upload cards. Visual verification by user confirms correct behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/23-output-path-grammar-file-slots-export-contentmapping/23-03-SUMMARY.md`
</output>
