---
phase: 03-workflow-orchestration
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/frontend/src/pages/admin/WorkflowEditor.tsx
  - packages/frontend/src/components/workflow/canvas/WorkflowCanvas.tsx
  - packages/frontend/src/components/workflow/canvas/NodeLibraryPanel.tsx
  - packages/frontend/src/components/workflow/canvas/nodes/InputTransformNode.tsx
  - packages/frontend/src/components/workflow/canvas/nodes/DesensitizeNode.tsx
  - packages/frontend/src/components/workflow/canvas/nodes/ModelCallNode.tsx
  - packages/frontend/src/components/workflow/canvas/nodes/RestoreNode.tsx
  - packages/frontend/src/components/workflow/canvas/nodes/ExportNode.tsx
  - packages/frontend/src/components/workflow/canvas/edges/DataFlowEdge.tsx
  - packages/frontend/package.json
autonomous: true
requirements: [FLOW-02, FLOW-03, FLOW-13]

must_haves:
  truths:
    - "Admin can see a drag-and-drop canvas editor when opening a workflow"
    - "Left-side node library panel shows 5 node types that can be dragged onto the canvas"
    - "Nodes auto-connect to the previous node when added"
    - "Admin can manually disconnect and reconnect nodes by dragging edges"
    - "Each node shows type icon, label, and configuration status badge"
    - "Same node type can be added multiple times with independent instances"
    - "Canvas supports zoom, pan, and minimap"
  artifacts:
    - path: "packages/frontend/src/pages/admin/WorkflowEditor.tsx"
      provides: "Editor page layout (library panel + canvas + config panel slot)"
      min_lines: 100
    - path: "packages/frontend/src/components/workflow/canvas/WorkflowCanvas.tsx"
      provides: "SolidFlow wrapper with custom node types registered"
      min_lines: 80
    - path: "packages/frontend/src/components/workflow/canvas/NodeLibraryPanel.tsx"
      provides: "Left sidebar with 5 draggable node type entries"
      min_lines: 50
  key_links:
    - from: "WorkflowEditor.tsx"
      to: "/api/workflows/:id"
      via: "Eden Treaty GET to load workflow, PUT to save"
      pattern: "api\\.workflows"
    - from: "WorkflowCanvas.tsx"
      to: "@dschz/solid-flow"
      via: "SolidFlow, Background, Controls, MiniMap imports"
      pattern: "solid-flow"
---

<objective>
Install @dschz/solid-flow and build the visual workflow canvas editor with custom node components, node library panel, and edge connections.

Purpose: The canvas editor is the core UI for designing document generation workflows. Users drag nodes from a library, arrange them on the canvas, and connect them to define the flow.
Output: WorkflowEditor page with functional drag-and-drop canvas, 5 custom node components, and node library panel.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-orchestration/03-RESEARCH.md
@.planning/phases/03-workflow-orchestration/03-CONTEXT.md
@.planning/phases/03-workflow-orchestration/03-01-SUMMARY.md

<interfaces>
<!-- From 03-01: Shared types -->
```typescript
type WorkflowNodeType = "input_transform" | "desensitize" | "model_call" | "restore" | "export";
interface WorkflowNodeDef { id, type, label, position: {x,y}, config: NodeConfig, outputs: OutputDef[] }
interface WorkflowEdgeDef { id, source, target, sourceHandle?, targetHandle? }
interface Workflow extends BaseEntity { documentTypeId, name, description, status, isDefault, nodes: WorkflowNodeDef[], edges: WorkflowEdgeDef[], schemaVersion }
```

<!-- API endpoints from 03-01 -->
GET /api/workflows/:id → full Workflow object
PUT /api/workflows/:id { nodes, edges, name?, description? } → updated Workflow

<!-- @dschz/solid-flow API (mirrors React Flow) -->
SolidFlow, Background, Controls, MiniMap components
Handle component with type="source"|"target" and Position enum
Custom node registration via nodeTypes prop
onNodesChange, onEdgesChange, onConnect callbacks
useNodesState, useEdgesState hooks (or SolidJS equivalents)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install solid-flow and create canvas editor foundation</name>
  <files>packages/frontend/package.json, packages/frontend/src/pages/admin/WorkflowEditor.tsx, packages/frontend/src/components/workflow/canvas/WorkflowCanvas.tsx</files>
  <action>
1. Install `@dschz/solid-flow` in the frontend package:
   ```bash
   cd packages/frontend && bun add @dschz/solid-flow
   ```
   Pin the exact version in package.json after install.

2. Create `WorkflowEditor.tsx` — the editor page layout:
   - Fetches workflow by ID from route params via `GET /api/workflows/:id`
   - Three-column layout: NodeLibraryPanel (left, collapsible) | WorkflowCanvas (center, flex-1) | config panel slot (right, empty for now — Plan 03-04)
   - Top toolbar: workflow name (editable), "保存" button (calls PUT /api/workflows/:id with current nodes/edges), "返回" button (navigates back to /admin/workflows)
   - Manages nodes/edges state as SolidJS signals, initialized from fetched workflow data
   - Passes nodes/edges and their setters to WorkflowCanvas

3. Create `WorkflowCanvas.tsx` — the SolidFlow wrapper:
   - Renders `<SolidFlow>` with nodes, edges, onNodesChange, onEdgesChange, onConnect props
   - Registers custom nodeTypes map: { input_transform: InputTransformNode, desensitize: DesensitizeNode, model_call: ModelCallNode, restore: RestoreNode, export: ExportNode }
   - Includes `<Background />`, `<Controls />`, `<MiniMap />` from solid-flow
   - Handles `onDrop` event for receiving nodes dragged from the library panel
   - On drop: creates a new WorkflowNodeDef with unique ID (crypto.randomUUID()), the dropped node type, default label (Chinese: "输入转换"/"信息脱敏"/"模型调用"/"信息恢复"/"文件导出"), position from drop coordinates, empty config matching the type, empty outputs
   - Auto-connect: when a new node is dropped, find the last node in the flow and create an edge from it to the new node

4. Import the solid-flow CSS styles if provided by the library, or add minimal required styles for the canvas (typically a container with width/height 100%).
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter frontend tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>solid-flow installed. WorkflowEditor page renders with three-column layout, loads workflow data, and provides save functionality. WorkflowCanvas renders SolidFlow with background, controls, and minimap. Drop handler creates new nodes with auto-connect.</done>
</task>

<task type="auto">
  <name>Task 2: Custom node components and node library panel</name>
  <files>packages/frontend/src/components/workflow/canvas/NodeLibraryPanel.tsx, packages/frontend/src/components/workflow/canvas/nodes/InputTransformNode.tsx, packages/frontend/src/components/workflow/canvas/nodes/DesensitizeNode.tsx, packages/frontend/src/components/workflow/canvas/nodes/ModelCallNode.tsx, packages/frontend/src/components/workflow/canvas/nodes/RestoreNode.tsx, packages/frontend/src/components/workflow/canvas/nodes/ExportNode.tsx, packages/frontend/src/components/workflow/canvas/edges/DataFlowEdge.tsx</files>
  <action>
1. Create `NodeLibraryPanel.tsx`:
   - Left-side panel (w-60, collapsible via toggle button)
   - Lists 5 node types, each as a draggable card with icon + Chinese label + brief description:
     - 输入转换 (Input transform) — "配置输入表单和文件上传"
     - 信息脱敏 (Desensitize) — "识别和替换敏感信息"
     - 模型调用 (Model call) — "AI 模型生成内容"
     - 信息恢复 (Restore) — "恢复脱敏信息"
     - 文件导出 (Export) — "导出最终文档"
   - Each card uses HTML5 drag (draggable="true", onDragStart sets dataTransfer with node type)
   - Distinct color per node type for visual differentiation (use Tailwind colors: blue for input, orange for desensitize, purple for model_call, green for restore, red for export)
   - Panel title "节点库" with collapse/expand chevron button

2. Create 5 custom node components (one per type), each following the same pattern:
   - Compact card: type-specific color left border (4px), white background, rounded-lg, shadow-sm, min-w-[180px]
   - Content: type icon (simple SVG or emoji as placeholder) + label (editable text) + config status Badge ("已配置" green / "未配置" amber)
   - Config status: determined by checking if key fields are set (e.g., model_call is configured if modelId is not null)
   - `<Handle type="target" position={Position.Left} />` and `<Handle type="source" position={Position.Right} />`
   - Clicking the node should signal to WorkflowEditor which node is selected (for the config panel in Plan 03-04). Pass selectedNodeId up via callback prop.

3. Create `DataFlowEdge.tsx`:
   - Custom edge component showing directional arrow
   - Uses default bezier path from solid-flow but adds an arrow marker
   - Styled with the indigo theme color for consistency

4. Wire everything together in WorkflowCanvas: register nodeTypes and edgeTypes maps.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter frontend tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>5 custom node components render with type-specific colors, icons, labels, and config status badges. Node library panel shows all 5 types as draggable cards. DataFlowEdge renders directional arrows. Node selection callback wired for future config panel.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- solid-flow package installed and importable
- WorkflowEditor loads workflow data and renders canvas
- Nodes can be dragged from library panel onto canvas
- New nodes auto-connect to previous node
- Canvas supports zoom, pan, and minimap
- Each node type has distinct visual appearance
</verification>

<success_criteria>
- Functional drag-and-drop canvas editor with 5 node types
- Node library panel with collapsible sidebar
- Auto-connect and manual edge management
- Save button persists nodes/edges to backend
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-orchestration/03-03-SUMMARY.md`
</output>
