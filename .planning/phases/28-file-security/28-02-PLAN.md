---
phase: 28-file-security
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - packages/backend/src/modules/files/files.routes.ts
autonomous: true
requirements:
  - FSEC-03
  - FSEC-04
  - FSEC-05

must_haves:
  truths:
    - "POST /files body no longer accepts storagePath; the server generates it from documentId and a uuid"
    - "POST /files adds isDocumentProjectMember() check; unauthorized members receive 403"
    - "GET /files adds isDocumentProjectMember() check; non-members receive 403"
    - "listDocumentFiles is scoped to the requesting user's document membership"
  artifacts:
    - path: "packages/backend/src/modules/files/files.routes.ts"
      provides: "server-controlled storage paths and membership-gated file access"
      contains: "canEditDocument|isDocumentProjectMember|storagePath.*join"
  key_links:
    - from: "files.routes.ts::POST /"
      to: "files.service.ts::insertDocumentFile"
      via: "server generates storagePath"
      pattern: "join.*documentId"
    - from: "files.routes.ts"
      to: "versions.service.ts::isDocumentProjectMember"
      via: "membership gate before insert or list"
      pattern: "await isDocumentProjectMember"
---

<objective>
Harden the file upload and listing endpoints against path injection and unauthorized access.

Purpose: Close the storagePath injection vector and add membership checks to both POST and GET /files.
Output: Server-generated storage paths, isDocumentProjectMember guards on both endpoints.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/28-file-security/28-01-SUMMARY.md

@packages/backend/src/modules/files/files.routes.ts
@packages/backend/src/modules/files/files.service.ts
@packages/backend/src/modules/versions/versions.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove client-supplied storagePath from POST /files</name>
  <files>packages/backend/src/modules/files/files.routes.ts</files>
  <read_first>packages/backend/src/modules/files/files.routes.ts, packages/backend/src/modules/files/files.service.ts, packages/backend/src/common/sanitize.ts</read_first>
  <action>In `packages/backend/src/modules/files/files.routes.ts`, remove `storagePath` from the request body schema (the `body: t.Object({...})` block). Then modify the handler to generate the storagePath server-side:
1. Import `join` from "node:path" and `randomUUID` from "crypto".
2. Import `getUploadPath` from "./files.service".
3. Generate `const storagePath = join(getUploadPath(body.documentId), randomUUID() + "_" + sanitizeFilename(body.originalName))`.
4. Pass `storagePath` to `insertDocumentFile({...storagePath, ...})` instead of `body.storagePath`.
5. Keep all other fields (documentId, category, originalName, mimeType, fileSize, createdBy) from the body.

Do not remove the `originalName` field from the body — it is the user-facing display name and is safe (not used for filesystem access).
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "storagePath|getUploadPath|sanitizeFilename|randomUUID" packages/backend/src/modules/files/files.routes.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `storagePath` does not appear in the body schema
    - The handler generates `storagePath` server-side using `join(getUploadPath(documentId), randomUUID() + "_" + sanitizeFilename(originalName))`
    - `insertDocumentFile` receives the server-generated `storagePath`
  </acceptance_criteria>
  <done>Client-supplied storagePath can no longer be injected into the file index.</done>
</task>

<task type="auto">
  <name>Task 2: Add membership check to POST /files</name>
  <files>packages/backend/src/modules/files/files.routes.ts</files>
  <read_first>packages/backend/src/modules/files/files.routes.ts (after Task 1), packages/backend/src/modules/versions/versions.service.ts</read_first>
  <action>In `packages/backend/src/modules/files/files.routes.ts`, add the same two imports used in export.routes.ts: `import { canEditDocument, isDocumentProjectMember } from "../versions/versions.service"`. In the POST / handler, add the isDocumentProjectMember guard immediately after destructuring `{ body, user, set }` and before the `insertDocumentFile` call: `const isMember = await isDocumentProjectMember(body.documentId, user!.id); if (!isMember) { set.status = 403; return { error: "仅项目成员可上传文件" }; }`. Keep all other logic, response shape, and status code (201) unchanged.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "isDocumentProjectMember|仅项目成员可上传文件" packages/backend/src/modules/files/files.routes.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `isDocumentProjectMember` is imported in files.routes.ts
    - POST /files returns 403 with message "仅项目成员可上传文件" when user is not a member
    - The 201 response payload shape is unchanged
  </acceptance_criteria>
  <done>File upload is gated to project members only.</done>
</task>

<task type="auto">
  <name>Task 3: Add membership check to GET /files</name>
  <files>packages/backend/src/modules/files/files.routes.ts</files>
  <read_first>packages/backend/src/modules/files/files.routes.ts (after Task 2)</read_first>
  <action>In the GET /files handler in `packages/backend/src/modules/files/files.routes.ts`, add `isDocumentProjectMember` check immediately after destructuring `{ query, user, set }` and before `listDocumentFiles`: `const isMember = await isDocumentProjectMember(query.documentId, user!.id); if (!isMember) { set.status = 403; return { error: "仅项目成员可查看文件列表" }; }`. This ensures users can only enumerate files from documents they belong to. Keep the response shape unchanged.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "isDocumentProjectMember|仅项目成员可查看文件列表" packages/backend/src/modules/files/files.routes.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -20</verify>
  <acceptance_criteria>
    - GET /files returns 403 with message "仅项目成员可查看文件列表" when user is not a member
    - The file list response shape is unchanged
  </acceptance_criteria>
  <done>File listing is gated to project members only.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/backend/tsconfig.json` passes
- `storagePath` is absent from the body schema
- `storagePath` is generated server-side using `sanitizeFilename(originalName)`
- Both POST and GET /files have `isDocumentProjectMember` guards
- The insertDocumentFile call passes the server-generated storagePath
</verification>

<success_criteria>
- Clients cannot inject a storagePath into POST /files
- The storagePath is a deterministic server-generated path under getUploadPath(documentId)
- Unauthorized users get 403 on both file upload and file listing
</success_criteria>

<output>
After completion, create `.planning/phases/28-file-security/28-02-SUMMARY.md`
</output>
