---
phase: 27-permission-security
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - packages/backend/src/modules/runtime/runtime.routes.ts
autonomous: true
requirements:
  - PERM-02
  - PERM-05

must_haves:
  truths:
    - "runtime.routes.ts uses canEditDocument for init, advance, rollback, save draft, skip, and start-background before any runtime mutation happens"
    - "GET /runtime/:documentId continues to use isDocumentProjectMember so read-only members can still inspect runtime state"
    - "Background-task dedupe and concurrency-limit logic still executes only after the new write-permission check passes"
  artifacts:
    - path: "packages/backend/src/modules/runtime/runtime.routes.ts"
      provides: "Phase 27 write guards for core runtime routes"
      contains: "canEditDocument"
  key_links:
    - from: "runtime.routes.ts"
      to: "versions.service.ts::canEditDocument"
      via: "import and handler guard"
      pattern: "await canEditDocument("
    - from: "runtime.routes.ts::GET /:documentId"
      to: "versions.service.ts::isDocumentProjectMember"
      via: "read-only runtime state gate"
      pattern: "await isDocumentProjectMember("
---

<objective>
Move the core runtime mutation routes to the new creator-or-owner permission helper.

Purpose: Prevent regular project members from mutating runtime state while preserving read-only runtime access.
Output: `runtime.routes.ts` guards updated so all six write handlers use `canEditDocument()` and the runtime state GET route stays membership-based.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/27-permission-security/27-01-SUMMARY.md

@packages/backend/src/modules/runtime/runtime.routes.ts
@packages/backend/src/modules/versions/versions.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace membership guards on all core runtime write handlers</name>
  <files>packages/backend/src/modules/runtime/runtime.routes.ts</files>
  <read_first>packages/backend/src/modules/runtime/runtime.routes.ts, packages/backend/src/modules/versions/versions.service.ts</read_first>
  <action>In `packages/backend/src/modules/runtime/runtime.routes.ts`, import `canEditDocument` from `../versions/versions.service` next to `isDocumentProjectMember`. Then change the permission guard in these exact handlers to call `await canEditDocument(params.documentId, user!.id)` before any service call or background-task query: `POST /:documentId/init`, `POST /:documentId/advance/:nodeExecutionId`, `POST /:documentId/rollback`, `PUT /:documentId/nodes/:nodeExecutionId/draft`, `POST /:documentId/skip/:nodeExecutionId`, and `POST /:documentId/start-background`. Keep the handler bodies otherwise intact. Update the 403 messages to creator-or-owner wording so the guard intent is explicit: `仅文档创建者或项目负责人可初始化运行时`, `仅文档创建者或项目负责人可推进节点`, `仅文档创建者或项目负责人可回退节点`, `仅文档创建者或项目负责人可保存草稿`, `仅文档创建者或项目负责人可跳过节点`, and `仅文档创建者或项目负责人可启动后台生成`.</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "init|advance|rollback|draft|skip|start-background|canEditDocument\\(" packages/backend/src/modules/runtime/runtime.routes.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `packages/backend/src/modules/runtime/runtime.routes.ts` imports `canEditDocument`
    - The six mutating runtime handlers each contain `await canEditDocument(params.documentId, user!.id)`
    - The six 403 messages mention `仅文档创建者或项目负责人`
  </acceptance_criteria>
  <done>All core runtime mutations are blocked for regular members before any runtime write path begins.</done>
</task>

<task type="auto">
  <name>Task 2: Keep runtime reads on membership-only access</name>
  <files>packages/backend/src/modules/runtime/runtime.routes.ts</files>
  <read_first>packages/backend/src/modules/runtime/runtime.routes.ts, packages/backend/src/modules/versions/versions.service.ts</read_first>
  <action>Still in `packages/backend/src/modules/runtime/runtime.routes.ts`, leave `GET /:documentId` on `await isDocumentProjectMember(params.documentId, user!.id)` so read-only members can inspect runtime state. Do not switch `GET /my-tasks`, because it is already user-scoped by `backgroundTasks.userId = user!.id` rather than document-project membership. Keep all existing read-path logic, error bodies, and background-task query code unchanged except for the new write-route imports above.</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "GET current runtime state|/:documentId\"|isDocumentProjectMember\\(" packages/backend/src/modules/runtime/runtime.routes.ts</verify>
  <acceptance_criteria>
    - `GET /:documentId` in `runtime.routes.ts` still uses `await isDocumentProjectMember(params.documentId, user!.id)`
    - `GET /:documentId` does not call `canEditDocument`
    - The active-task dedupe and 429 limit checks in `start-background` remain below the new permission guard
  </acceptance_criteria>
  <done>Runtime reads stay accessible to project members while writes are restricted to creator-or-owner users.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/backend/tsconfig.json` passes
- `runtime.routes.ts` imports and uses `canEditDocument` in all six write handlers
- `GET /runtime/:documentId` still uses `isDocumentProjectMember`
- `start-background` still contains duplicate-task and concurrency checks after the permission gate
</verification>

<success_criteria>
- Non-owner/non-creator members can no longer init, advance, rollback, save draft, skip, or start background generation
- Read-only runtime state remains available to project members
- No unrelated runtime route logic is changed
</success_criteria>

<output>
After completion, create `.planning/phases/27-permission-security/27-02-SUMMARY.md`
</output>
