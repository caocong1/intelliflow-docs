---
phase: 01-foundation-auth-document-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - packages/backend/package.json
  - packages/backend/tsconfig.json
  - packages/backend/drizzle.config.ts
  - packages/backend/src/index.ts
  - packages/backend/src/db/index.ts
  - packages/backend/src/db/schema.ts
  - packages/backend/src/db/seed.ts
  - packages/backend/src/common/errors.ts
  - packages/frontend/package.json
  - packages/frontend/tsconfig.json
  - packages/frontend/vite.config.ts
  - packages/frontend/index.html
  - packages/frontend/src/index.tsx
  - packages/frontend/src/app.css
  - packages/frontend/src/App.tsx
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/types.ts
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - AUTH-04

must_haves:
  truths:
    - "pnpm install succeeds and all three workspace packages resolve"
    - "Backend starts on port 3001 and responds to GET /api/health with 200"
    - "Frontend dev server starts on port 3000 and renders a page"
    - "Vite proxy forwards /api requests to backend"
    - "drizzle-kit push creates users and document_types tables in PostgreSQL"
    - "Seed script creates default admin user in the database"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace config"
    - path: "packages/backend/src/db/schema.ts"
      provides: "Drizzle schema for users and document_types tables"
      contains: "pgTable"
    - path: "packages/backend/src/db/seed.ts"
      provides: "Default admin user seeding"
      contains: "Bun.password.hash"
    - path: "packages/backend/src/index.ts"
      provides: "ElysiaJS app entry with /api/health endpoint"
      contains: "Elysia"
    - path: "packages/frontend/src/App.tsx"
      provides: "SolidJS app entry point"
    - path: "packages/frontend/vite.config.ts"
      provides: "Vite config with SolidJS + Tailwind v4 + API proxy"
  key_links:
    - from: "packages/frontend/vite.config.ts"
      to: "packages/backend/src/index.ts"
      via: "Vite proxy /api -> localhost:3001"
      pattern: "proxy.*api.*3001"
    - from: "packages/backend/src/db/index.ts"
      to: "packages/backend/src/db/schema.ts"
      via: "Drizzle instance imports schema"
      pattern: "import.*schema"
---

<objective>
Scaffold the pnpm workspace monorepo with backend (Bun + ElysiaJS + Drizzle ORM), frontend (SolidJS + Tailwind CSS v4), and shared packages. Set up database schema for users and document_types tables, create seed script for default admin user, and verify the full dev stack runs end-to-end.

Purpose: Establish the foundational project structure that all subsequent plans and phases build upon.
Output: Working monorepo with backend serving /api/health, frontend rendering via Vite, database tables created, and admin user seeded.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth-document-types/01-RESEARCH.md
@.planning/phases/01-foundation-auth-document-types/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo with backend, frontend, and shared packages</name>
  <files>
    pnpm-workspace.yaml, package.json, .env.example, .gitignore,
    packages/backend/package.json, packages/backend/tsconfig.json, packages/backend/drizzle.config.ts,
    packages/frontend/package.json, packages/frontend/tsconfig.json, packages/frontend/vite.config.ts, packages/frontend/index.html,
    packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/types.ts
  </files>
  <action>
    Create pnpm workspace monorepo structure with three packages: backend, frontend, shared.

    **Root:**
    - `pnpm-workspace.yaml`: packages: ['packages/*']
    - `package.json`: name "@intelliflow/root", private true, scripts for dev (concurrently run backend + frontend), type "module"
    - `.gitignore`: node_modules, dist, .env, drizzle/, *.db
    - `.env.example`: DATABASE_URL=postgresql://user:pass@localhost:5432/intelliflow, JWT_SECRET=change-me-in-production, NODE_ENV=development

    **packages/backend:**
    - `package.json`: name "@intelliflow/backend", type "module"
    - Dependencies: elysia, @elysiajs/jwt, drizzle-orm, postgres (npm package, not bun:sql per research recommendation)
    - Dev dependencies: drizzle-kit, @types/bun
    - Scripts: "dev": "bun --watch src/index.ts", "db:push": "drizzle-kit push", "db:seed": "bun src/db/seed.ts"
    - `tsconfig.json`: target ESNext, module ESNext, moduleResolution bundler, strict true, jsx preserve
    - `drizzle.config.ts`: dialect postgresql, schema src/db/schema.ts, dbCredentials from DATABASE_URL env var

    **packages/frontend:**
    - `package.json`: name "@intelliflow/frontend", type "module"
    - Dependencies: solid-js, @solidjs/router
    - Dev dependencies: vite, vite-plugin-solid, tailwindcss, @tailwindcss/vite, @types/node
    - Add "@intelliflow/backend": "workspace:*" for Eden Treaty type imports in Plan 02
    - Scripts: "dev": "vite", "build": "vite build"
    - `tsconfig.json`: target ESNext, module ESNext, moduleResolution bundler, strict true, jsx "preserve", jsxImportSource "solid-js"
    - `vite.config.ts`: plugins [tailwindcss(), solidPlugin()], server port 3000, proxy /api to http://localhost:3001
    - `index.html`: standard Vite entry pointing to /src/index.tsx

    **packages/shared:**
    - `package.json`: name "@intelliflow/shared", type "module", main "src/types.ts"
    - `tsconfig.json`: target ESNext, module ESNext, moduleResolution bundler, strict true
    - `src/types.ts`: export UserRole type ('admin' | 'user'), basic shared type stubs

    Run `pnpm install` to verify workspace resolution.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && pnpm install && echo "PASS: workspace install succeeded"</automated>
  </verify>
  <done>All three workspace packages resolve, pnpm install completes without errors, all config files exist</done>
</task>

<task type="auto">
  <name>Task 2: Database schema, seed script, backend entry point, and frontend shell</name>
  <files>
    packages/backend/src/db/schema.ts, packages/backend/src/db/index.ts, packages/backend/src/db/seed.ts,
    packages/backend/src/index.ts, packages/backend/src/common/errors.ts,
    packages/frontend/src/index.tsx, packages/frontend/src/app.css, packages/frontend/src/App.tsx
  </files>
  <action>
    **Backend database layer:**

    `packages/backend/src/db/schema.ts`:
    - Import from drizzle-orm/pg-core: pgTable, uuid, varchar, boolean, timestamp, pgEnum
    - Define userRoleEnum: pgEnum('user_role', ['admin', 'user'])
    - Define `users` table: id (uuid, defaultRandom, pk), username (varchar 50, notNull, unique), passwordHash (varchar 255, notNull), displayName (varchar 100, notNull), role (userRoleEnum, default 'user', notNull), isActive (boolean, default true, notNull), createdAt (timestamp with tz, defaultNow, notNull), updatedAt (timestamp with tz, defaultNow, notNull)
    - Define `documentTypes` table: id (uuid, defaultRandom, pk), name (varchar 100, notNull), code (varchar 50, notNull, unique), description (varchar 500, nullable), isActive (boolean, default true, notNull), createdAt (timestamp with tz, defaultNow, notNull), updatedAt (timestamp with tz, defaultNow, notNull)

    `packages/backend/src/db/index.ts`:
    - Use drizzle-orm/postgres-js driver with postgres npm package (not bun:sql — per research open question, start with proven stable driver)
    - Import postgres from 'postgres', create connection from DATABASE_URL env var
    - Export drizzle instance with schema

    `packages/backend/src/db/seed.ts`:
    - Hash password 'admin123' using Bun.password.hash with argon2id algorithm
    - Insert default admin user (username: 'admin', displayName: 'System Administrator', role: 'admin', isActive: true)
    - Use onConflictDoNothing() on username to be idempotent
    - Log credentials to console on success
    - Exit process after completion

    **Backend entry point:**

    `packages/backend/src/index.ts`:
    - Create Elysia app with prefix '/api'
    - Add GET /api/health endpoint returning { status: 'ok', timestamp: new Date().toISOString() }
    - Listen on port 3001
    - Log "Backend running on http://localhost:3001" on start

    `packages/backend/src/common/errors.ts`:
    - Export AppError class extending Error with statusCode property
    - Export common error factory functions: notFound(message), unauthorized(message), forbidden(message), badRequest(message)

    **Frontend shell:**

    `packages/frontend/src/app.css`:
    - Add @import "tailwindcss" at the top (Tailwind v4 CSS-first config)

    `packages/frontend/src/index.tsx`:
    - Import app.css
    - Import { render } from 'solid-js/web'
    - Import App component
    - render(() => <App />, document.getElementById('root')!)

    `packages/frontend/src/App.tsx`:
    - Simple SolidJS component rendering a centered div with "IntelliFlow" heading and "智能文档流程平台" subtitle
    - Use Tailwind classes for styling: min-h-screen, flex items-center justify-center, bg-gray-50
    - This is a placeholder that Plan 02 will replace with router and layout

    **Verify the full stack:**
    - Run `drizzle-kit push` to create tables (requires running PostgreSQL)
    - Run seed script to create admin user
    - Start backend and verify /api/health responds
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && npx tsc --noEmit -p packages/backend/tsconfig.json 2>&1 | head -20 && npx tsc --noEmit -p packages/frontend/tsconfig.json 2>&1 | head -20 && echo "PASS: TypeScript compilation OK"</automated>
  </verify>
  <done>
    - Schema file defines users and documentTypes tables with all required columns
    - DB connection module exports drizzle instance
    - Seed script can create default admin user idempotently
    - Backend starts and GET /api/health returns 200 with { status: 'ok' }
    - Frontend renders placeholder page with Tailwind styling
    - TypeScript compiles without errors for both packages
  </done>
</task>

</tasks>

<verification>
1. `pnpm install` completes without errors
2. TypeScript compiles for all packages
3. Backend starts on port 3001, GET /api/health returns 200
4. Frontend dev server starts on port 3000, renders page
5. Vite proxy forwards /api/health from port 3000 to backend
6. drizzle-kit push creates tables in PostgreSQL (requires DB running)
7. Seed script creates admin user (requires DB running)
</verification>

<success_criteria>
- Monorepo structure with backend, frontend, shared packages all resolving
- Backend serves /api/health endpoint
- Frontend renders with SolidJS + Tailwind CSS v4
- Database schema ready for users and document_types
- Seed script ready for first admin user creation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth-document-types/01-01-SUMMARY.md`
</output>
