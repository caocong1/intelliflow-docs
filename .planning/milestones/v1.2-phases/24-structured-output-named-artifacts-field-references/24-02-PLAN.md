---
phase: 24-structured-output-named-artifacts-field-references
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - packages/frontend/src/components/workflow/config/ModelCallConfig.tsx
  - packages/frontend/src/components/workflow/config/JsonSchemaEditor.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin can select outputFormat (text/json/markdown) in model call config panel"
    - "JSON Schema editor appears when outputFormat is json, hidden otherwise"
    - "Admin can define namedOutputs with id, name, and format per artifact"
    - "stepDescription field is editable in config panel"
    - "JSON Schema editor has syntax highlighting and basic JSON validation"
  artifacts:
    - path: "packages/frontend/src/components/workflow/config/ModelCallConfig.tsx"
      provides: "Extended config panel with outputFormat, jsonSchema, stepDescription, namedOutputs"
    - path: "packages/frontend/src/components/workflow/config/JsonSchemaEditor.tsx"
      provides: "CodeMirror 6 based JSON schema editor component"
  key_links:
    - from: "ModelCallConfig.tsx"
      to: "types.ts ModelCallConfig"
      via: "config.outputFormat, config.jsonSchema, config.namedOutputs"
      pattern: "outputFormat|jsonSchema|namedOutputs"
    - from: "JsonSchemaEditor.tsx"
      to: "ModelCallConfig.tsx"
      via: "import and conditional render when outputFormat=json"
      pattern: "JsonSchemaEditor"
---

<objective>
Build the frontend configuration UI for structured output and named artifacts in the model call config panel, including a CodeMirror 6 JSON Schema editor.

Purpose: Allow workflow designers to configure outputFormat, jsonSchema, stepDescription, and namedOutputs on model call nodes.
Output: Extended ModelCallConfig panel with conditional schema editor and namedOutputs list builder.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-structured-output-named-artifacts-field-references/24-CONTEXT.md
@.planning/phases/24-structured-output-named-artifacts-field-references/24-RESEARCH.md
@.planning/phases/24-structured-output-named-artifacts-field-references/24-01-SUMMARY.md

@packages/frontend/src/components/workflow/config/ModelCallConfig.tsx
@packages/shared/src/types.ts

<interfaces>
<!-- Types from Plan 01 that this plan uses -->

From packages/shared/src/types.ts (after Plan 01):
```typescript
export interface NamedOutputDef {
  id: string;       // segmentKey, e.g. "blueprint"
  name: string;     // display name, e.g. "投标蓝图"
  format: "text" | "json" | "markdown";
  jsonSchema?: object;
}

export interface ModelCallConfig {
  type: "model_call";
  displayName: string;
  modelIds: string[];
  modelNames?: Record<string, string>;
  promptTemplate: string;
  inputRefs: VariableRef[];
  // Phase 24 additions:
  outputFormat?: "text" | "json" | "markdown";
  jsonSchema?: object;
  stepDescription?: string;
  namedOutputs?: NamedOutputDef[];
  autoAdvance?: boolean;
  allowEdit?: boolean;
  skippable?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JsonSchemaEditor component with CodeMirror 6</name>
  <files>packages/frontend/src/components/workflow/config/JsonSchemaEditor.tsx</files>
  <action>
**Install CodeMirror 6 packages (frontend dependency):**
```bash
cd packages/frontend && bun add codemirror @codemirror/lang-json @codemirror/lint @codemirror/view @codemirror/state
```

Create a new SolidJS wrapper component for CodeMirror 6 JSON editing:

**Props:**
```typescript
interface JsonSchemaEditorProps {
  value: string;           // JSON string
  onChange: (value: string) => void;
  placeholder?: string;
}
```

**Implementation:**
1. Use `onMount` to create `EditorView` in a container div ref
2. Configure with:
   - `@codemirror/lang-json` for JSON language support (syntax highlighting, bracket matching)
   - `@codemirror/lint` with `linter` extension for JSON syntax error underlines
   - Basic theme with reasonable defaults (light background, monospace font)
   - `EditorView.updateListener` to emit changes to `onChange` prop
3. Use `createEffect` to push external value changes via `EditorView.dispatch` when `props.value` changes from outside (avoid infinite loops by comparing with current editor content)
4. Use `onCleanup` to call `view.destroy()`
5. Style the container: border, rounded corners, min-height ~150px, max-height ~400px with overflow scroll
6. Add a JSON validation linter that shows red underlines on parse errors

**Important SolidJS integration notes (from RESEARCH.md Pitfall 4):**
- Never let SolidJS re-render the container div (use a stable ref)
- EditorView is imperative — only interact via dispatch
- Use `untrack` when reading editor content to avoid reactive loops
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter @intelliflow/frontend typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - JsonSchemaEditor.tsx exists with CodeMirror 6 integration
    - JSON syntax highlighting and bracket matching enabled
    - JSON parse errors shown as lint underlines
    - Component properly creates/destroys EditorView in SolidJS lifecycle
    - External value changes pushed via dispatch without infinite loops
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ModelCallConfig panel with outputFormat, jsonSchema, stepDescription, namedOutputs</name>
  <files>packages/frontend/src/components/workflow/config/ModelCallConfig.tsx</files>
  <action>
Extend the existing ModelCallConfigPanel component to add 4 new config sections below the existing prompt template area:

**1. stepDescription field:**
- Add a text input labeled "步骤描述" with placeholder "可选：描述此步骤的用途"
- Binds to `config.stepDescription`
- Position: below displayName, above prompt template

**2. outputFormat selector:**
- Add a `<select>` dropdown labeled "输出格式" with options:
  - "文本" (value: "text", default)
  - "JSON" (value: "json")
  - "Markdown" (value: "markdown")
- Binds to `config.outputFormat`
- Position: below prompt template section

**3. jsonSchema editor (conditional):**
- Show `<JsonSchemaEditor>` component only when `outputFormat === "json"`
- Label: "JSON Schema (可选)"
- Helper text below: "不填则仅做 JSON 语法校验，填写后额外做结构校验"
- Import from `./JsonSchemaEditor`
- Store as stringified JSON in config; parse on save, stringify on load
- Show inline error if JSON parse fails on blur

**4. namedOutputs list builder:**
- Section labeled "命名产物" with an "添加产物" button
- Each named output rendered as a row with:
  - `id` input (placeholder "标识符, 如 blueprint") — validated with `/^[a-zA-Z_][a-zA-Z0-9_]*$/`
  - `name` input (placeholder "显示名称, 如 投标蓝图")
  - `format` dropdown (text/json/markdown)
  - Delete button (trash icon)
- Per-artifact `jsonSchema` editor: when a named output's format is "json", show a small "配置 Schema" expandable section with another JsonSchemaEditor instance
- Empty state: show hint text "未配置命名产物时，模型输出作为单个整体"

**Config update pattern:** Follow existing pattern in ModelCallConfig.tsx — call the `onChange` prop with updated config object whenever any field changes. Use the same signal/store pattern already established.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter @intelliflow/frontend typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - outputFormat dropdown renders with text/json/markdown options
    - JsonSchemaEditor conditionally appears when json selected
    - stepDescription text input present and bound
    - namedOutputs list builder with add/remove/edit per artifact
    - Per-artifact jsonSchema editor shows when artifact format is json
    - All config changes propagate via onChange
  </done>
</task>

</tasks>

<verification>
1. `bun run --filter @intelliflow/frontend typecheck` succeeds
2. JsonSchemaEditor.tsx exists with CodeMirror imports
3. ModelCallConfig.tsx contains outputFormat select, JsonSchemaEditor conditional, namedOutputs list builder
4. CodeMirror packages present in frontend package.json
</verification>

<success_criteria>
- JsonSchemaEditor renders a CodeMirror 6 editor with JSON syntax highlighting and lint
- ModelCallConfig panel shows outputFormat dropdown, conditional schema editor, namedOutputs list
- All new config fields properly serialized and deserialized
- Frontend typechecks cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/24-structured-output-named-artifacts-field-references/24-02-SUMMARY.md`
</output>
