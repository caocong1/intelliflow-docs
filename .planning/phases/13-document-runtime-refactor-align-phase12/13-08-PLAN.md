---
phase: 13-document-runtime-refactor-align-phase12
plan: 08
type: execute
wave: 4
depends_on: ["13-03", "13-04", "13-05", "13-06", "13-07"]
files_modified: []
autonomous: false
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - NODE-01
  - NODE-02
  - NODE-03
  - NODE-04
  - NODE-05
  - NODE-06
  - NODE-07
  - NODE-08
  - NODE-09
  - NODE-10
  - NODE-11
  - NODE-12
  - NODE-13
  - NODE-14
  - NODE-15
  - NODE-16
  - NODE-17
  - NODE-18
  - NODE-19
  - NODE-20
  - NODE-21
  - NODE-22
  - NOPS-01
  - NOPS-02
  - NOPS-03
  - NOPS-04
  - RECV-01
  - RECV-02

must_haves:
  truths:
    - "User can create a document, select workflow with preview, and enter workspace"
    - "All 5 node executors render with real config and Chinese UI"
    - "Full workflow executes end-to-end: input -> desensitize -> model call -> restore -> export"
    - "Browser refresh resumes to correct state without re-triggering operations"
    - "Export produces downloadable file from model output"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete document runtime — create document, execute all 5 node types, verify state persistence, and confirm export works.

Purpose: Validate that all Phase 13 changes work together as a cohesive system.
Output: Verification report confirming end-to-end functionality.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-01-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-02-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-03-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-04-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-05-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-06-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification — typecheck, build, API smoke test</name>
  <files></files>
  <action>
1. Run `bun run typecheck` from workspace root — must pass with zero errors.
2. Run `bun run build` from packages/frontend — Vite build must succeed.
3. Start the dev server and run API smoke tests:
   - `GET /api/health` returns 200
   - `POST /api/auth/login` with test credentials returns token
   - `GET /api/runtime/:testDocId/init` returns DocumentRuntimeState with `workflowNodes` field
   - Verify `workflowNodes` is a non-empty array with `config` objects
4. Verify DB schema:
   - `model_call_logs` table exists
   - `node_executions` table has `execution_round` and `is_current` columns
5. Check no English text remains in key files:
   - grep for common English UI strings ("Loading", "Submit", "Cancel", "Error", "Save") in all executor and workspace files
   - Any found English strings are bugs to be fixed
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Typecheck passes. Build succeeds. API returns correct response structure. DB schema is correct. No English UI strings remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 13 runtime refactor:
- Backend: init API returns workflowNodes, nodeId variable resolution, models Record export fix, model_call_logs, execution versioning
- Frontend: DocumentWorkspace wires real configs, all 5 executors redesigned with Chinese UI and Stitch design
- Features: workflow preview in create modal, document progress in list, network banner, auto-save, model call log admin page
  </what-built>
  <how-to-verify>
1. Open browser to http://localhost:3000
2. Log in as admin, navigate to a project
3. Click "新建文档", select document type, select workflow — verify workflow preview appears
4. Create the document — verify workspace opens with Chinese UI
5. Walk through each node:
   a. Input transform: fill form fields, upload a file, confirm — all Chinese
   b. Desensitize: should auto-detect on mount — review items, confirm — all Chinese
   c. Model call: select model, start generation, observe SSE streaming — all Chinese
   d. Restore: verify before/after comparison — all Chinese
   e. Export: select Word format, preview, download — PPT option should NOT appear
6. Refresh browser mid-workflow — should resume at correct node
7. Check admin sidebar for "模型调用日志" — view logged model calls
8. Verify document list shows progress for in_progress documents
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Typecheck and build pass
- End-to-end workflow execution works from create to export
- All UI text is in Chinese
- State persistence works across browser refresh
- Model call logs are visible in admin panel
</verification>

<success_criteria>
Complete Phase 13 is verified working end-to-end. User can create documents with the Phase 12 editor output and execute workflows through all 5 node types with Chinese UI, state persistence, and export.
</success_criteria>

<output>
After completion, create `.planning/phases/13-document-runtime-refactor-align-phase12/13-08-SUMMARY.md`
</output>
