---
phase: 13-document-runtime-refactor-align-phase12
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
  - packages/frontend/src/components/workspace/StepperBar.tsx
  - packages/frontend/src/components/workspace/NodeHistoryPanel.tsx
  - packages/frontend/src/components/workspace/InlineEditor.tsx
  - packages/frontend/src/components/workspace/ActionBar.tsx
autonomous: true
requirements:
  - DOC-03
  - DOC-04
  - DOC-05
  - NOPS-01
  - NOPS-02
  - NOPS-03
  - NOPS-04

must_haves:
  truths:
    - "DocumentWorkspace passes real config from workflowNodes to each executor component"
    - "StepperBar displays Chinese labels and correct progress states"
    - "NodeHistoryPanel shows Chinese text and supports executionRound dropdown"
    - "ActionBar buttons are in Chinese (confirm/next, skip, rollback)"
    - "All workspace shell components display Chinese text"
  artifacts:
    - path: "packages/frontend/src/pages/workspace/DocumentWorkspace.tsx"
      provides: "Config wiring from workflowNodes, Chinese UI, read-only mode for completed docs"
      contains: "getNodeConfig"
    - path: "packages/frontend/src/components/workspace/StepperBar.tsx"
      provides: "Chinese progress labels"
    - path: "packages/frontend/src/components/workspace/NodeHistoryPanel.tsx"
      provides: "Chinese text, execution round selector"
  key_links:
    - from: "packages/frontend/src/pages/workspace/DocumentWorkspace.tsx"
      to: "packages/shared/src/types.ts"
      via: "DocumentRuntimeState.workflowNodes used in getNodeConfig helper"
      pattern: "workflowNodes"
---

<objective>
Wire real workflow configs to executor components in DocumentWorkspace and localize all workspace shell components (StepperBar, NodeHistoryPanel, ActionBar, InlineEditor) to Chinese.

Purpose: Frontend must pass real configs instead of empty objects, and all UI text must be Chinese per user decision.
Output: DocumentWorkspace with config wiring, all shell components in Chinese.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/shared/src/types.ts
@packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
@packages/frontend/src/components/workspace/StepperBar.tsx
@packages/frontend/src/components/workspace/NodeHistoryPanel.tsx
@packages/frontend/src/components/workspace/InlineEditor.tsx

<interfaces>
<!-- DocumentRuntimeState after 13-01 expansion -->
From packages/shared/src/types.ts (after Plan 01):
```typescript
export interface DocumentRuntimeState {
  documentId: string;
  workflowName: string;
  currentNodeIndex: number;
  nodes: NodeExecution[];
  workflowNodes: WorkflowNodeDef[];  // NEW
}

export interface NodeExecution {
  // ... existing fields ...
  executionRound: number;  // NEW
  isCurrent: boolean;      // NEW
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire real configs in DocumentWorkspace + add read-only and re-execution support</name>
  <files>packages/frontend/src/pages/workspace/DocumentWorkspace.tsx</files>
  <action>
1. Add a `getNodeConfig` helper function that looks up config from `state().workflowNodes`:
```typescript
const getNodeConfig = (nodeExec: NodeExecution): NodeConfig | undefined => {
  const s = state();
  if (!s?.workflowNodes) return undefined;
  const wfNode = s.workflowNodes.find(n => n.id === nodeExec.nodeId);
  return wfNode?.config;
};
```

2. Replace ALL `({} as InputTransformConfig)`, `({} as DesensitizeConfig)`, `({} as ModelCallConfig)`, `({} as RestoreConfig)`, `({} as ExportConfig)` with calls to `getNodeConfig(currentNode()!)` cast to the appropriate type. Add a loading/error guard: if config is undefined, show a Chinese message "加载节点配置中..." instead of rendering the executor.

3. Add `readOnly` signal derived from document status: if all nodes are completed, render in read-only mode. Pass `readOnly` prop to all executors.

4. For completed documents, add a "从此节点重新执行" (re-execute from here) button on each node in the stepper. When clicked, show a confirmation dialog ("确认重新执行？后续节点状态将被重置"), then call rollback API.

5. Localize ALL English text in DocumentWorkspace to Chinese:
   - Page title area: "文档工作台"
   - Loading states: "加载中..."
   - Error states: "加载失败，请重试"
   - Empty states: "未找到文档"
   - Any button labels, headings, helper text
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "getNodeConfig" packages/frontend/src/pages/workspace/DocumentWorkspace.tsx && ! grep -q "as InputTransformConfig)" packages/frontend/src/pages/workspace/DocumentWorkspace.tsx && echo "no empty config casts"</automated>
  </verify>
  <done>DocumentWorkspace passes real config from workflowNodes to each executor; no more `{} as XConfig` casts; read-only mode for completed docs; re-execute button with confirmation; all text in Chinese.</done>
</task>

<task type="auto">
  <name>Task 2: Localize workspace shell components to Chinese + add execution round selector</name>
  <files>packages/frontend/src/components/workspace/StepperBar.tsx, packages/frontend/src/components/workspace/NodeHistoryPanel.tsx, packages/frontend/src/components/workspace/InlineEditor.tsx, packages/frontend/src/components/workspace/ActionBar.tsx</files>
  <action>
**StepperBar.tsx:**
1. Translate all status labels to Chinese: "待执行" (pending), "执行中" (in_progress), "已完成" (completed), "已跳过" (skipped), "失败" (failed).
2. Translate node type labels: "输入转换", "信息脱敏", "模型调用", "信息恢复", "文件导出".
3. Ensure the stepper visually indicates current node and completed/pending states with appropriate colors.

**NodeHistoryPanel.tsx:**
1. Translate heading to "节点历史记录".
2. Translate all labels: "输入数据", "输出数据", "执行时间", "状态" etc.
3. Add execution round selector: if a node has multiple execution rounds, show a dropdown "执行轮次: 第N次" that switches the displayed history entry. The dropdown only appears when `executionRound > 1` for any execution of that nodeId.

**InlineEditor.tsx:**
1. Translate button labels: "编辑" (edit), "保存" (save), "取消" (cancel).
2. Translate placeholder text and any tooltips to Chinese.

**ActionBar.tsx (or the action button area in DocumentWorkspace):**
1. Translate all action buttons: "确认并继续" (confirm/next), "跳过此节点" (skip), "回退" (rollback).
2. Add the 1.5s debounced auto-save indicator: show "已自动保存" with a subtle checkmark when draft is saved, "保存中..." while saving.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "待执行\|执行中\|已完成" packages/frontend/src/components/workspace/StepperBar.tsx && grep -c "节点历史" packages/frontend/src/components/workspace/NodeHistoryPanel.tsx</automated>
  </verify>
  <done>All workspace shell components display Chinese text. StepperBar shows Chinese status labels. NodeHistoryPanel has execution round dropdown. ActionBar has Chinese button labels and auto-save indicator.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes from workspace root
- DocumentWorkspace renders without white screen — executor receives real config
- All visible text in workspace UI is in Chinese
- StepperBar shows correct Chinese status labels for each node state
- NodeHistoryPanel heading and labels are in Chinese
</verification>

<success_criteria>
Workspace shell is fully Chinese-localized. Real configs flow from workflowNodes to executor components. No more empty config object casts. Read-only mode works for completed documents.
</success_criteria>

<output>
After completion, create `.planning/phases/13-document-runtime-refactor-align-phase12/13-02-SUMMARY.md`
</output>
