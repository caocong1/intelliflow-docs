---
phase: 13-document-runtime-refactor-align-phase12
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx
  - packages/frontend/src/components/workspace/nodes/DesensitizeExecutor.tsx
autonomous: true
requirements:
  - NODE-01
  - NODE-02
  - NODE-03
  - NODE-04
  - NODE-05
  - NODE-06
  - NODE-07
  - NODE-08

must_haves:
  truths:
    - "InputTransformExecutor displays all UI text in Chinese and works with real config"
    - "DesensitizeExecutor auto-triggers detection on mount (no manual button needed to start)"
    - "DesensitizeExecutor displays all UI text in Chinese"
    - "Both executors use Stitch-designed layouts implemented in SolidJS + Tailwind"
  artifacts:
    - path: "packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx"
      provides: "Chinese-localized input transform executor with Stitch-designed UI"
    - path: "packages/frontend/src/components/workspace/nodes/DesensitizeExecutor.tsx"
      provides: "Chinese-localized desensitize executor with auto-detect on mount"
  key_links:
    - from: "packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx"
      to: "packages/shared/src/types.ts"
      via: "InputTransformConfig prop with real formFields and uploadOptions"
      pattern: "InputTransformConfig"
    - from: "packages/frontend/src/components/workspace/nodes/DesensitizeExecutor.tsx"
      to: "packages/shared/src/types.ts"
      via: "DesensitizeConfig prop with categories array"
      pattern: "DesensitizeConfig"
---

<objective>
Redesign InputTransformExecutor and DesensitizeExecutor with Chinese localization, Stitch-designed UI, and auto-detect behavior for desensitize node.

Purpose: These two executors handle the first stages of document workflow — they must work with real configs and display in Chinese.
Output: Two redesigned executor components with professional Chinese UI.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/shared/src/types.ts
@packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx
@packages/frontend/src/components/workspace/nodes/DesensitizeExecutor.tsx
@.planning/phases/13-document-runtime-refactor-align-phase12/13-01-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-02-SUMMARY.md

<interfaces>
From packages/shared/src/types.ts:
```typescript
export interface InputTransformConfig {
  formFields: FormFieldDef[];
  uploadOptions?: {
    allowedTypes: string[];
    maxFiles: number;
    maxSizeMB: number;
  };
  outputFiles?: string[];
}

export interface FormFieldDef {
  id: string;
  type: "text" | "textarea" | "select" | "file";
  label: string;
  required: boolean;
  placeholder?: string;
  options?: string[];
}

export interface DesensitizeConfig {
  categories: string[];
  localModelId?: string;
  autoDetect?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign InputTransformExecutor — Stitch design + Chinese UI</name>
  <files>packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx</files>
  <action>
1. **Generate Stitch design mockup** for the input transform executor using Stitch MCP (`GEMINI_3_1_PRO` model). Prompt should specify:
   - "Document input form for a Chinese enterprise document generation platform"
   - "SolidJS + Tailwind CSS, clean professional style"
   - "Form fields (text, textarea, select, file upload), file parsing results area, confirm button"
   - "All text in Chinese: 输入转换, 填写表单, 上传文件, 解析结果, 确认提交"

2. **Implement the redesigned UI** based on the Stitch mockup, using SolidJS primitives (createSignal, Show, For, Switch/Match). Key changes:
   - All button labels: "上传文件", "确认并继续", "重新解析", "编辑", "保存"
   - All headings: "输入转换", "表单填写", "文件上传", "解析结果"
   - All status messages: "上传中...", "解析中...", "解析完成", "上传失败，请重试"
   - All placeholder text: "请输入...", "请选择..."
   - Error messages: "此字段为必填项", "文件大小超出限制", "不支持的文件类型"

3. **Ensure config is used correctly**: render form fields from `props.config.formFields` using `field.id` as key (NOT field.name — field.name was removed in Phase 12). Upload options from `props.config.uploadOptions`.

4. Add null guard: if `props.config?.formFields` is undefined or empty, show "未配置表单字段" message.

5. Keep existing functional logic intact (XHR upload with progress, file parsing, editable results, confirm to advance). Only change UI presentation and text.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "输入转换\|上传文件\|确认" packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx && grep -c "field.id" packages/frontend/src/components/workspace/nodes/InputTransformExecutor.tsx</automated>
  </verify>
  <done>InputTransformExecutor displays Chinese UI with Stitch-designed layout. Uses field.id as key. All text localized. Functional logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign DesensitizeExecutor — auto-detect + Stitch design + Chinese UI</name>
  <files>packages/frontend/src/components/workspace/nodes/DesensitizeExecutor.tsx</files>
  <action>
1. **Generate Stitch design mockup** for the desensitize executor using Stitch MCP (`GEMINI_3_1_PRO`). Prompt should specify:
   - "Sensitive information detection and review interface for Chinese enterprise platform"
   - "SolidJS + Tailwind CSS, two-phase flow: auto-detection then review checklist"
   - "Detected items as inline highlights, checklist for confirm/reject each item, re-detect button"
   - "All text in Chinese: 信息脱敏, 检测中..., 敏感信息审核, 确认脱敏, 重新检测"

2. **Implement auto-detect on mount**: Add an `onMount` hook that auto-triggers detection when the node enters "detect" phase and input text exists:
```typescript
import { onMount } from "solid-js";
onMount(() => {
  if (phase() === "detect" && inputText().trim() && !props.readOnly) {
    handleDetect();
  }
});
```
Remove the manual "Start Detection" / "开始检测" button from the initial view. Keep a "重新检测" (re-detect) button visible during the review phase for manual re-trigger.

3. **Localize all text to Chinese:**
   - Phase labels: "正在检测敏感信息...", "检测完成，请审核", "审核完成"
   - Checklist labels: "确认脱敏", "保留原文", "手动标记"
   - Category labels from config.categories: display as-is (they're already Chinese names like "手机号", "邮箱", "身份证号", "银行卡号")
   - Status: "已检测到 N 条敏感信息", "未检测到敏感信息"
   - Buttons: "确认脱敏", "重新检测", "手动添加"
   - Error: "检测失败，请重试"

4. **Use config.categories**: The desensitize config now uses `categories: string[]` (not `ruleTypes` + `placeholderFormat`). Ensure the component reads from `props.config.categories` to display which categories were configured. Placeholder format `[TYPE_N]` is a system constant, don't expose in UI.

5. Add null guard: if `props.config?.categories` is undefined, show "未配置脱敏类别" message.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "onMount" packages/frontend/src/components/workspace/nodes/DesensitizeExecutor.tsx && grep -c "信息脱敏\|检测中\|重新检测" packages/frontend/src/components/workspace/nodes/DesensitizeExecutor.tsx && grep -c "categories" packages/frontend/src/components/workspace/nodes/DesensitizeExecutor.tsx</automated>
  </verify>
  <done>DesensitizeExecutor auto-triggers detection on mount. Chinese UI with Stitch design. Uses categories config. Re-detect button available. All text localized.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- InputTransformExecutor renders form fields from real config, all text Chinese
- DesensitizeExecutor auto-starts detection without manual button click
- Both components handle missing/empty config gracefully with Chinese messages
</verification>

<success_criteria>
Input transform and desensitize executors are redesigned with professional Chinese UI, work with real configs from Phase 12 editor, and desensitize auto-triggers detection on mount.
</success_criteria>

<output>
After completion, create `.planning/phases/13-document-runtime-refactor-align-phase12/13-03-SUMMARY.md`
</output>
