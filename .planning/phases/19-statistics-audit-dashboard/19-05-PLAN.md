---
phase: 19-statistics-audit-dashboard
plan: 05
type: execute
wave: 3
depends_on: [19-03, 19-04]
files_modified:
  - packages/frontend/src/pages/admin/stats/AuditDetails.tsx
  - packages/frontend/src/pages/admin/StatsDashboard.tsx
  - packages/frontend/src/components/nav/Sidebar.tsx
  - packages/frontend/src/App.tsx
requirements: [STAT-04, STAT-05]
autonomous: true

must_haves:
  truths:
    - "Audit tab has two sub-tabs: 按用户 and 按文档"
    - "按用户 sub-tab shows user list; expanding a user row shows their document generation records"
    - "按文档 sub-tab shows document generation list with user info; expanding shows node/model call details"
    - "Each audit record shows: who, workflow, nodes/models, duration, tokens, cost"
    - "Sidebar has 统计面板 menu entry visible to admins"
    - "/admin/statistics route renders the StatsDashboard page"
  artifacts:
    - path: "packages/frontend/src/pages/admin/stats/AuditDetails.tsx"
      provides: "Audit tab with by-user and by-document sub-tabs and expandable rows"
  key_links:
    - from: "App.tsx"
      to: "StatsDashboard.tsx"
      via: "Route path=/admin/statistics"
    - from: "Sidebar.tsx"
      to: "/admin/statistics"
      via: "A href link"
    - from: "AuditDetails.tsx"
      to: "statistics API audit endpoints"
      via: "fetchAuditByUser, fetchAuditByDocument"
---

<objective>
Build the audit detail tab with expandable rows, wire the dashboard route and sidebar navigation.

Purpose: Complete the statistics dashboard with full audit trail visibility and make it accessible from the sidebar.
Output: Fully functional statistics dashboard accessible via sidebar navigation.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-statistics-audit-dashboard/19-RESEARCH.md
@.planning/phases/19-statistics-audit-dashboard/19-01-SUMMARY.md
@.planning/phases/19-statistics-audit-dashboard/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend audit endpoints + frontend audit tab</name>
  <files>packages/frontend/src/pages/admin/stats/AuditDetails.tsx</files>
  <action>
**Note:** The audit backend endpoints (getAuditByUser, getAuditByDocument, audit/document-detail) were moved to Plan 01 Task 2 and are already available.

**Frontend — create AuditDetails.tsx:**
- Props: `filters` accessor
- Two sub-tabs using button group: "按用户" | "按文档"
- **按用户 view:**
  - Table: 用户, 调用次数, 文档数, Token 消耗, 估算成本
  - Expandable rows: clicking expands to show that user's recent document generation records (fetch on expand)
  - Pagination at bottom
- **按文档 view:**
  - Table: 时间, 文档名称, 用户, 流程, 调用次数, Token 数, 耗时, 成本
  - Expandable rows: clicking expands to show per-node/model call details for that document (fetch document-detail endpoint on expand)
  - Expanded detail shows: 节点名称, 模型, Token(输入/输出/总计), 耗时(ms), 成本, 状态
  - Pagination at bottom
- Follow existing expandable row pattern from ModelCallLogs.tsx
- Do NOT show full prompts/model output in expanded rows (keep it concise — just metadata)
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && test -f packages/frontend/src/pages/admin/stats/AuditDetails.tsx && grep -q "fetchAuditByUser\|fetchAuditByDocument" packages/frontend/src/pages/admin/stats/AuditDetails.tsx</automated>
  </verify>
  <done>Audit tab renders with two sub-tabs, expandable rows, and pagination; backed by 3 audit API endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Route registration + sidebar nav + final wiring</name>
  <files>packages/frontend/src/App.tsx, packages/frontend/src/components/nav/Sidebar.tsx, packages/frontend/src/pages/admin/StatsDashboard.tsx</files>
  <action>
1. **App.tsx:** Add route for statistics dashboard:
   ```typescript
   import StatsDashboard from "./pages/admin/StatsDashboard";
   // Inside admin routes:
   <Route path="/admin/statistics" component={() => <AdminRoute><StatsDashboard /></AdminRoute>} />
   ```

2. **Sidebar.tsx:** Add "统计面板" link in the admin section (inside the `<Show when={auth.isAdmin()}>` block), after the existing "模型调用日志" link:
   - Use a chart/bar-chart SVG icon (viewBox 0 0 24 24, strokeLinecap round, strokeLinejoin round)
   - Path suggestion: `M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z`
   - Label: "统计面板"
   - href: "/admin/statistics"

3. **StatsDashboard.tsx:** Wire in the AuditDetails component:
   - Import AuditDetails
   - Replace audit tab placeholder with `<AuditDetails filters={filters} />`
   - All 5 tabs now have real content
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -q "statistics" packages/frontend/src/App.tsx && grep -q "统计面板" packages/frontend/src/components/nav/Sidebar.tsx && grep -q "AuditDetails" packages/frontend/src/pages/admin/StatsDashboard.tsx</automated>
  </verify>
  <done>Statistics dashboard is accessible via sidebar "统计面板" link; route registered; all 5 tabs functional</done>
</task>

</tasks>

<verification>
- `bun run check` passes for both frontend and backend
- Navigating to /admin/statistics shows full dashboard
- Sidebar shows "统计面板" for admin users
- Audit tab shows expandable user and document views
- All filters work across all tabs
</verification>

<success_criteria>
Complete statistics dashboard is accessible from sidebar, with overview KPIs, 4 chart modules, 3 dimension drill-down tabs, and audit detail tab with expandable rows — all driven by backend aggregation APIs with shared filtering.
</success_criteria>

<output>
After completion, create `.planning/phases/19-statistics-audit-dashboard/19-05-SUMMARY.md`
</output>
