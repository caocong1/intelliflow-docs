---
phase: 26-conditional-node-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/backend/src/db/schema.ts
  - packages/backend/drizzle/0006_add_blocked_status.sql
  - packages/backend/src/modules/runtime/conditions.service.ts
  - packages/backend/src/modules/runtime/runtime.service.ts
  - packages/backend/src/modules/runtime/background.service.ts
  - packages/backend/src/modules/workflows/validation.ts
autonomous: true
requirements:
  - COND-01
  - COND-02
  - COND-03
  - COND-04
  - COND-07
  - COND-08

must_haves:
  truths:
    - "NodeCondition and NodeExecutionRule types exist and are exported from shared types"
    - "All 5 config interfaces have optional executionRule field"
    - "NodeExecutionStatus includes 'blocked' value"
    - "advanceNode evaluates executionRule before entering a node — skip auto-skips, block sets blocked status"
    - "Background pipeline stops on blocked node and marks task as failed with descriptive message"
    - "Workflow validation rejects invalid executionRule references"
  artifacts:
    - path: "packages/shared/src/types.ts"
      provides: "NodeCondition, NodeExecutionRule types; blocked status; executionRule on all 5 config types"
      contains: "NodeCondition"
    - path: "packages/backend/src/db/schema.ts"
      provides: "blocked value in nodeExecutionStatusEnum"
      contains: "blocked"
    - path: "packages/backend/src/modules/runtime/conditions.service.ts"
      provides: "evaluateCondition and evaluateExecutionRule functions"
      exports: ["evaluateCondition", "evaluateExecutionRule"]
    - path: "packages/backend/src/modules/runtime/runtime.service.ts"
      provides: "Condition evaluation in advanceNode before setting in_progress"
    - path: "packages/backend/src/modules/runtime/background.service.ts"
      provides: "Condition evaluation in pipeline loop; blocked stops pipeline with notification"
    - path: "packages/backend/src/modules/workflows/validation.ts"
      provides: "executionRule validation rules"
  key_links:
    - from: "conditions.service.ts"
      to: "model-call.service.ts::resolveRef"
      via: "import and call"
      pattern: "resolveRef"
    - from: "runtime.service.ts::advanceNode"
      to: "conditions.service.ts::evaluateExecutionRule"
      via: "call before setting in_progress"
      pattern: "evaluateExecutionRule"
    - from: "background.service.ts"
      to: "conditions.service.ts::evaluateExecutionRule"
      via: "call in pipeline loop after skippable check"
      pattern: "evaluateExecutionRule"
---

<objective>
Add conditional node execution types, backend evaluation logic, and runtime integration.

Purpose: Enable nodes to be automatically skipped or blocked based on upstream output values, evaluated at runtime before entering each node.
Output: Shared types (NodeCondition, NodeExecutionRule, blocked status), conditions.service.ts, modified advanceNode/background pipeline, workflow validation.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-conditional-node-execution/26-CONTEXT.md
@.planning/phases/26-conditional-node-execution/26-RESEARCH.md

@packages/shared/src/types.ts
@packages/backend/src/db/schema.ts
@packages/backend/src/modules/runtime/runtime.service.ts
@packages/backend/src/modules/runtime/background.service.ts
@packages/backend/src/modules/runtime/model-call.service.ts
@packages/backend/src/modules/workflows/validation.ts

<interfaces>
<!-- Key types the executor needs -->

From packages/shared/src/types.ts:
```typescript
export interface VariableRef {
  nodeId: string;
  outputId: string;       // stores segmentKey
  variableName: string;
  fieldPath?: string;
}

export type NodeExecutionStatus = "pending" | "in_progress" | "completed" | "skipped" | "failed";

// All 5 config types end with optional skippable/autoAdvance fields:
export interface InputTransformConfig { type: "input_transform"; ... }
export interface DesensitizeConfig { type: "desensitize"; ... }
export interface ModelCallConfig { type: "model_call"; ... }
export interface RestoreConfig { type: "restore"; ... }
export interface ExportConfig { type: "export"; ... }
export type NodeConfig = InputTransformConfig | DesensitizeConfig | ModelCallConfig | RestoreConfig | ExportConfig;
```

From packages/backend/src/modules/runtime/model-call.service.ts:
```typescript
export function resolveRef(
  ref: { nodeId: string; outputId: string; fieldPath?: string },
  nodeExecs: Array<{ nodeId: string; outputData: Record<string, unknown> | null }>,
): string | undefined;
```

From packages/backend/src/db/schema.ts (line 201):
```typescript
export const nodeExecutionStatusEnum = pgEnum("node_execution_status", [
  "pending", "in_progress", "completed", "skipped", "failed",
]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared types + DB migration + conditions service</name>
  <files>
    packages/shared/src/types.ts,
    packages/backend/src/db/schema.ts,
    packages/backend/drizzle/0006_add_blocked_status.sql,
    packages/backend/src/modules/runtime/conditions.service.ts
  </files>
  <action>
1. In `packages/shared/src/types.ts`:
   - Add `NodeCondition` interface: `{ sourceRef: VariableRef; operator: "equals" | "not_equals" | "exists" | "not_exists" | "contains"; value?: string; }` — place it right after the VariableRef interface.
   - Add `NodeExecutionRule` interface: `{ action: "skip" | "block"; conditions: NodeCondition[]; logic: "and" | "or"; }` — place it right after NodeCondition.
   - Extend `NodeExecutionStatus` to include `"blocked"`: `"pending" | "in_progress" | "completed" | "skipped" | "failed" | "blocked"`.
   - Add optional `executionRule?: NodeExecutionRule` field to ALL 5 config interfaces: `InputTransformConfig`, `DesensitizeConfig`, `ModelCallConfig`, `RestoreConfig`, `ExportConfig`. Place it after the existing `skippable?: boolean` field in each.
   - Export the new types.

2. In `packages/backend/src/db/schema.ts`:
   - Add `"blocked"` to the `nodeExecutionStatusEnum` pgEnum array (after `"failed"`).

3. Create migration `packages/backend/drizzle/0006_add_blocked_status.sql`:
   ```sql
   ALTER TYPE node_execution_status ADD VALUE IF NOT EXISTS 'blocked';
   ```
   Note: This must NOT be inside a transaction block.

4. Create `packages/backend/src/modules/runtime/conditions.service.ts`:
   - Import `resolveRef` from `./model-call.service`.
   - Import `NodeCondition`, `NodeExecutionRule` from `@intelliflow/shared`.
   - Implement `evaluateCondition(cond: NodeCondition, resolvedValue: string | undefined): boolean` with switch on operator:
     - `exists`: `resolvedValue !== undefined && resolvedValue !== ""`
     - `not_exists`: `resolvedValue === undefined || resolvedValue === ""`
     - `equals`: `resolvedValue === cond.value`
     - `not_equals`: `resolvedValue !== cond.value`
     - `contains`: `resolvedValue !== undefined && resolvedValue.includes(cond.value ?? "")`
   - Implement `evaluateExecutionRule(rule: NodeExecutionRule, nodeExecs: Array<{ nodeId: string; outputData: Record<string, unknown> | null }>): { triggered: boolean; reason: string }`:
     - For each condition, call `resolveRef({ nodeId: cond.sourceRef.nodeId, outputId: cond.sourceRef.outputId, fieldPath: cond.sourceRef.fieldPath }, nodeExecs)` to get value.
     - Evaluate each condition via `evaluateCondition`.
     - Combine results: `rule.logic === "and" ? every : some`.
     - Build reason string from met conditions: `"variableName operator value"` joined by ` AND ` or ` OR `.
   - Export both functions.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bunx tsc --noEmit --project packages/shared/tsconfig.json && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>NodeCondition, NodeExecutionRule exported from shared types; blocked in NodeExecutionStatus and pgEnum; all 5 config types have executionRule field; conditions.service.ts compiles with evaluateCondition and evaluateExecutionRule functions</done>
</task>

<task type="auto">
  <name>Task 2: Runtime integration — advanceNode, background pipeline, validation</name>
  <files>
    packages/backend/src/modules/runtime/runtime.service.ts,
    packages/backend/src/modules/runtime/background.service.ts,
    packages/backend/src/modules/workflows/validation.ts
  </files>
  <action>
1. In `packages/backend/src/modules/runtime/runtime.service.ts::advanceNode()`:
   - Import `evaluateExecutionRule` from `./conditions.service`.
   - After finding `nextNode` (line ~236) and before setting it to `in_progress`, insert condition evaluation:
     - Get workflow data (already available as `wfData` from line 240).
     - Find `nextNodeDef` from `wfData.nodes` matching `nextNode.nodeId`.
     - Read `executionRule` from `nextNodeDef?.config?.executionRule`.
     - If `executionRule` exists:
       - Query all current node executions for this document (fresh from DB, not stale).
       - Call `evaluateExecutionRule(executionRule, allCurrentExecs)`.
       - If `triggered && action === "skip"`: update `nextNode` status to `"skipped"` with `outputData: { skipReason: reason, skipType: "conditional" }`, set `completedAt`. Then recursively call `advanceNode(documentId, nextNode.id, userId)` to find the real next node.
       - If `triggered && action === "block"`: update `nextNode` status to `"blocked"` with `outputData: { blockReason: reason, blockType: "conditional" }`, set `updatedAt`. Return the current runtime state (blocked node is now visible to frontend).
   - Add a recursion depth guard: pass an optional `depth` parameter (default 0), if depth > 50 throw an error to prevent infinite loops from misconfigured conditions.

2. In `packages/backend/src/modules/runtime/background.service.ts::executeDocumentPipeline()`:
   - Import `evaluateExecutionRule` from `./conditions.service`.
   - In the pipeline loop, AFTER the `skippable + autoAdvance` check (line ~230-243) and BEFORE setting node to `in_progress` (line ~246):
     - Read `executionRule` from `nodeDef.config?.executionRule`.
     - If `executionRule` exists:
       - Query fresh node executions from DB: `SELECT * FROM nodeExecutions WHERE documentId = ? AND isCurrent = true ORDER BY stepOrder ASC`.
       - Call `evaluateExecutionRule(executionRule, freshExecs)`.
       - If `triggered && action === "skip"`: set status to `"skipped"` with `outputData: { skipReason: reason, skipType: "conditional" }`, update progress, `continue` to next node.
       - If `triggered && action === "block"`: set status to `"blocked"` with `outputData: { blockReason: reason, blockType: "conditional" }`. Mark background task as `"failed"` with `errorMessage: "条件阻断：节点「${nodeDef.label}」- ${reason}"`. Send notification (reuse existing notification helper pattern). `return` to stop pipeline.

3. In `packages/backend/src/modules/workflows/validation.ts::validateWorkflow()`:
   - Add validation for `executionRule` on each node:
     - If `node.config.executionRule` exists:
       - `action` must be "skip" or "block".
       - `logic` must be "and" or "or".
       - `conditions` must be a non-empty array.
       - For each condition:
         - `sourceRef.nodeId` must exist in the workflow nodes.
         - `sourceRef.nodeId` must be upstream of the current node (use edge traversal — check that a path exists from sourceRef.nodeId to current node via edges).
         - When operator is `equals`, `not_equals`, or `contains`: `value` must be a non-empty string.
         - When operator is `exists` or `not_exists`: `value` should be absent or empty (warn but don't block).
     - Add error messages like: `"节点 "${node.label}" 的执行条件引用了不存在的上游节点"`.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>advanceNode evaluates executionRule before entering nodes (skip auto-advances, block stops); background pipeline stops on block with notification; validation rejects invalid executionRule references; all compiles cleanly</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` passes for both shared and backend packages
- New types `NodeCondition`, `NodeExecutionRule` are exported from shared/types.ts
- `"blocked"` is in the pgEnum definition
- conditions.service.ts exports both evaluation functions
- advanceNode has condition check before in_progress transition
- background.service.ts has condition check in pipeline loop
- validation.ts checks executionRule references
</verification>

<success_criteria>
- All new types compile and are exported
- advanceNode handles skip (recursive advance) and block (stop with status) correctly
- Background pipeline stops on block, sends notification, marks task failed
- Workflow validation rejects bad sourceRef references
- No regressions in existing TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/phases/26-conditional-node-execution/26-01-SUMMARY.md`
</output>
