---
phase: 28-file-security
plan: 04
type: execute
wave: 3
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - packages/backend/src/modules/runtime/export.service.ts
autonomous: true
requirements:
  - FSEC-07
  - FSEC-08

must_haves:
  truths:
    - "generateExport() calls sanitizeFilename(body.filename) before constructing the storage path"
    - "downloadExport() calls assertWithinRoot(getExportPath(documentId), storagePath) before readFile()"
    - "A tampered storagePath pointing to /tmp/evil.pdf is rejected by assertWithinRoot and returns null"
  artifacts:
    - path: "packages/backend/src/modules/runtime/export.service.ts"
      provides: "path-traversal-safe export generation and download"
      contains: "sanitizeFilename|assertWithinRoot"
  key_links:
    - from: "export.service.ts::generateExport"
      to: "sanitize.ts::sanitizeFilename"
      via: "sanitize filename parameter before writing"
      pattern: "sanitizeFilename.*filename"
    - from: "export.service.ts::downloadExport"
      to: "sanitize.ts::assertWithinRoot"
      via: "validate storagePath before readFile"
      pattern: "assertWithinRoot.*storagePath"
---

<objective>
Sanitize export filenames on write and validate stored paths on read.

Purpose: Prevent path traversal during export generation (FSEC-07) and reject tampered storage paths during download (FSEC-08).
Output: generateExport sanitizes the filename parameter; downloadExport validates the DB storagePath is within the export directory.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/28-file-security/28-01-SUMMARY.md

@packages/backend/src/modules/runtime/export.service.ts
@packages/backend/src/modules/runtime/export.routes.ts
@packages/backend/src/common/sanitize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sanitize filename parameter in generateExport</name>
  <files>packages/backend/src/modules/runtime/export.service.ts</files>
  <read_first>packages/backend/src/modules/runtime/export.service.ts (lines 1337-1400), packages/backend/src/common/sanitize.ts</read_first>
  <action>In `packages/backend/src/modules/runtime/export.service.ts`, add `import { sanitizeFilename } from "../../common/sanitize";` at the top of the file (after the existing imports). In `generateExport`, find where `const storagePath = join(exportDir, filename);` is constructed (around line 1381). Change it to `const safeFilename = sanitizeFilename(filename); const storagePath = join(exportDir, safeFilename);`. Also update the `insertDocumentFile` call: change `originalName: filename` to `originalName: filename` (keep originalName as the user's original name for display purposes, not sanitized). Add a comment `// sanitize to prevent path traversal in filename` above the safeFilename line.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "sanitizeFilename|safeFilename" packages/backend/src/modules/runtime/export.service.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `sanitizeFilename` is imported in export.service.ts
    - `generateExport` applies `sanitizeFilename(filename)` before constructing the storage path
    - `originalName` in insertDocumentFile stays as the raw `filename` (display name)
    - `storagePath` always ends with a sanitized filename
  </acceptance_criteria>
  <done>generateExport sanitizes user-supplied filenames before writing export files.</done>
</task>

<task type="auto">
  <name>Task 2: Validate storagePath in downloadExport with assertWithinRoot</name>
  <files>packages/backend/src/modules/runtime/export.service.ts</files>
  <read_first>packages/backend/src/modules/runtime/export.service.ts (lines 1424-1462), packages/backend/src/common/sanitize.ts, packages/backend/src/modules/files/files.service.ts</read_first>
  <action>In `packages/backend/src/modules/runtime/export.service.ts`, add `import { assertWithinRoot } from "../../common/sanitize";` at the top (add to the existing import line from sanitize.ts if already present). In `downloadExport`, immediately after extracting `const storagePath = output.storagePath as string;` (line 1440), add a validation call: `const validatedPath = assertWithinRoot(getExportPath(documentId), storagePath);`. Then change `const buffer = await readFile(storagePath);` to `const buffer = await readFile(validatedPath);`. Add a comment `// assertWithinRoot prevents serving files outside export directory` above the validatedPath assignment. Do not change any other logic, return shape, or mimeType detection.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "assertWithinRoot|validatedPath" packages/backend/src/modules/runtime/export.service.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `assertWithinRoot` is imported in export.service.ts
    - `downloadExport` calls `assertWithinRoot(getExportPath(documentId), storagePath)` before readFile
    - If storagePath was tampered to `/tmp/evil.pdf`, assertWithinRoot throws AppError(400) and the function propagates the error
    - The validatedPath is used for the readFile call
  </acceptance_criteria>
  <done>downloadExport rejects tampered storage paths outside the export directory.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/backend/tsconfig.json` passes
- `generateExport` sanitizes the filename before constructing the storage path
- `downloadExport` calls `assertWithinRoot` on the storagePath from the DB before `readFile`
- Tampered paths are rejected with a 400 error, not silently served
</verification>

<success_criteria>
- A malicious filename like "../../../tmp/evil.pdf" submitted to generateExport is stored as "tmp_evil_pdf" under the export directory
- A tampered storagePath in the DB (e.g., "/tmp/evil.pdf") causes assertWithinRoot to throw before readFile in downloadExport
- The originalName in DB and the download response use the unsanitized display name
</success_criteria>

<output>
After completion, create `.planning/phases/28-file-security/28-04-SUMMARY.md`
</output>
