---
phase: 29-xss-defense
plan: 04
type: execute
wave: 3
depends_on: ["29-01"]
files_modified:
  - packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx
  - packages/frontend/src/components/workflow/prompt/PromptEditor.tsx
autonomous: true
requirements:
  - XSS-04

must_haves:
  truths:
    - "ExportExecutor.tsx imports sanitizeHtml and wraps renderMarkdown() with sanitizeHtml() on innerHTML"
    - "PromptEditor.tsx wraps the innerHTML assignment in syncEditorFromProp() and initEditor() with sanitizeHtml()"
    - "The PromptEditor buildEditorHTML function output is passed through sanitizeHtml() before being assigned to editorRef.innerHTML"
  artifacts:
    - path: "packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx"
      provides: "XSS-safe export preview with sanitizeHtml on renderMarkdown output"
      contains: "import.*sanitizeHtml|innerHTML={sanitizeHtml"
    - path: "packages/frontend/src/components/workflow/prompt/PromptEditor.tsx"
      provides: "XSS-safe prompt editor with sanitizeHtml on buildEditorHTML output"
      contains: "import.*sanitizeHtml|innerHTML.*sanitizeHtml"
---

<objective>
Wrap innerHTML usages in ExportExecutor.tsx and PromptEditor.tsx with sanitizeHtml() for defense-in-depth.

Purpose: ExportExecutor displays AI-generated markdown previews, and PromptEditor constructs HTML from user-authored variable templates. Both are secondary but visible rendering paths where sanitizeHtml adds defense-in-depth protection.
Output: `renderMarkdown(previewContent())` in ExportExecutor and `buildEditorHTML(props.value, ...)` in PromptEditor are both wrapped with `sanitizeHtml()` before innerHTML assignment.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/29-xss-defense/29-01-SUMMARY.md

@packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx
@packages/frontend/src/components/workflow/prompt/PromptEditor.tsx
@packages/frontend/src/lib/sanitize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sanitizeHtml import to ExportExecutor.tsx</name>
  <files>packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx</files>
  <read_first>packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx (lines 1-5)</read_first>
  <action>In `packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx`, add `import { sanitizeHtml } from "../../../lib/sanitize";` after the api import on line 3.</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "import.*sanitizeHtml" packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx</verify>
  <acceptance_criteria>
    - `sanitizeHtml` is imported from "../../../lib/sanitize"
  </acceptance_criteria>
  <done>sanitizeHtml is available in ExportExecutor.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap ExportExecutor preview innerHTML with sanitizeHtml()</name>
  <files>packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx</files>
  <read_first>packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx (lines 288-296)</read_first>
  <action>In `packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx`, find the preview div around line 293: `<div ... innerHTML={renderMarkdown(previewContent())} ...>`. Change `innerHTML={renderMarkdown(previewContent())}` to `innerHTML={sanitizeHtml(renderMarkdown(previewContent()))}`. Preserve all other attributes (class, prose, max-h-96, etc.).
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "innerHTML.*renderMarkdown" packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx</verify>
  <acceptance_criteria>
    - The preview innerHTML uses `sanitizeHtml(renderMarkdown(previewContent()))`
    - No other innerHTML in ExportExecutor.tsx is unsanitized
  </acceptance_criteria>
  <done>ExportExecutor preview innerHTML is sanitized.</done>
</task>

<task type="auto">
  <name>Task 3: Add sanitizeHtml import to PromptEditor.tsx</name>
  <files>packages/frontend/src/components/workflow/prompt/PromptEditor.tsx</files>
  <read_first>packages/frontend/src/components/workflow/prompt/PromptEditor.tsx (lines 1-10)</read_first>
  <action>In `packages/frontend/src/components/workflow/prompt/PromptEditor.tsx`, add `import { sanitizeHtml } from "../../../lib/sanitize";` after the existing imports (after the type imports on line 3).</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "import.*sanitizeHtml" packages/frontend/src/components/workflow/prompt/PromptEditor.tsx</verify>
  <acceptance_criteria>
    - `sanitizeHtml` is imported from "../../../lib/sanitize"
  </acceptance_criteria>
  <done>sanitizeHtml is available in PromptEditor.tsx.</done>
</task>

<task type="auto">
  <name>Task 4: Wrap PromptEditor innerHTML assignments with sanitizeHtml()</name>
  <files>packages/frontend/src/components/workflow/prompt/PromptEditor.tsx</files>
  <read_first>packages/frontend/src/components/workflow/prompt/PromptEditor.tsx (lines 206-226, 393-398)</read_first>
  <action>In `packages/frontend/src/components/workflow/prompt/PromptEditor.tsx`, find the two places where `editorRef.innerHTML` is set:
1. In `syncEditorFromProp()` around line 212: change `editorRef.innerHTML = buildEditorHTML(props.value, props.upstreamNodes);` to `editorRef.innerHTML = sanitizeHtml(buildEditorHTML(props.value, props.upstreamNodes));`.
2. In `initEditor()` around line 397: change `el.innerHTML = buildEditorHTML(props.value, props.upstreamNodes);` to `el.innerHTML = sanitizeHtml(buildEditorHTML(props.value, props.upstreamNodes));`.

This ensures that the HTML produced by `buildEditorHTML` (which includes user-supplied variable display names) is sanitized before DOM insertion. The variable keys (nodeId, segmentKey) used in data-var attributes are controlled by the system, but user-visible display names could potentially contain malicious content if a node label is crafted adversarially.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "editorRef.innerHTML|el.innerHTML" packages/frontend/src/components/workflow/prompt/PromptEditor.tsx</verify>
  <acceptance_criteria>
    - Both `editorRef.innerHTML` assignments in syncEditorFromProp() and initEditor() use `sanitizeHtml(buildEditorHTML(...))`
    - No raw `innerHTML =` assignment without sanitizeHtml wrapper remains in PromptEditor.tsx
    - `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
  </acceptance_criteria>
  <done>PromptEditor innerHTML assignments are sanitized.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
- `rg "innerHTML=(?!sanitizeHtml|true|false)" packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx packages/frontend/src/components/workflow/prompt/PromptEditor.tsx` returns zero results
- All innerHTML assignments in both files call sanitizeHtml()
</verification>

<success_criteria>
- XSS-04 is fully satisfied: all 3 components (InlineEditor, ExportExecutor, PromptEditor) sanitize innerHTML
- XSS-03 is fully satisfied: render-markdown.tsx sanitizes all innerHTML
- XSS-01 and XSS-02 are fully satisfied: DOMPurify is installed and sanitizeHtml() is available
- Phase 29 success criteria: all innerHTML assignments route through sanitizeHtml()
</success_criteria>

<output>
After completion, create `.planning/phases/29-xss-defense/29-04-SUMMARY.md`
</output>
