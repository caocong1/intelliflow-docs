---
phase: 13-document-runtime-refactor-align-phase12
plan: 05
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - packages/frontend/src/components/workspace/nodes/RestoreExecutor.tsx
  - packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx
autonomous: true
requirements:
  - NODE-17
  - NODE-18
  - NODE-19
  - NODE-20
  - NODE-21
  - NODE-22

must_haves:
  truths:
    - "RestoreExecutor displays all UI text in Chinese with Stitch-designed layout"
    - "ExportExecutor displays all UI text in Chinese and hides PPT option"
    - "ExportExecutor works correctly with models Record data structure (via backend fix)"
    - "Both executors use real config from workflowNodes"
  artifacts:
    - path: "packages/frontend/src/components/workspace/nodes/RestoreExecutor.tsx"
      provides: "Chinese-localized restore executor with split-view comparison"
    - path: "packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx"
      provides: "Chinese-localized export executor without PPT option"
  key_links:
    - from: "packages/frontend/src/components/workspace/nodes/RestoreExecutor.tsx"
      to: "packages/shared/src/types.ts"
      via: "RestoreConfig prop"
      pattern: "RestoreConfig"
    - from: "packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx"
      to: "packages/shared/src/types.ts"
      via: "ExportConfig prop with format options"
      pattern: "ExportConfig"
---

<objective>
Redesign RestoreExecutor and ExportExecutor with Chinese localization and Stitch-designed UI. Hide PPT option in export. Ensure export works with the fixed models Record data structure.

Purpose: These executors handle the final stages — restore sensitive info and export the document. Must display Chinese and work with current data structures.
Output: Two redesigned executor components with professional Chinese UI.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/shared/src/types.ts
@packages/frontend/src/components/workspace/nodes/RestoreExecutor.tsx
@packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx
@.planning/phases/13-document-runtime-refactor-align-phase12/13-01-SUMMARY.md
@.planning/phases/13-document-runtime-refactor-align-phase12/13-02-SUMMARY.md

<interfaces>
From packages/shared/src/types.ts:
```typescript
export interface RestoreConfig {
  autoRestore?: boolean;
  showDiff?: boolean;
}

export interface ExportConfig {
  formats: ("word" | "pdf" | "markdown" | "ppt")[];
  defaultFormat?: "word" | "pdf" | "markdown" | "ppt";
  templateId?: string;
  contentMapping?: VariableRef[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign RestoreExecutor — Stitch design + Chinese UI</name>
  <files>packages/frontend/src/components/workspace/nodes/RestoreExecutor.tsx</files>
  <action>
1. **Generate Stitch design mockup** for the restore executor using Stitch MCP (`GEMINI_3_1_PRO`). Prompt:
   - "Sensitive information restoration interface for Chinese enterprise document platform"
   - "SolidJS + Tailwind CSS, split-view layout with before/after comparison"
   - "Left panel: desensitized text with highlighted placeholders. Right panel: restored text with highlighted real values"
   - "Failed items marked in red with inline edit capability"
   - "All Chinese: 信息恢复, 恢复前, 恢复后, 恢复成功, 恢复失败, 手动修正, 确认恢复"

2. **Implement redesigned UI:**
   - Heading: "信息恢复"
   - Split view labels: "脱敏文本 (恢复前)" / "恢复文本 (恢复后)"
   - Highlight colors: green for successfully restored, red for failed
   - Status messages: "正在恢复...", "恢复完成", "N 处恢复成功，M 处需手动修正"
   - Failed item action: "手动修正" button that opens inline edit
   - Inline edit labels: "原始占位符", "替换为", "保存修正", "取消"
   - Confirm button: "确认恢复结果"
   - Error: "恢复失败，请重试"

3. Keep existing functional logic: read desensitize mappings, replace placeholders, show diff, allow manual correction of failed items.

4. Add null guard for config: if `props.config` is undefined, show "正在加载恢复配置..." message.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "信息恢复\|恢复前\|手动修正\|确认恢复" packages/frontend/src/components/workspace/nodes/RestoreExecutor.tsx</automated>
  </verify>
  <done>RestoreExecutor displays Chinese UI with Stitch-designed split-view layout. All text localized. Failed items have manual correction capability.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ExportExecutor — Stitch design + Chinese UI + hide PPT</name>
  <files>packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx</files>
  <action>
1. **Generate Stitch design mockup** for the export executor using Stitch MCP (`GEMINI_3_1_PRO`). Prompt:
   - "Document export interface for Chinese enterprise document platform"
   - "SolidJS + Tailwind CSS, format selector (Word/PDF/Markdown), preview area, filename editor, download button"
   - "Clean card-based layout with preview panel"
   - "All Chinese: 文件导出, 导出格式, 文件预览, 文件名称, 下载文件, 导出中..."

2. **Implement redesigned UI:**
   - Heading: "文件导出"
   - Format selector: "Word 文档", "PDF 文件", "Markdown 文件" — **hide PPT option** by filtering out "ppt" from `props.config.formats`:
   ```typescript
   const availableFormats = () => (props.config?.formats ?? ["word", "pdf", "markdown"]).filter(f => f !== "ppt");
   ```
   - Format labels map: `{ word: "Word 文档", pdf: "PDF 文件", markdown: "Markdown 文件" }`
   - Preview area: "文件预览" heading, rendered preview content
   - Filename editor: "文件名称" label, text input with ".docx" / ".pdf" / ".md" suffix shown
   - Download button: "下载文件" with download icon
   - Status: "正在生成...", "导出完成", "导出失败，请重试"
   - Preview loading: "正在生成预览..."

3. Keep existing functional logic: format selection, preview generation via API, filename editing, download.

4. The backend `resolveContent` fix (Plan 01) ensures the export API finds content from the `models` Record. No frontend changes needed for data lookup — just verify the export preview and download work end-to-end.

5. Add null guard for config: if `props.config` is undefined, show "正在加载导出配置..." message.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "文件导出\|导出格式\|下载文件" packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx && ! grep -q '"ppt"' packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx && echo "ppt hidden"</automated>
  </verify>
  <done>ExportExecutor displays Chinese UI with Stitch design. PPT option hidden. Format selector shows Word/PDF/Markdown in Chinese. Preview and download work with fixed backend.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- RestoreExecutor shows Chinese split-view comparison with highlighted restorations
- ExportExecutor format selector shows only Word/PDF/Markdown (no PPT)
- All text in both executors is in Chinese
- Export preview works (requires backend resolveContent fix from Plan 01)
</verification>

<success_criteria>
Restore and export executors are redesigned with professional Chinese UI. PPT is hidden from export options. Both work with real configs from workflowNodes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-document-runtime-refactor-align-phase12/13-05-SUMMARY.md`
</output>
