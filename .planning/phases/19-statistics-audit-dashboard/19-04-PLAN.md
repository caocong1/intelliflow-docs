---
phase: 19-statistics-audit-dashboard
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - packages/frontend/src/pages/admin/stats/ModelStats.tsx
  - packages/frontend/src/pages/admin/stats/UserStats.tsx
  - packages/frontend/src/pages/admin/stats/WorkflowStats.tsx
  - packages/frontend/src/pages/admin/StatsDashboard.tsx
requirements: [STAT-02, STAT-03, STAT-05, STAT-06, STAT-07]
autonomous: true

must_haves:
  truths:
    - "Model stats tab shows per-model table (calls, tokens, success rate, cost) and per-model trend chart"
    - "User stats tab shows per-user table (frequency, doc count, tokens, cost) and per-user trend chart"
    - "Workflow stats tab shows per-workflow table (usage count, user count, doc count, cost) and trend chart"
    - "All dimension tabs respect shared filter state"
  artifacts:
    - path: "packages/frontend/src/pages/admin/stats/ModelStats.tsx"
      provides: "Model dimension statistics with table and trend chart"
    - path: "packages/frontend/src/pages/admin/stats/UserStats.tsx"
      provides: "User dimension statistics with table and trend chart"
    - path: "packages/frontend/src/pages/admin/stats/WorkflowStats.tsx"
      provides: "Workflow dimension statistics with table and trend chart"
  key_links:
    - from: "ModelStats.tsx"
      to: "statistics.ts API fetchByModel"
      via: "createResource"
    - from: "StatsDashboard.tsx"
      to: "ModelStats/UserStats/WorkflowStats"
      via: "lazy tab rendering"
---

<objective>
Build the three dimension drill-down tabs: model stats, user stats, and workflow stats.

Purpose: Allow admins to drill down into usage by specific dimensions with both tabular data and trend visualization.
Output: Three complete tab panels with tables and charts.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-statistics-audit-dashboard/19-RESEARCH.md
@.planning/phases/19-statistics-audit-dashboard/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model, User, and Workflow stats tab components</name>
  <files>packages/frontend/src/pages/admin/stats/ModelStats.tsx, packages/frontend/src/pages/admin/stats/UserStats.tsx, packages/frontend/src/pages/admin/stats/WorkflowStats.tsx</files>
  <action>
All three follow the same layout pattern: top section = trend chart, bottom section = data table.

**ModelStats.tsx:**
- Props: `filters` accessor (shared filter state)
- Fetch `fetchByModel(filters)` via createResource — returns `{ summary, trends }`
- Top: Line chart (ChartContainer) with per-model trend lines. Each model = one series. X-axis = period, Y-axis = call count. Show legend to toggle models.
- Bottom: Table with columns: 模型名称, 调用次数, Token 消耗, 成功率(%), 估算成本($). Sort by call count desc. Use Intl.NumberFormat for numbers. Format success rate as percentage.
- Expandable rows: clicking a model row expands to show that model's mini trend sparkline (optional — use Claude's discretion; if complexity is high, skip expand and the chart above suffices).

**UserStats.tsx:**
- Fetch `fetchByUser(filters)` — returns `{ summary, trends }`
- Top: Bar chart showing top 10 users by call count (vertical bars).
- Bottom: Table with columns: 用户, 调用次数, 文档生成数, Token 消耗, 估算成本($). Sort by call count desc.

**WorkflowStats.tsx:**
- Fetch `fetchByWorkflow(filters)` — returns `{ summary, trends }`
- Top: Line chart with per-workflow trend lines.
- Bottom: Table with columns: 流程名称, 使用次数, 用户数, 文档数, 估算成本($). Sort by usage count desc.

Shared patterns across all three:
- Use Tailwind styling consistent with existing admin pages
- Loading state: show skeleton/spinner while data loads
- Empty state: show "暂无数据" message when no data
- Format large numbers with Intl.NumberFormat (e.g., 1,234,567)
- Cost displays "$X.XX" format
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs/packages/frontend && test -f src/pages/admin/stats/ModelStats.tsx && test -f src/pages/admin/stats/UserStats.tsx && test -f src/pages/admin/stats/WorkflowStats.tsx</automated>
  </verify>
  <done>Three dimension tab components render tables and charts from API data with proper formatting</done>
</task>

<task type="auto">
  <name>Task 2: Wire dimension tabs into dashboard</name>
  <files>packages/frontend/src/pages/admin/StatsDashboard.tsx</files>
  <action>
Update StatsDashboard.tsx:

1. Import ModelStats, UserStats, WorkflowStats.
2. Replace tab placeholder content with actual components:
   - Tab "模型统计" → `<ModelStats filters={filters} />`
   - Tab "用户统计" → `<UserStats filters={filters} />`
   - Tab "流程统计" → `<WorkflowStats filters={filters} />`
3. Use `<Show when={activeTab() === "model"}>` pattern for lazy rendering — only render the active tab's component to avoid unnecessary API calls.
4. The "审计明细" tab remains as placeholder (next plan).
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs/packages/frontend && grep -q "ModelStats" src/pages/admin/StatsDashboard.tsx && grep -q "UserStats" src/pages/admin/StatsDashboard.tsx && grep -q "WorkflowStats" src/pages/admin/StatsDashboard.tsx</automated>
  </verify>
  <done>Switching to model/user/workflow tabs renders the corresponding stats component with live data</done>
</task>

</tasks>

<verification>
- `bun run check` passes
- Each dimension tab shows both chart and table
- Filter changes cause all tabs to re-fetch
</verification>

<success_criteria>
Admin can switch between model, user, and workflow tabs to see dimension-specific statistics with both trend charts and summary tables.
</success_criteria>

<output>
After completion, create `.planning/phases/19-statistics-audit-dashboard/19-04-SUMMARY.md`
</output>
