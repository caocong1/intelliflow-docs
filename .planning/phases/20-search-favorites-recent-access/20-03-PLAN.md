---
phase: 20-search-favorites-recent-access
plan: 03
type: execute
wave: 2
depends_on: ["20-01", "20-02"]
files_modified:
  - packages/frontend/src/components/favorites/FavoriteButton.tsx
  - packages/frontend/src/pages/projects/ProjectHome.tsx
  - packages/frontend/src/pages/projects/ProjectList.tsx
  - packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
autonomous: true
requirements: [SRCH-03, SRCH-04, SRCH-05]

must_haves:
  truths:
    - "Star icon appears on project cards in ProjectList and project header in ProjectHome"
    - "Star icon appears on document workspace header"
    - "Clicking star toggles favorite state (filled=favorited, outline=not)"
    - "Entering ProjectHome records a recent access event for that project"
    - "Entering DocumentWorkspace records a recent access event for that document"
  artifacts:
    - path: "packages/frontend/src/components/favorites/FavoriteButton.tsx"
      provides: "Reusable star toggle component"
      exports: ["default"]
      min_lines: 25
  key_links:
    - from: "packages/frontend/src/components/favorites/FavoriteButton.tsx"
      to: "packages/frontend/src/lib/api/user-activity.ts"
      via: "toggleFavorite API call on click"
      pattern: "toggleFavorite"
    - from: "packages/frontend/src/pages/projects/ProjectHome.tsx"
      to: "packages/frontend/src/lib/api/user-activity.ts"
      via: "recordAccess on mount"
      pattern: "recordAccess.*project"
    - from: "packages/frontend/src/pages/workspace/DocumentWorkspace.tsx"
      to: "packages/frontend/src/lib/api/user-activity.ts"
      via: "recordAccess on mount"
      pattern: "recordAccess.*document"
---

<objective>
Integrate FavoriteButton component into existing pages and add recent access recording hooks on detail page entry.

Purpose: Wire the favorites and recent access features into the actual user workflow. Users see star icons on projects and documents they can toggle. Visiting detail pages automatically records access history.
Output: FavoriteButton component, star icons on project/document views, automatic access recording on detail pages.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-search-favorites-recent-access/20-CONTEXT.md
@.planning/phases/20-search-favorites-recent-access/20-01-SUMMARY.md

@packages/frontend/src/pages/projects/ProjectHome.tsx
@packages/frontend/src/pages/projects/ProjectList.tsx
@packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
@packages/frontend/src/lib/api/user-activity.ts

<interfaces>
<!-- API client functions (from Plan 01/02) -->

```typescript
// From lib/api/user-activity.ts
export async function toggleFavorite(targetType: "project" | "document" | "workflow", targetId: string): Promise<{ favorited: boolean }>;
export async function checkFavorites(items: Array<{ targetType: string; targetId: string }>): Promise<string[]>;
export async function recordAccess(targetType: "project" | "document" | "workflow", targetId: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FavoriteButton component and integrate into project pages</name>
  <files>packages/frontend/src/components/favorites/FavoriteButton.tsx, packages/frontend/src/pages/projects/ProjectList.tsx, packages/frontend/src/pages/projects/ProjectHome.tsx</files>
  <action>
**FavoriteButton.tsx** — Reusable star toggle component:
- Props: `{ targetType: "project" | "document" | "workflow"; targetId: string; initialFavorited?: boolean; class?: string }`
- Internal `createSignal` for `favorited` state, initialized from `initialFavorited` (default false).
- Internal `createSignal` for `loading` to prevent double-clicks.
- On click: call `toggleFavorite(targetType, targetId)`, update local state from response `{ favorited }`.
- Render: `<button>` with star SVG. Filled star (solid fill, text-amber-400) when favorited, outline star (stroke only, text-slate-300 hover:text-amber-400) when not.
- Star SVG path (filled): `M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z`
- Use `fill="currentColor"` when favorited, `fill="none" stroke="currentColor"` when not.
- Button has `cursor-pointer`, `disabled` when loading, `transition-colors duration-150`.
- Prevent event bubbling with `e.stopPropagation()` (button may be inside a clickable card).

**ProjectList.tsx** — Add FavoriteButton to each project card:
- Import FavoriteButton and `checkFavorites`.
- After project list loads, call `checkFavorites` with all project IDs (targetType: "project") to get initial favorited state.
- Store result in a `createSignal<Set<string>>` of favorited project IDs.
- Place FavoriteButton in the top-right corner of each project card (absolute positioned or flex end).
- Pass `initialFavorited={favoritedIds().has(project.id)}`.

**ProjectHome.tsx** — Two additions:
1. Add FavoriteButton next to the project title/header area. Call `checkFavorites` for this single project to get initial state.
2. Add recent access recording: Use `onMount(() => { recordAccess("project", projectId) })` to record access when the page loads. Import `recordAccess` from user-activity API. Fire-and-forget (no await needed, don't block rendering). Only call if projectId is valid.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bunx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>FavoriteButton renders star icon with toggle behavior. ProjectList shows stars on all project cards with correct initial state. ProjectHome shows star and records recent access on mount.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate FavoriteButton and recent access into DocumentWorkspace</name>
  <files>packages/frontend/src/pages/workspace/DocumentWorkspace.tsx</files>
  <action>
**DocumentWorkspace.tsx** — Two additions:

1. **FavoriteButton**: Import FavoriteButton component. Add it next to the document title in the header area (near the existing document info section). Call `checkFavorites([{ targetType: "document", targetId: documentId }])` to determine initial state. Place the star button where it is visible but not intrusive (e.g., after the document title, inline).

2. **Recent access recording**: Import `recordAccess` from user-activity API. Use `onMount(() => { recordAccess("document", documentId) })` — fire-and-forget. Only call when documentId is available (not undefined). The CONTEXT.md specifies that only detail page entry counts, not list browsing — this is the correct page for documents.

Note: Workflow editor pages are admin-only. Per CONTEXT.md, workflows should also support favorites. However, the workflow editor is inside `/admin/workflows/:id/edit` which is admin-gated. For now, add favorite/access recording for projects and documents. Workflow favorites can be toggled from the search results and favorites page (Plan 02 already handles this display). Recording workflow access from the admin editor can be added if needed but is lower priority since admins manage workflows.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bunx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>DocumentWorkspace shows star icon next to document title with correct toggle behavior. Entering DocumentWorkspace records recent access for that document.</done>
</task>

</tasks>

<verification>
- Frontend TypeScript compilation passes
- FavoriteButton component is self-contained and reusable
- Star icons appear on ProjectList cards, ProjectHome header, DocumentWorkspace header
- Recent access is recorded on ProjectHome and DocumentWorkspace mount (not on list pages)
- Clicking star toggles visual state and persists via API
</verification>

<success_criteria>
- FavoriteButton renders correctly in both favorited and unfavorited states
- Batch checkFavorites call on ProjectList prevents N+1 API calls
- Recent access recording fires on mount without blocking page render
- Star toggle prevents double-click with loading state
- Event propagation stopped so clicking star on a card doesn't navigate
</success_criteria>

<output>
After completion, create `.planning/phases/20-search-favorites-recent-access/20-03-SUMMARY.md`
</output>
