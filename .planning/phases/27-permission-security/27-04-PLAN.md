---
phase: 27-permission-security
plan: 04
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - packages/backend/src/modules/runtime/export.routes.ts
autonomous: true
requirements:
  - PERM-04
  - PERM-05

must_haves:
  truths:
    - "POST /runtime/:documentId/export/:nodeExecutionId/generate uses canEditDocument before calling generateExport()"
    - "GET preview and GET download in export.routes.ts still use isDocumentProjectMember so read-only members can inspect and download existing exports"
    - "The download response still sets the same content-type and content-disposition headers after the permission change"
  artifacts:
    - path: "packages/backend/src/modules/runtime/export.routes.ts"
      provides: "creator-or-owner guard on export generation and membership guards on export reads"
      contains: "canEditDocument"
  key_links:
    - from: "export.routes.ts::POST generate"
      to: "versions.service.ts::canEditDocument"
      via: "route guard before generateExport()"
      pattern: "await canEditDocument("
    - from: "export.routes.ts::GET preview/download"
      to: "versions.service.ts::isDocumentProjectMember"
      via: "read-only export access gate"
      pattern: "await isDocumentProjectMember("
---

<objective>
Restrict export generation to document creators and project owners without breaking read-only export access.

Purpose: Close the dedicated export-generation gap from Phase 27 while keeping preview and download accessible to project members.
Output: `export.routes.ts` uses `canEditDocument()` only for `generate`, while `preview` and `download` stay on membership checks.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/27-permission-security/27-01-SUMMARY.md

@packages/backend/src/modules/runtime/export.routes.ts
@packages/backend/src/modules/runtime/export.service.ts
@packages/backend/src/modules/versions/versions.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move export generation to creator-or-owner access</name>
  <files>packages/backend/src/modules/runtime/export.routes.ts</files>
  <read_first>packages/backend/src/modules/runtime/export.routes.ts, packages/backend/src/modules/runtime/export.service.ts, packages/backend/src/modules/versions/versions.service.ts</read_first>
  <action>In `packages/backend/src/modules/runtime/export.routes.ts`, import `canEditDocument` next to `isDocumentProjectMember`. Change only `POST /:documentId/export/:nodeExecutionId/generate` to use `await canEditDocument(params.documentId, user!.id)` before the call to `generateExport(...)`. Update its 403 response text to `仅文档创建者或项目负责人可生成导出`. Do not change the `generateExport()` call signature, body validation schema, or any response payload shape.</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "generate|canEditDocument\\(" packages/backend/src/modules/runtime/export.routes.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -30</verify>
  <acceptance_criteria>
    - `packages/backend/src/modules/runtime/export.routes.ts` imports `canEditDocument`
    - The `generate` handler contains `await canEditDocument(params.documentId, user!.id)`
    - The `generate` 403 message is `仅文档创建者或项目负责人可生成导出`
  </acceptance_criteria>
  <done>Export generation is no longer available to ordinary project members.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve membership-only access for preview and download</name>
  <files>packages/backend/src/modules/runtime/export.routes.ts</files>
  <read_first>packages/backend/src/modules/runtime/export.routes.ts, packages/backend/src/modules/runtime/export.service.ts</read_first>
  <action>Keep `GET /:documentId/export/:nodeExecutionId/preview` and `GET /:documentId/export/:nodeExecutionId/download` on `await isDocumentProjectMember(params.documentId, user!.id)`. Do not move them to `canEditDocument`, because Phase 27 requires read-only members to retain export visibility. Keep the preview response shape unchanged, and keep the download header logic exactly as it is now: `content-type` from `result.mimeType` and `content-disposition` using `attachment; filename=\"${encodeURIComponent(result.filename)}\"`.</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "preview|download|isDocumentProjectMember\\(|Content-Disposition|content-type" packages/backend/src/modules/runtime/export.routes.ts</verify>
  <acceptance_criteria>
    - `preview` and `download` in `export.routes.ts` still use `await isDocumentProjectMember(params.documentId, user!.id)`
    - `preview` and `download` do not call `canEditDocument`
    - The `download` handler still sets `content-type` and `content-disposition` headers from `result.mimeType` and `result.filename`
  </acceptance_criteria>
  <done>Read-only export access remains intact for project members after the generation guard tightens.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/backend/tsconfig.json` passes
- `export.routes.ts` uses `canEditDocument` only in the `generate` handler
- `preview` and `download` still use `isDocumentProjectMember`
- The download response headers remain unchanged
</verification>

<success_criteria>
- Non-owner/non-creator members receive 403 on export generation
- Read-only members can still preview and download existing export artifacts
- Export route behavior remains unchanged outside the permission boundary
</success_criteria>

<output>
After completion, create `.planning/phases/27-permission-security/27-04-SUMMARY.md`
</output>
