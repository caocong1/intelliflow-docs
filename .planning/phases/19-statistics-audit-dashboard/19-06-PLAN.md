---
phase: 19-statistics-audit-dashboard
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/lib/api/statistics.ts
  - packages/frontend/src/pages/admin/stats/KpiCards.tsx
  - packages/frontend/src/pages/admin/stats/ModelStats.tsx
  - packages/frontend/src/pages/admin/stats/UserStats.tsx
  - packages/frontend/src/pages/admin/stats/WorkflowStats.tsx
  - packages/frontend/src/pages/admin/stats/OverviewCharts.tsx
autonomous: true
gap_closure: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
  - STAT-07

must_haves:
  truths:
    - "KPI cards display correct documentCount and successRate values (not NaN/0)"
    - "Model stats table shows successRate as 0-100 percentage (not 9500%)"
    - "User stats table shows correct user names and document counts"
    - "Workflow stats chart and table show correct usage counts"
    - "Overview recent audit table renders rows from API data"
  artifacts:
    - path: "packages/frontend/src/lib/api/statistics.ts"
      provides: "Correctly typed API interfaces matching backend field names"
      contains: "docCount"
    - path: "packages/frontend/src/pages/admin/stats/OverviewCharts.tsx"
      provides: "Fixed flattenAuditRecords using PaginatedResponse.data"
      contains: "auditDocs()?.data"
  key_links:
    - from: "packages/frontend/src/lib/api/statistics.ts"
      to: "packages/backend/src/modules/statistics/statistics.service.ts"
      via: "OverviewData interface field names match backend getOverviewKpis return object"
      pattern: "docCount.*avgSuccessRate"
    - from: "packages/frontend/src/pages/admin/stats/KpiCards.tsx"
      to: "packages/frontend/src/lib/api/statistics.ts"
      via: "card.key matches OverviewData field names"
      pattern: "docCount.*avgSuccessRate"
    - from: "packages/frontend/src/pages/admin/stats/WorkflowStats.tsx"
      to: "packages/frontend/src/lib/api/statistics.ts"
      via: "row field access matches fetchByWorkflow return type"
      pattern: "row\\.callCount"
---

<objective>
Fix 5 frontend data-binding gaps between backend API field names and frontend TypeScript types/components.

Purpose: Backend statistics API is complete and returns correct data, but 5 field name mismatches and one structural bug cause KPI cards, model stats, user stats, workflow stats, and overview audit table to display incorrect/empty values.

Output: All statistics dashboard components correctly render data from the backend API.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-statistics-audit-dashboard/19-VERIFICATION.md
@packages/frontend/src/lib/api/statistics.ts
@packages/backend/src/modules/statistics/statistics.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align frontend API types with backend field names</name>
  <files>packages/frontend/src/lib/api/statistics.ts</files>
  <action>
Fix 3 type mismatches in statistics.ts to match the actual backend response field names:

1. **OverviewData interface** (line 11-20): Rename `documentCount` to `docCount` and `successRate` to `avgSuccessRate`. The backend `getOverviewKpis` returns `docCount` (line 152) and `avgSuccessRate` (line 156).

2. **fetchByUser return type** (line 57-58): Change the inline return type fields from `displayName` to `userName` and from `documentCount` to `docCount`. The backend `getByUser` selects `userName: users.displayName` (line 268) and `docCount: countDistinct(...)` (line 270).

3. **fetchByWorkflow return type** (line 61-62): Change `usageCount` to `callCount` and `documentCount` to `docCount`. The backend `getByWorkflow` selects `callCount: count(modelCallLogs.id)` (line 344) and `docCount: countDistinct(...)` (line 346).

Do NOT rename backend fields — the backend API is correct and verified. Only update the frontend types to match.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -n "docCount\|avgSuccessRate\|userName\|callCount" packages/frontend/src/lib/api/statistics.ts</automated>
  </verify>
  <done>OverviewData uses docCount+avgSuccessRate; fetchByUser type uses userName+docCount; fetchByWorkflow type uses callCount+docCount — all matching backend field names</done>
</task>

<task type="auto">
  <name>Task 2: Fix consuming components to use corrected field names</name>
  <files>
    packages/frontend/src/pages/admin/stats/KpiCards.tsx
    packages/frontend/src/pages/admin/stats/ModelStats.tsx
    packages/frontend/src/pages/admin/stats/UserStats.tsx
    packages/frontend/src/pages/admin/stats/WorkflowStats.tsx
    packages/frontend/src/pages/admin/stats/OverviewCharts.tsx
  </files>
  <action>
Fix 5 component bugs corresponding to the type changes and one structural bug:

**KpiCards.tsx:**
- Line 23: Change `key: "documentCount"` to `key: "docCount"` in the "文档生成数" card definition.
- Line 27: Change `key: "successRate"` to `key: "avgSuccessRate"` in the "平均成功率(%)" card definition.

**ModelStats.tsx:**
- Line 98: Change `{(row.successRate * 100).toFixed(1)}%` to `{Number(row.successRate).toFixed(1)}%`. The backend SQL `ROUND(... * 100.0 / COUNT(*), 2)` already returns a 0-100 percentage value. Remove the `* 100` multiplication to avoid double-multiplying.

**UserStats.tsx:**
- Line 30: Change `u.displayName` to `u.userName` in the chart yAxis data mapping.
- Line 40: Change `u.callCount` to `u.callCount` (already correct for the chart data).
- Line 105: Change `row.displayName` to `row.userName` in the table cell.
- Line 110: Change `row.documentCount` to `row.docCount` in the table cell.

**WorkflowStats.tsx:**
- Line 37: Change `w.usageCount` to `w.callCount` in the chart series data mapping.
- Line 98 (For each): Update `sortedByUsageCount` function to sort by `callCount` instead of `usageCount`.
- Line 103: Change `row.usageCount` to `row.callCount` in the table cell.
- Line 122-123: Rename `sortedByUsageCount` to `sortedByCallCount` and change `b.usageCount - a.usageCount` to `b.callCount - a.callCount`.
- Line 109: Change `row.documentCount` to `row.docCount` in the table cell.

**OverviewCharts.tsx:**
- Line 123 (yAxis data): Change `d.displayName` to `d.userName` in the user TOP chart option builder.
- Lines 149-163 (flattenAuditRecords function): Complete rewrite. The current code treats `auditDocs()` as an array and accesses `doc.records` which doesn't exist. `fetchAuditByDocument` returns `PaginatedResponse<AuditDocumentRow>`. Fix:
  ```typescript
  function flattenAuditRecords(): AuditRecord[] {
    const response = auditDocs();
    const docs = response?.data ?? [];
    return docs
      .sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime())
      .slice(0, 10)
      .map((doc) => ({
        calledAt: doc.createdAt,
        displayName: doc.userName,
        workflowName: doc.workflowName,
        modelName: "-",
        totalTokens: doc.totalTokens,
        durationMs: doc.totalDuration,
        estimatedCost: doc.estimatedCost,
      }));
  }
  ```
  This extracts `response.data` (the array from PaginatedResponse), maps `AuditDocumentRow` fields to the `AuditRecord` display interface, and sorts by `createdAt` descending. The `modelName` is set to "-" since audit-by-document groups all model calls per document (individual model names are in the detail view).
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -n "docCount\|avgSuccessRate\|userName\|callCount\|\.data" packages/frontend/src/pages/admin/stats/KpiCards.tsx packages/frontend/src/pages/admin/stats/ModelStats.tsx packages/frontend/src/pages/admin/stats/UserStats.tsx packages/frontend/src/pages/admin/stats/WorkflowStats.tsx packages/frontend/src/pages/admin/stats/OverviewCharts.tsx</automated>
  </verify>
  <done>KpiCards uses docCount+avgSuccessRate keys; ModelStats displays successRate directly (no * 100); UserStats uses userName+docCount; WorkflowStats uses callCount+docCount; OverviewCharts flattenAuditRecords extracts PaginatedResponse.data and maps AuditDocumentRow fields correctly</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full chain:

1. `statistics.ts` OverviewData interface field names match `statistics.service.ts` getOverviewKpis return object
2. `statistics.ts` fetchByUser return type field names match `statistics.service.ts` getByUser select object
3. `statistics.ts` fetchByWorkflow return type field names match `statistics.service.ts` getByWorkflow select object
4. KpiCards card keys are valid members of the updated OverviewData interface
5. ModelStats renders `row.successRate.toFixed(1)` (no multiplication)
6. UserStats renders `row.userName` and `row.docCount`
7. WorkflowStats renders `row.callCount` and `row.docCount`
8. OverviewCharts flattenAuditRecords accesses `auditDocs()?.data` (PaginatedResponse.data)
9. `bun run check` (or TypeScript compilation) passes without type errors in modified files
</verification>

<success_criteria>
All 5 verification gaps from 19-VERIFICATION.md are resolved:
- KPI cards render correct values for docCount and avgSuccessRate
- Model stats success rate displays as 95.0% (not 9500%)
- User stats table shows correct user names and document counts
- Workflow stats chart and table show correct usage counts (not 0/undefined)
- Overview recent audit table renders actual records from the API
</success_criteria>

<output>
After completion, create `.planning/phases/19-statistics-audit-dashboard/19-06-SUMMARY.md`
</output>
