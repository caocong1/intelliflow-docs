---
phase: 29-xss-defense
plan: 03
type: execute
wave: 3
depends_on: ["29-01"]
files_modified:
  - packages/frontend/src/components/workspace/InlineEditor.tsx
autonomous: true
requirements:
  - XSS-04

must_haves:
  truths:
    - "InlineEditor.tsx imports sanitizeHtml from '../../lib/sanitize'"
    - "All innerHTML usages in InlineEditor.tsx are wrapped with sanitizeHtml()"
    - "The markdownToHtml() function output is sanitized before being assigned to innerHTML"
  artifacts:
    - path: "packages/frontend/src/components/workspace/InlineEditor.tsx"
      provides: "XSS-safe inline editor with sanitizeHtml on markdownToHtml output"
      contains: "import.*sanitizeHtml|innerHTML={sanitizeHtml"
---

<objective>
Wrap innerHTML usages in InlineEditor.tsx with sanitizeHtml() for defense-in-depth.

Purpose: InlineEditor renders user/AI-authored markdown content and also displays AI streaming output. Even though the primary rendering path is in render-markdown.tsx, defense-in-depth requires sanitizing at this layer too.
Output: `markdownToHtml(localContent())` calls in InlineEditor.tsx are wrapped with `sanitizeHtml()`.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/29-xss-defense/29-01-SUMMARY.md

@packages/frontend/src/components/workspace/InlineEditor.tsx
@packages/frontend/src/lib/sanitize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sanitizeHtml import to InlineEditor.tsx</name>
  <files>packages/frontend/src/components/workspace/InlineEditor.tsx</files>
  <read_first>packages/frontend/src/components/workspace/InlineEditor.tsx (lines 1-10)</read_first>
  <action>In `packages/frontend/src/components/workspace/InlineEditor.tsx`, add `import { sanitizeHtml } from "../../lib/sanitize";` after the existing imports (after the streamSSE import on line 4).</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "import.*sanitizeHtml" packages/frontend/src/components/workspace/InlineEditor.tsx</verify>
  <acceptance_criteria>
    - `sanitizeHtml` is imported from "../../lib/sanitize"
  </acceptance_criteria>
  <done>sanitizeHtml is available in InlineEditor.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap innerHTML in read-only preview with sanitizeHtml()</name>
  <files>packages/frontend/src/components/workspace/InlineEditor.tsx</files>
  <read_first>packages/frontend/src/components/workspace/InlineEditor.tsx (lines 281-290)</read_first>
  <action>In the read-only preview section of InlineEditor.tsx (around line 285), find `<div class="prose prose-sm max-w-none" innerHTML={markdownToHtml(localContent())} />` and change it to `<div class="prose prose-sm max-w-none" innerHTML={sanitizeHtml(markdownToHtml(localContent()))} />`. This is the read-only rendering path (props.readOnly === true).
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "innerHTML.*markdownToHtml" packages/frontend/src/components/workspace/InlineEditor.tsx</verify>
  <acceptance_criteria>
    - The read-only preview innerHTML uses `sanitizeHtml(markdownToHtml(...))`
  </acceptance_criteria>
  <done>Read-only preview innerHTML is sanitized.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap innerHTML in split-view preview with sanitizeHtml()</name>
  <files>packages/frontend/src/components/workspace/InlineEditor.tsx</files>
  <read_first>packages/frontend/src/components/workspace/InlineEditor.tsx (lines 444-450)</read_first>
  <action>In the split-view preview section (the `<Show when={viewMode() !== "edit"}>` block around line 446), find `<div class="prose prose-sm max-w-none" innerHTML={markdownToHtml(localContent())} />` and change it to `<div class="prose prose-sm max-w-none" innerHTML={sanitizeHtml(markdownToHtml(localContent()))} />`.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "innerHTML.*markdownToHtml" packages/frontend/src/components/workspace/InlineEditor.tsx</verify>
  <acceptance_criteria>
    - The split-view preview innerHTML uses `sanitizeHtml(markdownToHtml(...))`
    - No unsanitized innerHTML remains in InlineEditor.tsx
    - `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
  </acceptance_criteria>
  <done>Split-view preview innerHTML is sanitized.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
- `rg "innerHTML={(?!sanitizeHtml)" packages/frontend/src/components/workspace/InlineEditor.tsx` returns zero results
- Both read-only and split-view innerHTML usages call `sanitizeHtml()`
</verification>

<success_criteria>
- XSS-04 is partially satisfied: InlineEditor.tsx sanitizes all innerHTML
- Both the read-only and split-view preview modes use sanitizeHtml()
</success_criteria>

<output>
After completion, create `.planning/phases/29-xss-defense/29-03-SUMMARY.md`
</output>
