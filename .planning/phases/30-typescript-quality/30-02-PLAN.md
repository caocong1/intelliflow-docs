---
phase: 30-typescript-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types.ts
  - packages/backend/src/modules/documents/documents.service.ts
  - packages/frontend/src/lib/flow-engine/named-output-helpers.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "shared types DocumentStatus union includes 'failed' as a valid value"
    - "documents.service.ts listDocuments() filter accepts status='failed' as a valid parameter"
    - "InputSource.outputId has a JSDoc comment explaining it references the output's segmentKey"
    - "VariableRef.outputId has a JSDoc comment explaining it stores the segmentKey (not OutputDef.id)"
    - "validation.ts or named-output-helpers.ts contains inline comments on outputId comparison logic"
    - "`bunx tsc --noEmit` passes for both frontend and shared packages"
---

<objective>
Fix shared type contracts so they accurately reflect actual backend behavior. Two changes are needed: (1) DocumentStatus needs "failed" in the shared type (the backend already supports it), and (2) outputId semantics need clear documentation.

Purpose: The shared types are the contract between frontend and backend. When they are incomplete or ambiguous, developers make incorrect assumptions that cause runtime bugs. The outputId field especially is confusing — it can refer to either OutputDef.id or OutputDef.segmentKey, depending on context.
Output: DocumentStatus includes "failed", the backend accepts it in filters, and outputId semantics are documented.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/shared/src/types.ts
@packages/backend/src/modules/documents/documents.service.ts
@packages/frontend/src/lib/flow-engine/named-output-helpers.ts
@packages/backend/src/modules/runtime/runtime.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "failed" to DocumentStatus in shared types</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>packages/shared/src/types.ts</read_first>
  <action>Find the `DocumentStatus` type definition in `packages/shared/src/types.ts` (around line 309) and add "failed" as a valid value.

Current: `export type DocumentStatus = "draft" | "in_progress" | "completed";`
Change to: `export type DocumentStatus = "draft" | "in_progress" | "completed" | "failed";`
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "DocumentStatus" packages/shared/src/types.ts && bunx tsc --noEmit --project packages/shared/tsconfig.json 2>&1 | head -10</verify>
  <acceptance_criteria>
    - DocumentStatus type includes "failed" as a valid value
    - TypeScript compilation passes for the shared package
  </acceptance_criteria>
  <done>DocumentStatus includes "failed" in shared types.</done>
</task>

<task type="auto">
  <name>Task 2: Update backend documents.service.ts to accept status=failed</name>
  <files>packages/backend/src/modules/documents/documents.service.ts</files>
  <read_first>packages/backend/src/modules/documents/documents.service.ts</read_first>
  <action>Find the status filter in the `listDocuments` function in `packages/backend/src/modules/documents/documents.service.ts`. The filter currently checks `["draft", "in_progress", "completed"].includes(params.status)`. Update it to also include "failed".

Change the array from `["draft", "in_progress", "completed"]` to `["draft", "in_progress", "completed", "failed"]` in the status filter condition.

Also update the DocumentRow type alias inside the file (line 21) to include "failed" in the status union if it is a local type alias (check if it's defined locally — it uses `status: "draft" | "in_progress" | "completed" | "failed"` already at line 21, which means the backend type is correct). Only update the filter array if needed.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "failed" packages/backend/src/modules/documents/documents.service.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -10</verify>
  <acceptance_criteria>
    - The status filter array includes "failed"
    - The local DocumentRow type includes "failed" (it already does at line 21)
    - Backend TypeScript compilation passes
  </acceptance_criteria>
  <done>Backend document list API accepts status=failed as a filter parameter.</done>
</task>

<task type="auto">
  <name>Task 3: Add JSDoc comments to InputSource.outputId and VariableRef.outputId</name>
  <files>packages/shared/src/types.ts</files>
  <read_first>packages/shared/src/types.ts</read_first>
  <action>Add JSDoc comments to the `outputId` fields in both `InputSource` and `VariableRef` interfaces in `packages/shared/src/types.ts`.

For `InputSource` (around line 106-110):
Add JSDoc to the `outputId` field explaining it references the upstream node's OutputDef.segmentKey (the canonical path identifier for variable resolution), not OutputDef.id.

For `VariableRef` (around line 113-118):
The JSDoc on `outputId` should explain it stores the segmentKey of the referenced output (not OutputDef.id). The comment at line 115 already says `// stores segmentKey (not OutputDef.id)` — if it's already there, just ensure the JSDoc on the field makes this unambiguous.

Add JSDoc to `InputSource.outputId`:
```typescript
/** References the upstream node's OutputDef.segmentKey (canonical path identifier),
 * not OutputDef.id. Used by desensitize/restore nodes to declare input dependencies. */
outputId: string;
```

Add JSDoc to `VariableRef.outputId`:
```typescript
/** Stores OutputDef.segmentKey (not OutputDef.id).
 * Used in prompt templates for variable substitution (e.g., ${nodeId.outputId.fieldPath}). */
outputId: string;
```
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "@type|segmentKey" packages/shared/src/types.ts && bunx tsc --noEmit --project packages/shared/tsconfig.json 2>&1 | head -10</verify>
  <acceptance_criteria>
    - InputSource.outputId has a JSDoc explaining segmentKey semantics
    - VariableRef.outputId has a JSDoc explaining it stores segmentKey, not OutputDef.id
    - Shared package TypeScript compilation passes
  </acceptance_criteria>
  <done>InputSource.outputId and VariableRef.outputId have JSDoc comments explaining their semantics.</done>
</task>

<task type="auto">
  <name>Task 4: Add inline comments on outputId comparison logic in validation or helpers</name>
  <files>packages/frontend/src/lib/flow-engine/named-output-helpers.ts</files>
  <read_first>packages/frontend/src/lib/flow-engine/named-output-helpers.ts</read_first>
  <action>Add inline comments to `packages/frontend/src/lib/flow-engine/named-output-helpers.ts` (or a new `validation.ts` if needed) explaining the outputId comparison logic used when matching VariableRef.outputId to OutputDef fields.

If `named-output-helpers.ts` is the primary place where outputId comparisons happen, add a JSDoc comment block at the top of the file explaining:
1. `OutputDef.id` — the stable output slot identifier, assigned at node config time
2. `OutputDef.segmentKey` — the canonical path identifier for variable resolution, used in VariableRef.outputId
3. When comparing, use segmentKey (via OutputDef.segmentKey or OutputDef.id when segmentKey is absent) to match VariableRef.outputId

If named-output-helpers.ts doesn't contain comparison logic, create a minimal validation.ts file at `packages/frontend/src/lib/flow-engine/validation.ts` with a single documented function:
```typescript
/**
 * Resolve the canonical segmentKey for an OutputDef.
 * 
 * Comparison rules:
 * - Prefer OutputDef.segmentKey if present (Phase 24+ canonical identifier)
 * - Fall back to OutputDef.id when segmentKey is absent (backward compatibility)
 * 
 * This matters because VariableRef.outputId always stores the segmentKey,
 * so comparison must use the same resolution strategy on both sides.
 */
export function resolveOutputSegmentKey(outputDef: { id: string; segmentKey?: string }): string {
  return outputDef.segmentKey ?? outputDef.id;
}
```
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "segmentKey|outputId" packages/frontend/src/lib/flow-engine/ && bunx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -10</verify>
  <acceptance_criteria>
    - validation.ts or named-output-helpers.ts contains inline comments on outputId comparison logic
    - Frontend TypeScript compilation passes
  </acceptance_criteria>
  <done>validation.ts or named-output-helpers.ts contains inline comments on outputId comparison logic.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/shared/tsconfig.json` passes
- `bunx tsc --noEmit --project packages/backend/tsconfig.json` passes
- `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
- DocumentStatus includes "failed" in shared types
- Backend filter accepts status="failed"
- InputSource.outputId and VariableRef.outputId have JSDoc comments
- outputId comparison logic is documented in validation or helpers
</verification>

<success_criteria>
- All 4 Contract Fix requirements are satisfied (CONT-01, CONT-02, CONT-03, CONT-04)
- The DocumentStatus type includes "failed" and the backend document list API accepts status=failed as a filter parameter
- InputSource.outputId and VariableRef.outputId have JSDoc comments explaining their semantics
- validation.ts contains inline comments on outputId comparison logic
</success_criteria>

<output>
After completion, create `.planning/phases/30-typescript-quality/30-02-SUMMARY.md`
</output>
