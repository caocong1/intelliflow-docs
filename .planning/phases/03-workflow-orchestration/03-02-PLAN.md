---
phase: 03-workflow-orchestration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/frontend/src/pages/admin/WorkflowManagement.tsx
  - packages/frontend/src/components/nav/Sidebar.tsx
  - packages/frontend/src/App.tsx
autonomous: true
requirements: [FLOW-01, FLOW-11, FLOW-12]

must_haves:
  truths:
    - "Admin can see a table of workflows with name, document type, status, default badge, and actions"
    - "Admin can create a workflow by selecting a document type, entering name and description"
    - "Admin can enable/disable, delete, and copy workflows from the action menu"
    - "Admin can filter workflows by document type dropdown and search by name"
    - "Admin can set a workflow as default for its document type"
    - "Copy workflow supports choosing a different target document type"
  artifacts:
    - path: "packages/frontend/src/pages/admin/WorkflowManagement.tsx"
      provides: "Workflow management list page with table CRUD"
      min_lines: 200
    - path: "packages/frontend/src/components/nav/Sidebar.tsx"
      provides: "Sidebar with workflow management nav entry"
      contains: "流程管理"
  key_links:
    - from: "packages/frontend/src/pages/admin/WorkflowManagement.tsx"
      to: "/api/workflows"
      via: "Eden Treaty API calls"
      pattern: "api\\.workflows"
    - from: "packages/frontend/src/App.tsx"
      to: "WorkflowManagement"
      via: "Route registration"
      pattern: "WorkflowManagement"
---

<objective>
Build the workflow management list page following the existing admin table pattern (UserManagement, DocumentTypeManagement). Provides CRUD operations for workflows via a table interface.

Purpose: Admins need to create, browse, filter, and manage workflows before designing them in the editor. This page is the entry point for all workflow operations.
Output: WorkflowManagement.tsx with table, modals, filters, and all management actions.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-orchestration/03-CONTEXT.md
@.planning/phases/03-workflow-orchestration/03-01-SUMMARY.md

<interfaces>
<!-- From 03-01: Backend API endpoints -->
GET    /api/workflows?documentTypeId=&search=&page=&pageSize=  → { data: WorkflowListItem[], total: number }
POST   /api/workflows         { documentTypeId, name, description }   → Workflow
PUT    /api/workflows/:id     { name?, description? }                 → Workflow
DELETE /api/workflows/:id                                             → { success: true }
POST   /api/workflows/:id/copy   { name, targetDocumentTypeId? }     → Workflow
PATCH  /api/workflows/:id/status { status: "active"|"disabled"|"draft" } → Workflow
PATCH  /api/workflows/:id/set-default                                → Workflow

<!-- From shared types (03-01) -->
WorkflowListItem: { id, documentTypeId, documentTypeName, name, description, status, isDefault, nodeCount, createdAt, updatedAt }
WorkflowStatus: "draft" | "active" | "disabled"

<!-- Existing reusable components -->
Table, Modal, Toast, Badge, Pagination, SearchInput from packages/frontend/src/components/ui/
Sidebar pattern: admin-only menu items with Show when={auth.isAdmin}
Route pattern: lazy(() => import("...")) in App.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workflow management list page with table CRUD</name>
  <files>packages/frontend/src/pages/admin/WorkflowManagement.tsx</files>
  <action>
Create WorkflowManagement.tsx following the exact pattern of DocumentTypeManagement.tsx and UserManagement.tsx:

1. **Data fetching**: Use Eden Treaty to call `GET /api/workflows` with pagination, search, and documentTypeId filter. Also fetch `GET /api/document-types` for the filter dropdown options.

2. **Table columns**: 流程名称, 文档类型 (documentTypeName), 状态 (Badge: 草稿/yellow, 启用/green, 停用/gray), 默认 (Badge "默认" if isDefault), 节点数 (nodeCount), 创建时间, 操作.

3. **Filters above table**:
   - Document type dropdown (select): "全部文档类型" default + all active document types
   - SearchInput for keyword search by workflow name
   - "新建流程" primary button (right-aligned)

4. **Create modal**: Form with document type select (required), name input (required), description textarea (optional). Calls `POST /api/workflows`. On success, toast + refresh list.

5. **Action menu per row** (dropdown or inline buttons):
   - "编辑" — opens editor page (navigate to `/admin/workflows/${id}/edit`, page created in Plan 03-03)
   - "启用"/"停用" — calls `PATCH /:id/status` with "active" or "disabled". If enable fails (validation), show error toast with message.
   - "设为默认" — calls `PATCH /:id/set-default`. Only shown for active workflows. Refresh list after.
   - "复制" — opens copy modal
   - "删除" — confirm dialog, calls `DELETE /:id`

6. **Copy modal**: Name input (pre-filled with "副本 - {original name}"), document type select (pre-filled with current, can change for cross-type copy). Calls `POST /:id/copy`.

7. **Pagination**: Reuse existing Pagination component. Default pageSize 10.

All user-facing text in Chinese. Follow existing indigo theme and Tailwind styling patterns.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter frontend tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>WorkflowManagement page renders table with all columns, filters by document type and search, supports create/copy/delete/toggle-status/set-default actions via modals and API calls.</done>
</task>

<task type="auto">
  <name>Task 2: Route registration and sidebar navigation</name>
  <files>packages/frontend/src/components/nav/Sidebar.tsx, packages/frontend/src/App.tsx</files>
  <action>
1. In Sidebar.tsx, add "流程管理" nav item in the admin section (after "AI 模型配置" or after "文档类型管理"). Use a workflow/flow icon (e.g., a branching/nodes icon SVG). Link to `/admin/workflows`.

2. In App.tsx:
   - Add lazy import: `const WorkflowManagement = lazy(() => import("./pages/admin/WorkflowManagement"));`
   - Add route inside AdminRoute: `<Route path="/admin/workflows" component={WorkflowManagement} />`
   - Also add placeholder route for editor: `<Route path="/admin/workflows/:id/edit" component={WorkflowEditor} />` with a simple placeholder component (or lazy import that will be created in 03-03).

Follow the exact pattern used for UserManagement and ModelConfiguration routes.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bun run --filter frontend tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Sidebar shows "流程管理" for admin users. Route registered at /admin/workflows. Editor route placeholder at /admin/workflows/:id/edit.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- WorkflowManagement page imported and routed
- Sidebar has workflow management entry visible to admin
- Table displays workflow list items with all specified columns
- Create, copy, delete, toggle-status, set-default actions call correct API endpoints
</verification>

<success_criteria>
- Admin can navigate to workflow management via sidebar
- Table shows workflows with document type filter and search
- All CRUD actions (create, copy, delete, enable/disable, set default) work via modals/actions
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-orchestration/03-02-SUMMARY.md`
</output>
