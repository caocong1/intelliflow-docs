---
phase: 28-file-security
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - packages/backend/src/modules/runtime/input-transform.service.ts
autonomous: true
requirements:
  - FSEC-06

must_haves:
  truths:
    - "handleFileUpload uses sanitizeFilename(file.name) when constructing the on-disk path"
    - "confirmInputTransform uses sanitizeFilename for any filename derived from user input before writing to disk"
    - "No raw file.name (or derived value) from user input is passed to join() without sanitizeFilename"
  artifacts:
    - path: "packages/backend/src/modules/runtime/input-transform.service.ts"
      provides: "path-traversal-safe file writes for input-transform node uploads"
      contains: "sanitizeFilename"
  key_links:
    - from: "input-transform.service.ts::handleFileUpload"
      to: "sanitize.ts::sanitizeFilename"
      via: "import and apply to file.name"
      pattern: "import.*sanitizeFilename"
---

<objective>
Sanitize all user-supplied filenames in the input-transform service before writing to disk.

Purpose: Prevent path traversal via malicious filenames during file upload and confirm phases.
Output: `handleFileUpload` and `confirmInputTransform` use `sanitizeFilename()` on every user-supplied filename.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/28-file-security/28-01-SUMMARY.md

@packages/backend/src/modules/runtime/input-transform.service.ts
@packages/backend/src/common/sanitize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sanitize filename in handleFileUpload</name>
  <files>packages/backend/src/modules/runtime/input-transform.service.ts</files>
  <read_first>packages/backend/src/modules/runtime/input-transform.service.ts (lines 82-135), packages/backend/src/common/sanitize.ts</read_first>
  <action>In `packages/backend/src/modules/runtime/input-transform.service.ts`, add `import { sanitizeFilename } from "../../common/sanitize";` at the top (after the existing imports). In `handleFileUpload`, change the line `const filePath = join(uploadDir, file.name);` to `const filePath = join(uploadDir, sanitizeFilename(file.name));`. Then update the `originalName: file.name` field in the `insertDocumentFile` call to `originalName: file.name` (keep originalName unchanged — it is the display name stored in DB, not used for disk I/O). The `record.originalName` in the return value also stays as `file.name`. Only the disk path uses the sanitized name. Add a comment `// sanitize to prevent path traversal` above the filePath assignment.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "sanitizeFilename|uploadDir, file.name" packages/backend/src/modules/runtime/input-transform.service.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -20</verify>
  <acceptance_criteria>
    - `sanitizeFilename` is imported in input-transform.service.ts
    - `handleFileUpload` calls `join(uploadDir, sanitizeFilename(file.name))`
    - `originalName` in insertDocumentFile and return value remain as `file.name` (unsanitized display name)
    - No un-sanitized user filename reaches a filesystem call
  </acceptance_criteria>
  <done>handleFileUpload no longer writes files using raw user-supplied filenames.</done>
</task>

<task type="auto">
  <name>Task 2: Sanitize filename in confirmInputTransform output.txt path</name>
  <files>packages/backend/src/modules/runtime/input-transform.service.ts</files>
  <read_first>packages/backend/src/modules/runtime/input-transform.service.ts (lines 265-280), packages/backend/src/common/sanitize.ts</read_first>
  <action>In `confirmInputTransform` (around line 271-273), find where `writeFile(join(outputDir, "output.txt"), combinedText, "utf-8")` is called. The filename "output.txt" is a server-controlled constant and does not need sanitization. However, add a comment `# safely written: "output.txt" is a server-controlled constant` above the writeFile call to document why it is safe. This confirms that all user-controlled filenames in this function have been reviewed.

Also check: in `buildFileSlots`, if any `file.name` is used in a string that could become a path key, it is used only as a DB value (not a filesystem path), so no change is needed there. Add a comment confirming this above the `slots[slotId].files.push(...)` line.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "output.txt|writeFile.*outputDir" packages/backend/src/modules/runtime/input-transform.service.ts && bunx tsc --noEmit --project packages/backend/tsconfig.json 2>&1 | head -20</verify>
  <acceptance_criteria>
    - A comment documents that "output.txt" is a server-controlled constant
    - buildFileSlots uses file.name only in DB value context (not filesystem)
    - All filesystem paths in the module are safe
  </acceptance_criteria>
  <done>confirmInputTransform has been reviewed and all filesystem writes are safe.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/backend/tsconfig.json` passes
- `sanitizeFilename` is imported and applied to the file.name in handleFileUpload
- `output.txt` is documented as a server-controlled constant
- No un-sanitized user input reaches a filesystem API
</verification>

<success_criteria>
- A malicious upload filename like "../../../etc/passwd" is sanitized to "etc_passwd" on disk
- The originalName in the DB and the display name remain the user's original filename
- The service compiles and exports unchanged APIs
</success_criteria>

<output>
After completion, create `.planning/phases/28-file-security/28-03-SUMMARY.md`
</output>
