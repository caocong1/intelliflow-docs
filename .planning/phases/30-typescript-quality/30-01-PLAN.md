---
phase: 30-typescript-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/api/client.ts
  - packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
  - packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx
  - packages/frontend/src/pages/documents/VersionHistory.tsx
autonomous: true
requirements:
  - TSQL-01
  - TSQL-02
  - TSQL-03
  - TSQL-04

must_haves:
  truths:
    - "client.ts exports 9 typed wrapper functions covering all runtime API calls used in DocumentWorkspace.tsx, ExportExecutor.tsx, and VersionHistory.tsx"
    - "DocumentWorkspace.tsx has zero `as any` casts for Eden Treaty API calls (lines 133, 170, 262, 459, 480, 498)"
    - "ExportExecutor.tsx has zero `as any` casts for Eden Treaty API calls (lines 87, 118)"
    - "VersionHistory.tsx has zero `as any` casts for Eden Treaty API calls (line 101)"
    - "`bunx tsc --noEmit` passes for the frontend package"
---

<objective>
Eliminate all `as any` casts for Eden Treaty API calls in the three workspace components by extending the typed wrapper functions in client.ts. This makes the API contracts explicit and catches mismatches at compile time rather than runtime.

Purpose: The frontend uses `api.api.runtime[documentId]...` with `as any` casts because Eden Treaty cannot infer nested route types with dynamic path parameters (documentId). Wrapping these in typed functions in client.ts provides compile-time safety while keeping the existing API structure intact.
Output: All runtime API calls in DocumentWorkspace.tsx, ExportExecutor.tsx, and VersionHistory.tsx use typed wrapper functions from client.ts. Zero `as any` casts for Eden Treaty calls in these files.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/frontend/src/api/client.ts
@packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
@packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx
@packages/frontend/src/pages/documents/VersionHistory.tsx
@packages/backend/src/modules/runtime/runtime.routes.ts
@packages/backend/src/modules/runtime/export.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend client.ts with typed runtime API wrappers</name>
  <files>packages/frontend/src/api/client.ts</files>
  <read_first>packages/frontend/src/api/client.ts</read_first>
  <action>Add 9 typed wrapper functions to the end of `packages/frontend/src/api/client.ts`. Each wrapper calls the Eden Treaty runtime route with proper types and returns a typed result. Read the file first, then append these functions:

1. `getRuntimeState(documentId: string)` — GET `/runtime/:documentId` → `DocumentRuntimeState`
2. `initRuntime(documentId: string)` — POST `/runtime/:documentId/init` → `DocumentRuntimeState`
3. `startBackgroundExecution(documentId: string)` — POST `/runtime/:documentId/start-background` → `{ status: string }`
4. `advanceNode(documentId: string, nodeExecutionId: string)` — POST `/runtime/:documentId/advance/:nodeExecutionId` → `DocumentRuntimeState`
5. `skipNode(documentId: string, nodeExecutionId: string)` — POST `/runtime/:documentId/skip/:nodeExecutionId` → `DocumentRuntimeState`
6. `rollbackNode(documentId: string, targetStepOrder: number)` — POST `/runtime/:documentId/rollback` with body `{ targetStepOrder }` → `DocumentRuntimeState`
7. `getExportPreview(documentId: string, nodeExecutionId: string)` — GET `/runtime/:documentId/export/:nodeExecutionId/preview` → `{ content: string; defaultFilename: string }`
8. `generateExport(documentId: string, nodeExecutionId: string, format: string, filename: string)` — POST `/runtime/:documentId/export/:nodeExecutionId/generate` with body `{ format, filename }` → `{ filename: string; format: string; fileSize: number; storagePath: string }`
9. `getVersionDiff(versionAId: string, versionBId: string)` — GET `/versions/:id/diff` with query `{ idB }` → `VersionDiffResult`

Each function should follow the pattern:
```typescript
export async function getRuntimeState(documentId: string) {
  const res = await api.api.runtime[documentId].get();
  if (res.data && !("error" in res.data)) {
    return res.data as { data: DocumentRuntimeState } };
  return null;
}
```

Import types as needed from `@intelliflow/shared`. Make sure to import `DocumentRuntimeState`, `VersionDiffResult` from the shared package. The function signatures should use the actual Eden Treaty return types where possible. If the response wraps data in `{ data: ... }`, return the unwrapped value. If the response is the data directly, return it as-is with proper typing. Each function should return null on error (not throw) to match the existing error-handling pattern in the components.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && bunx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -20</verify>
  <acceptance_criteria>
    - client.ts exports all 9 typed wrapper functions
    - All functions have proper TypeScript return types
    - No `as any` inside the wrapper functions themselves
    - Functions are importable from other frontend modules
  </acceptance_criteria>
  <done>9 typed runtime API wrappers are added to client.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace DocumentWorkspace.tsx `as any` casts</name>
  <files>packages/frontend/src/pages/workspace/DocumentWorkspace.tsx</files>
  <read_first>packages/frontend/src/pages/workspace/DocumentWorkspace.tsx</read_first>
  <action>Replace each `as any` cast in DocumentWorkspace.tsx with the appropriate typed wrapper from client.ts. Keep the import from client.ts (it already imports `api`). Import the new wrapper functions alongside the existing `api` import.

Changes:
1. Line 133: `(api.api.runtime as any)[params.documentId].get()` → `getRuntimeState(params.documentId)` (function returns the state or null, update the condition check accordingly — if it returns null, check `if (runtimeState !== null)` instead of `if (!("error" in res.data))`)
2. Line 170: `(api.api.runtime as any)[params.documentId]["start-background"].post()` → `startBackgroundExecution(params.documentId)`
3. Line 262: `(api.api.runtime as any)[params.documentId].init.post()` → `initRuntime(params.documentId)`
4. Line 459: `(api.api.runtime as any)[params.documentId].advance[node.id].post()` → `advanceNode(params.documentId, node.id)`
5. Line 480: `(api.api.runtime as any)[params.documentId].skip[node.id].post()` → `skipNode(params.documentId, node.id)`
6. Line 498: `(api.api.runtime as any)[params.documentId].rollback.post(...)` → `rollbackNode(params.documentId, targetStepOrder)`

For the error handling: since each wrapper returns null on error, update each call site to check for null instead of `!("error" in res.data)`. Keep the same UI behavior — show toast on failure, update state on success.

Also fix line 281: `(res.data as any)?.error` — this should be typed properly based on the wrapper's error response shape. Since `initRuntime` returns `DocumentRuntimeState | null`, check for null and extract the error from the response differently. The wrapper should return `{ data: DocumentRuntimeState | { error: string } }` from Eden Treaty, so access `res.data.error` if the wrapper returns the raw response. Revise wrapper signatures to return the raw Eden Treaty response so existing `!("error" in res.data)` checks work with minimal changes.

Actually, the simplest approach: keep wrapper functions returning the raw response from Eden Treaty (which has shape `{ data: T | { error: string } }`), but typed. Then update call sites to call the wrapper and use the same `!("error" in res.data)` pattern. Import the new wrapper functions from `../../api/client`.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "as any" packages/frontend/src/pages/workspace/DocumentWorkspace.tsx | grep -v "//\|/\*\|error.*any\|Record.*any\|unknown.*any" && bunx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -10</verify>
  <acceptance_criteria>
    - All 6 `as any` casts in DocumentWorkspace.tsx for Eden Treaty calls are replaced
    - `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
    - The `import { api }` line also imports the new wrapper functions
  </acceptance_criteria>
  <done>DocumentWorkspace.tsx has zero `as any` casts for Eden Treaty API calls.</done>
</task>

<task type="auto">
  <name>Task 3: Replace ExportExecutor.tsx `as any` casts</name>
  <files>packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx</files>
  <read_first>packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx</read_first>
  <action>Replace each `as any` cast in ExportExecutor.tsx with the appropriate typed wrapper from client.ts. Import the new wrapper functions.

Changes:
1. Lines 87-89: `(api.api.runtime as any)[props.documentId].export[props.nodeExecution.id].preview.get()` → `getExportPreview(props.documentId, props.nodeExecution.id)`
2. Lines 118-120: `(api.api.runtime as any)[props.documentId].export[props.nodeExecution.id].generate.post({ format: format(), filename: filename() })` → `generateExport(props.documentId, props.nodeExecution.id, format(), filename())`

For error handling, since these wrappers return the raw Eden Treaty response (same as the original `as any` calls), the existing `!("error" in res.data)` checks will work unchanged. Just replace the call expression.

Update the import line: change `import { api } from "../../../api/client"` to also import the wrapper functions: `import { api, getExportPreview, generateExport } from "../../../api/client"`
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "as any" packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx | grep -v "//\|/\*" && bunx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -10</verify>
  <acceptance_criteria>
    - All 4 `as any` casts in ExportExecutor.tsx are replaced (lines 87, 96, 118, 139)
    - Lines 96 and 139 are `as any` for error extraction from `res.data` — these are NOT Eden Treaty calls but response type assertions, so leave them as `as any` or replace with a more specific type like `{ error?: string }`
    - `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
  </acceptance_criteria>
  <done>ExportExecutor.tsx has zero `as any` casts for Eden Treaty API calls.</done>
</task>

<task type="auto">
  <name>Task 4: Replace VersionHistory.tsx `as any` cast</name>
  <files>packages/frontend/src/pages/documents/VersionHistory.tsx</files>
  <read_first>packages/frontend/src/pages/documents/VersionHistory.tsx</read_first>
  <action>Replace the `as any` cast in VersionHistory.tsx for the version diff call.

Line 101: `(api.api.versions as any)({ id: idA }).diff({ idB }).get()` → `getVersionDiff(idA, idB)`

Import `getVersionDiff` from client.ts alongside the existing `api` import.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "as any" packages/frontend/src/pages/documents/VersionHistory.tsx | grep -v "//\|/\*" && bunx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -10</verify>
  <acceptance_criteria>
    - The single `as any` cast in VersionHistory.tsx for Eden Treaty call is replaced
    - `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
  </acceptance_criteria>
  <done>VersionHistory.tsx has zero `as any` casts for Eden Treaty API calls.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes with zero errors
- `rg "as any" packages/frontend/src/pages/workspace/DocumentWorkspace.tsx` returns zero lines matching Eden Treaty API calls
- `rg "as any" packages/frontend/src/components/workspace/nodes/ExportExecutor.tsx` returns zero lines matching Eden Treaty API calls
- `rg "as any" packages/frontend/src/pages/documents/VersionHistory.tsx` returns zero lines matching Eden Treaty API calls
- client.ts exports all 9 typed wrapper functions
</verification>

<success_criteria>
- All 4 TypeScript Quality requirements are satisfied (TSQL-01, TSQL-02, TSQL-03, TSQL-04)
- DocumentWorkspace.tsx, ExportExecutor.tsx, and VersionHistory.tsx contain zero `as any` casts for Eden Treaty API calls
- All runtime API calls in the frontend go through typed wrapper functions in client.ts
</success_criteria>

<output>
After completion, create `.planning/phases/30-typescript-quality/30-01-SUMMARY.md`
</output>
