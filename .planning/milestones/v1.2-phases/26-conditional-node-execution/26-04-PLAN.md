---
phase: 26-conditional-node-execution
plan: 04
type: execute
wave: 1
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
autonomous: true
requirements:
  - COND-06

must_haves:
  truths:
    - "Rollback button triggers confirmation dialog then DocumentWorkspace computes targetNodeId from workflowNodes config and calls rollbackToNode"
  artifacts:
    - path: "packages/frontend/src/pages/workspace/DocumentWorkspace.tsx"
      provides: "handleBlockedRollback derives stepOrder from s.nodes and sends { targetStepOrder } to rollback API"
  key_links:
    - from: "DocumentWorkspace.tsx::handleBlockedRollback"
      to: "runtime rollback API"
      via: "sends { targetStepOrder: number } (looked up from s.nodes), not { targetNodeId }"
      pattern: "targetStepOrder"
    - from: "DocumentWorkspace.tsx::handleBlockedRollback"
      to: "s.nodes NodeExecution records"
      via: "finds stepOrder by nodeId lookup in s.nodes"
      pattern: "s.nodes.find"
---

<objective>
Fix handleBlockedRollback in DocumentWorkspace.tsx to send the correct API body. The function was implemented in Plan 03 but sent { targetNodeId } instead of { targetStepOrder }, and stepOrder was never derived from execution records.
</objective>

<tasks>

<task type="auto">
  <name>Fix handleBlockedRollback: derive stepOrder from s.nodes, send { targetStepOrder } to API</name>
  <files>
    packages/frontend/src/pages/workspace/DocumentWorkspace.tsx
  </files>
  <action>
In `packages/frontend/src/pages/workspace/DocumentWorkspace.tsx`, inside `handleBlockedRollback` (lines 335-363):

1. After line 352 (`const targetNodeId = sourceExecs[0]?.nodeId;`), add a lookup to derive stepOrder from the current node execution records:
```typescript
const targetExec = s.nodes.find((n) => n.nodeId === targetNodeId);
const targetStepOrder = targetExec?.stepOrder;
if (!targetStepOrder) return;
```

2. Change the fetch body at line 359 from `{ targetNodeId }` to `{ targetStepOrder }`:
```typescript
body: JSON.stringify({ targetStepOrder }),
```

No other changes needed. BlockedNodeCard, StepperBar, NodeHistoryPanel, and the confirmation dialog are all already wired correctly from Plan 03.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && bunx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>handleBlockedRollback looks up stepOrder from s.nodes via nodeId, guards on missing stepOrder, and sends { targetStepOrder } to the rollback API</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` passes for frontend package
- handleBlockedRollback sends `{ targetStepOrder }` in fetch body (not `{ targetNodeId }`)
- stepOrder is derived from `s.nodes.find(n => n.nodeId === targetNodeId)?.stepOrder`, not from workflowNodes
</verification>

<success_criteria>
- Rollback button on a blocked node sends `{ targetStepOrder: number }` to `/api/runtime/:documentId/rollback`
- stepOrder is looked up from current NodeExecution records (s.nodes) by nodeId
- Confirmation dialog still fires before the API call (unchanged from Plan 03)
</success_criteria>
