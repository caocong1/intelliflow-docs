---
phase: 29-xss-defense
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - packages/frontend/src/lib/render-markdown.tsx
autonomous: true
requirements:
  - XSS-03

must_haves:
  truths:
    - "All 6 innerHTML assignments in render-markdown.tsx are wrapped with sanitizeHtml()"
    - "The sanitizeHtml import is added at the top of render-markdown.tsx"
    - "renderMarkdown() still renders markdown correctly (bold, italic, code, headers, lists)"
  artifacts:
    - path: "packages/frontend/src/lib/render-markdown.tsx"
      provides: "XSS-safe markdown rendering using sanitizeHtml() on all innerHTML"
      contains: "import.*sanitizeHtml|innerHTML={sanitizeHtml"
  key_links:
    - from: "render-markdown.tsx::inlineFormat calls"
      to: "sanitize.ts::sanitizeHtml"
      via: "import and wrap"
      pattern: "innerHTML={sanitizeHtml"
---

<objective>
Wrap all 6 innerHTML assignments in render-markdown.tsx with sanitizeHtml().

Purpose: render-markdown.tsx is the primary channel for displaying AI-generated content. Malicious markdown input (e.g., `<script>`, `<img onerror>`, `javascript:` links) must be neutralized before DOM insertion. This is the CRITICAL path for Phase 29.
Output: Every `innerHTML={...}` and `innerHTML={...}` in render-markdown.tsx uses `sanitizeHtml(...)` as the outer wrapper.
</objective>

<execution_context>
@/Users/dongli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/29-xss-defense/29-01-SUMMARY.md

@packages/frontend/src/lib/render-markdown.tsx
@packages/frontend/src/lib/sanitize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sanitizeHtml import to render-markdown.tsx</name>
  <files>packages/frontend/src/lib/render-markdown.tsx</files>
  <read_first>packages/frontend/src/lib/render-markdown.tsx (lines 1-60), packages/frontend/src/lib/sanitize.ts</read_first>
  <action>In `packages/frontend/src/lib/render-markdown.tsx`, add `import { sanitizeHtml } from "./sanitize";` as a new import line after the existing solid-js imports (line 1).</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "import.*sanitizeHtml" packages/frontend/src/lib/render-markdown.tsx</verify>
  <acceptance_criteria>
    - `sanitizeHtml` is imported from "./sanitize"
    - The import is at the top of the file, before any component code
  </acceptance_criteria>
  <done>sanitizeHtml is available in render-markdown.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap all 6 innerHTML assignments with sanitizeHtml()</name>
  <files>packages/frontend/src/lib/render-markdown.tsx</files>
  <read_first>packages/frontend/src/lib/render-markdown.tsx (lines 45-96)</read_first>
  <action>In `packages/frontend/src/lib/render-markdown.tsx`, find all 6 innerHTML JSX attributes and wrap their values with `sanitizeHtml()`. The 6 locations are:
1. Line ~53: `innerHTML={inlineFormat(el.content)}` in the `<p>` fallback — change to `innerHTML={sanitizeHtml(inlineFormat(el.content))}`
2. Line ~61: `innerHTML={inlineFormat(el.content)}` in `<h1>` — change to `innerHTML={sanitizeHtml(inlineFormat(el.content))}`
3. Line ~68: `innerHTML={inlineFormat(el.content)}` in `<h2>` — change to `innerHTML={sanitizeHtml(inlineFormat(el.content))}`
4. Line ~75: `innerHTML={inlineFormat(el.content)}` in `<h3>` — change to `innerHTML={sanitizeHtml(inlineFormat(el.content))}`
5. Line ~81: `innerHTML={inlineFormat(el.content)}` in the `<span>` for `<li>` items — change to `innerHTML={sanitizeHtml(inlineFormat(el.content))}`
6. Line ~86: `innerHTML={inlineFormat(el.content)}` in the `<span>` for `<oli>` items — change to `innerHTML={sanitizeHtml(inlineFormat(el.content))}`

Note: The `<p>` fallback also has `class="..."` on the same element — preserve the class attribute and add `innerHTML={sanitizeHtml(inlineFormat(el.content))}`. No other changes to element attributes or structure are needed.
</action>
  <verify>cd /Users/dongli/Workspace/intelliflow-docs && rg -n "innerHTML" packages/frontend/src/lib/render-markdown.tsx</verify>
  <acceptance_criteria>
    - All 6 `innerHTML` attributes in render-markdown.tsx use `sanitizeHtml(...)` as the outermost wrapper
    - No `innerHTML={...}` without `sanitizeHtml` wrapper remains in the file
    - `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
  </acceptance_criteria>
  <done>All 6 innerHTML assignments in render-markdown.tsx are sanitized.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit --project packages/frontend/tsconfig.json` passes
- `rg "innerHTML={(?!sanitizeHtml)" packages/frontend/src/lib/render-markdown.tsx` returns zero results
- All `innerHTML` values call `sanitizeHtml()`
</verification>

<success_criteria>
- XSS-03 is fully satisfied: render-markdown.tsx sanitizes all innerHTML
- Markdown content renders correctly (bold, italic, headers, lists, inline code)
- `<script>alert(1)</script>`, `<img onerror>`, and `javascript:` links in markdown are stripped before DOM insertion
</success_criteria>

<output>
After completion, create `.planning/phases/29-xss-defense/29-02-SUMMARY.md`
</output>
