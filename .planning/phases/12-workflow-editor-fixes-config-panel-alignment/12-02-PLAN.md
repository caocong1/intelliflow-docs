---
phase: 12-workflow-editor-fixes-config-panel-alignment
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - packages/frontend/src/components/workflow/config/ConfigPanel.tsx
  - packages/frontend/src/components/workflow/config/RuntimeSettings.tsx
  - packages/frontend/src/components/workflow/config/InputTransformConfig.tsx
  - packages/frontend/src/components/workflow/config/DesensitizeConfig.tsx
  - packages/frontend/src/components/workflow/config/ModelCallConfig.tsx
  - packages/frontend/src/components/workflow/config/RestoreConfig.tsx
  - packages/frontend/src/components/workflow/config/ExportConfig.tsx
  - packages/frontend/src/components/workflow/config/OutputsEditor.tsx
autonomous: true
requirements:
  - FLOW-04
  - FLOW-05
  - FLOW-06
  - FLOW-07
  - FLOW-08
must_haves:
  truths:
    - "Every field in shared NodeConfig types has a corresponding UI control in the config panel"
    - "ModelCallConfig uses modelIds[] checkbox multi-select, not single modelId dropdown"
    - "All 5 config panels have a RuntimeSettings collapsible section at the bottom with autoAdvance/allowEdit/skippable toggles"
    - "InputTransformConfig labels fields as '用户输入项' and auto-generates name from label"
    - "DesensitizeConfig shows category editor (name+description) with system-defined placeholder format"
    - "ExportConfig includes PPT format option"
    - "Manual OutputsEditor is removed from ConfigPanel"
  artifacts:
    - path: "packages/frontend/src/components/workflow/config/RuntimeSettings.tsx"
      provides: "Shared collapsible runtime settings section"
      exports: ["default"]
    - path: "packages/frontend/src/components/workflow/config/ConfigPanel.tsx"
      provides: "Config panel without OutputsEditor, with RuntimeSettings"
    - path: "packages/frontend/src/components/workflow/config/ModelCallConfig.tsx"
      provides: "Multi-select model checkboxes"
  key_links:
    - from: "packages/frontend/src/components/workflow/config/ConfigPanel.tsx"
      to: "packages/frontend/src/components/workflow/config/RuntimeSettings.tsx"
      via: "renders RuntimeSettings for every node type"
      pattern: "RuntimeSettings"
---

<objective>
Overhaul all 5 config panels to align with updated shared types and runtime expectations, add RuntimeSettings component, remove manual OutputsEditor.

Purpose: Config panels are the primary UI for defining node behavior. They must produce configs that Phase 5 runtime can consume directly.
Output: 7 updated/new config components fully aligned with shared types.
</objective>

<execution_context>
@/Users/dongli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dongli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-workflow-editor-fixes-config-panel-alignment/12-CONTEXT.md
@.planning/phases/12-workflow-editor-fixes-config-panel-alignment/12-01-SUMMARY.md

<interfaces>
<!-- Types after Plan 01 updates -->
From packages/shared/src/types.ts (after Plan 01):
```typescript
export interface DesensitizeCategory {
  id: string;
  name: string;
  description: string;
}

export interface DesensitizeConfig {
  type: "desensitize";
  categories: DesensitizeCategory[];
  localModelId: string | null;
  autoAdvance?: boolean;
  allowEdit?: boolean;
  skippable?: boolean;
}

export interface ExportConfig {
  type: "export";
  format: "word" | "pdf" | "markdown" | "ppt";
  templateId: string | null;
  contentMapping: VariableRef[];
  autoAdvance?: boolean;
  allowEdit?: boolean;
  skippable?: boolean;
}

export interface FormFieldDef {
  id: string;
  /** @deprecated Auto-generated from label */
  name?: string;
  label: string;
  type: "text" | "textarea" | "file";
  required: boolean;
}

export interface ModelCallConfig {
  type: "model_call";
  displayName: string;
  modelIds: string[];
  /** @deprecated */
  modelId?: string | null;
  promptTemplate: string;
  inputRefs: VariableRef[];
  autoAdvance?: boolean;
  allowEdit?: boolean;
  skippable?: boolean;
}
```

From packages/frontend/src/components/workflow/config/ConfigPanel.tsx (current):
- Imports and renders OutputsEditor (to be removed)
- Props include onOutputsChange (to be removed — Plan 01 already removed it from WorkflowEditor)
- Switch/Match routes to 5 config components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RuntimeSettings and update ConfigPanel</name>
  <files>packages/frontend/src/components/workflow/config/RuntimeSettings.tsx, packages/frontend/src/components/workflow/config/ConfigPanel.tsx, packages/frontend/src/components/workflow/config/OutputsEditor.tsx</files>
  <action>
**Step A: Create RuntimeSettings.tsx**

Create `packages/frontend/src/components/workflow/config/RuntimeSettings.tsx`:
- A collapsible section titled "运行时设置" with a chevron toggle
- Three boolean toggles:
  - `autoAdvance` (自动流转) — default false — "节点完成后自动进入下一步"
  - `allowEdit` (允许编辑) — default true — "允许用户编辑节点输出"
  - `skippable` (可跳过) — default false — "允许用户跳过此节点"
- Props: `config: { autoAdvance?: boolean; allowEdit?: boolean; skippable?: boolean }`, `onChange: (patch: { autoAdvance?: boolean; allowEdit?: boolean; skippable?: boolean }) => void`
- Use consistent styling: toggle switches or checkboxes with labels, slate-600 text, xs font size
- Collapsed by default, click header to expand

**Step B: Update ConfigPanel.tsx**

1. Remove `OutputsEditor` import and its rendering at the bottom of the panel body
2. Remove `onOutputsChange` from `ConfigPanelProps` interface and all references
3. Add `RuntimeSettings` import
4. After each config panel's `<Match>` content (inside the scrollable panel body), render `<RuntimeSettings>` with the current node's config. The RuntimeSettings onChange should merge the patch into the config and call `handleConfigChange`.
5. Replace emoji icons (NODE_TYPE_ICONS) with simple colored dot indicators or SVG icons — per ui-ux-pro-max rule "no emoji icons". Use small colored circles matching the node type color scheme:
   - input_transform: blue dot
   - desensitize: orange dot
   - model_call: purple dot
   - restore: green dot
   - export: red dot

**Step C: Delete or gut OutputsEditor.tsx**

Since outputs are now auto-derived (Plan 01), delete the content of OutputsEditor.tsx and replace with a simple read-only display showing auto-derived outputs (for user visibility). Or delete the file entirely if ConfigPanel no longer imports it.

Preferred: Delete the file entirely. ConfigPanel will no longer reference it.
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && test -f packages/frontend/src/components/workflow/config/RuntimeSettings.tsx && echo "RuntimeSettings exists" && grep -c "RuntimeSettings" packages/frontend/src/components/workflow/config/ConfigPanel.tsx && grep -c "OutputsEditor" packages/frontend/src/components/workflow/config/ConfigPanel.tsx</automated>
  </verify>
  <done>RuntimeSettings.tsx exists with 3 boolean toggles, ConfigPanel renders RuntimeSettings for all node types, OutputsEditor removed from ConfigPanel (grep count = 0), no emoji icons in ConfigPanel</done>
</task>

<task type="auto">
  <name>Task 2: Update all 5 config panels for type alignment</name>
  <files>packages/frontend/src/components/workflow/config/InputTransformConfig.tsx, packages/frontend/src/components/workflow/config/DesensitizeConfig.tsx, packages/frontend/src/components/workflow/config/ModelCallConfig.tsx, packages/frontend/src/components/workflow/config/RestoreConfig.tsx, packages/frontend/src/components/workflow/config/ExportConfig.tsx</files>
  <action>
**InputTransformConfig.tsx:**
1. Change section header from "表单字段" to "用户输入项"
2. Remove the `name` input field (the one with placeholder "字段名 (英文)"). The name is auto-generated from label — no UI needed.
3. Ensure `label` input is prominent (it becomes the primary field identifier)
4. Keep type selector (单行文本/多行文本/文件上传) and required checkbox
5. Keep file upload toggle and accepted file types section

**DesensitizeConfig.tsx:**
1. Replace `ruleTypes` string array editor with a `categories` editor:
   - "脱敏类别" section header with "添加类别" button
   - Each category card: name input ("类别名称", e.g. "公司名称"), description textarea ("描述", e.g. "用于识别和替换公司名称信息")
   - Delete button per category, reorder buttons
   - Add new category with `id: crypto.randomUUID(), name: "", description: ""`
2. Remove `placeholderFormat` input entirely — system uses fixed `[TYPE_N]` format. Show a small info text: "占位符格式由系统自动生成（如 [COMPANY_1]、[PERSON_1]）"
3. Keep `localModelId` model selector (dropdown of local/private models)

**ModelCallConfig.tsx:**
1. Replace single `<select>` model dropdown with checkbox multi-select list:
   - Group by provider (keep existing provider grouping)
   - Each model is a checkbox: `checked={config.modelIds.includes(model.id)}`
   - Toggle adds/removes from modelIds array
   - Show selected count: "已选 N 个模型"
2. Remove all references to deprecated `modelId` field — only use `modelIds[]`
3. Keep displayName input, promptTemplate PromptEditor, and existing upstream variable system

**RestoreConfig.tsx:**
1. Keep `pairedDesensitizeNodeId` selector as-is (dropdown of desensitize nodes)
2. No other changes needed for this simple config — RuntimeSettings added by ConfigPanel

**ExportConfig.tsx:**
1. Add "PPT" option to format selector (radio buttons or select): value "ppt", label "PPT"
2. Keep contentMapping VariableRef editor
3. Ensure no "user content input" field exists — content comes from upstream via contentMapping only
  </action>
  <verify>
    <automated>cd /Users/dongli/Workspace/intelliflow-docs && grep -c "用户输入项" packages/frontend/src/components/workflow/config/InputTransformConfig.tsx && grep -c "categories" packages/frontend/src/components/workflow/config/DesensitizeConfig.tsx && grep -c "modelIds" packages/frontend/src/components/workflow/config/ModelCallConfig.tsx && grep -c "ppt" packages/frontend/src/components/workflow/config/ExportConfig.tsx</automated>
  </verify>
  <done>InputTransformConfig shows "用户输入项" (not "表单字段"), no name field input; DesensitizeConfig uses categories editor; ModelCallConfig uses modelIds[] checkbox multi-select; ExportConfig includes PPT format; RestoreConfig unchanged except RuntimeSettings from ConfigPanel</done>
</task>

</tasks>

<verification>
1. RuntimeSettings.tsx exists with autoAdvance/allowEdit/skippable toggles
2. ConfigPanel renders RuntimeSettings for every selected node
3. OutputsEditor no longer imported by ConfigPanel
4. InputTransformConfig: "用户输入项" header, no name field input
5. DesensitizeConfig: categories editor with name+description, no placeholderFormat input
6. ModelCallConfig: checkbox list grouped by provider, uses modelIds[] array
7. ExportConfig: format includes PPT option
8. No emoji icons in ConfigPanel header
</verification>

<success_criteria>
- All 5 config panels produce configs matching the updated shared types from Plan 01
- RuntimeSettings collapsible section appears at the bottom of every node's config
- ModelCallConfig uses multi-select checkboxes for modelIds[]
- OutputsEditor removed — outputs are auto-derived (Plan 01)
- Config panel styling consistent across all node types
</success_criteria>

<output>
After completion, create `.planning/phases/12-workflow-editor-fixes-config-panel-alignment/12-02-SUMMARY.md`
</output>
